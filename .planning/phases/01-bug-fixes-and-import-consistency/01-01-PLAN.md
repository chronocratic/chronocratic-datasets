---
phase: 01-bug-fixes-and-import-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rbspaper/models/augmentation/strategies.py
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Importing AugmentationMethod from augmentation package does not crash with ImportError"
    - "test_pipeline_core.py collects without import errors"
  artifacts:
    - path: "src/rbspaper/models/augmentation/strategies.py"
      provides: "AugmentationMethod and all concrete strategies"
      min_lines: 50
  key_links:
    - from: "src/rbspaper/models/augmentation/__init__.py"
      to: "src/rbspaper/models/augmentation/strategies.py"
      via: "import AugmentationMethod"
      pattern: "from src.rbspaper.models.augmentation.strategies import"
---

<objective>
Fix circular import: augmentation/__init__.py -> strategies.py -> ts2vec/utils.py -> ts2vec/__init__.py -> ts2vec/model.py -> augmentation/factories.py -> strategies.py (CYCLE).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md

# Circular import chain (verified by running import):
# 1. augmentation/__init__.py imports strategies.py (line 3)
# 2. strategies.py imports ts2vec/utils.py (line 17)
# 3. ts2vec/utils.py triggers ts2vec/__init__.py (package initialization)
# 4. ts2vec/__init__.py imports ts2vec/model.py (line 3)
# 5. ts2vec/model.py imports augmentation/factories.py (line 15)
# 6. augmentation/factories.py imports augmentation/strategies.py (line 14)
# 7. strategies.py is partially initialized — CYCLE CRASH

# The function extract_subsequences_per_row is only used in one method:
# CropShiftAugmentation.augment() — lines 124 and 131
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task: Break circular import via lazy import in CropShiftAugmentation</name>
  <files>src/rbspaper/models/augmentation/strategies.py</files>
  <behavior>
    - Test 1: `from src.rbspaper.models.augmentation import AugmentationMethod` does not raise ImportError
    - Test 2: `from src.rbspaper.models.ts2vec import TS2Vec` does not raise ImportError
    - Test 3: `pytest --collect-only test/test_pipeline_core.py` succeeds without import errors
  </behavior>
  <read_first>
    - src/rbspaper/models/augmentation/strategies.py
    - src/rbspaper/models/augmentation/__init__.py
    - src/rbspaper/models/ts2vec/__init__.py
    - src/rbspaper/models/ts2vec/model.py
    - src/rbspaper/models/ts2vec/utils.py
    - src/rbspaper/models/augmentation/factories.py
  </read_first>
  <action>
    Remove the top-level import `from src.rbspaper.models.ts2vec.utils import extract_subsequences_per_row` (line 17) from `strategies.py`.

    Move the import inside the `CropShiftAugmentation.augment()` method, as a local import at the top of the method body (before first usage on line 124). The function `extract_subsequences_per_row` is a pure utility (no side effects, no state) — a lazy import has zero runtime cost difference.

    Do NOT use TYPE_CHECKING for this — the import is needed at runtime, just not at module load time. The lazy import breaks the chain because strategies.py no longer eagerly pulls in ts2vec/, so ts2vec/__init__.py never fires during strategies.py initialization.

    After the change, verify:
    1. `uv run python -c "from src.rbspaper.models.augmentation import AugmentationMethod; print('OK')"` succeeds
    2. `uv run python -c "from src.rbspaper.models.ts2vec import TS2Vec; print('OK')"` succeeds
    3. `uv run pytest --collect-only test/test_pipeline_core.py 2>&1` shows test collection without ImportError
  </action>
  <verify>
    <automated>uv run pytest --collect-only test/test_pipeline_core.py 2>&1 | grep -c "ImportError" || echo "0 — no import errors"</automated>
  </verify>
  <done>
    - Top-level import of `extract_subsequences_per_row` removed from strategies.py
    - Local import added inside `CropShiftAugmentation.augment()` method
    - All augmentation and ts2vec imports resolve without circular dependency
    - test_pipeline_core.py collects successfully
  </done>
  <acceptance_criteria>
    - `grep -n "extract_subsequences_per_row" src/rbspaper/models/augmentation/strategies.py` shows the import is NOT at the top level (not in lines 1-17)
    - `grep -n "extract_subsequences_per_row" src/rbspaper/models/augmentation/strategies.py` shows the import IS inside the augment method
    - `uv run python -c "from src.rbspaper.models.augmentation import AugmentationMethod"` exits 0
    - `uv run python -c "from src.rbspaper.models.ts2vec import TS2Vec"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — internal module import resolution only.

## STRIDE Threat Register

No applicable threats. This is an internal circular import fix with no security surface.
</threat_model>

<verification>
- `uv run python -c "from src.rbspaper.models.augmentation import AugmentationMethod"` exits 0
- `uv run python -c "from src.rbspaper.pipeline.core import run_experiment_pipeline"` exits 0
- `uv run pytest --collect-only` shows all test files collected (including test_pipeline_core.py)
</verification>

<success_criteria>
- Circular import resolved: augmentation and ts2vec modules import without ImportError
- test_pipeline_core.py collects successfully
- No new ruff or ty errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug-fixes-and-import-consistency/01-01-SUMMARY.md`
</output>
