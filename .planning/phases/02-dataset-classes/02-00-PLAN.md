---
phase: 02
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/datasets/datasets/transformations.py
  - src/tscollection/datasets/utils/common.py
  - src/tscollection/datasets/utils/__init__.py
  - tests/conftest.py
  - tests/test_conftest_fixtures.py
  - tests/test_fixed_dataset.py
  - tests/test_flexible_dataset.py
  - tests/test_strategies.py
  - tests/test_ucr_dataset.py
  - tests/test_ett_dataset.py
  - tests/test_transformations.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "All six utility symbols importable from tscollection.datasets.utils"
    - "conftest.py provides 4 fixtures with correct shapes and dtypes"
  artifacts:
    - path: "src/tscollection/datasets/datasets/transformations.py"
      provides: "convert_numpy_to_tensor, convert_data_to_np_array, expand_data_dimensionality"
      min_lines: 40
    - path: "src/tscollection/datasets/utils/common.py"
      provides: "compose, FunctionComposer, get_num_samples_from_ts"
      min_lines: 30
    - path: "src/tscollection/datasets/utils/__init__.py"
      provides: "Re-exports for all utility symbols"
      contains: "__all__"
    - path: "tests/conftest.py"
      provides: "Synthetic pytest fixtures for dataset tests"
      min_lines: 30
  key_links:
    - from: "transformations.py"
      to: "torch"
      via: "torch.from_numpy zero-copy conversion"
      pattern: "torch\\.from_numpy"
    - from: "common.py"
      to: "collections.abc.Callable"
      via: "Callable type for compose"
      pattern: "from collections\\.abc import Callable"
---

<objective>
Port utility modules from rbspaper and create test infrastructure for Phase 2.

Purpose: Transform functions (numpy-to-tensor, dimension expansion), compose utilities, and pytest fixtures are foundational dependencies for all dataset classes. Test stubs satisfy the Nyquist Rule so later plans have automated verification.

Output: transformations.py, utils/common.py, updated utils/__init__.py, conftest.py, and 6 empty test stub files.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dataset-classes/02-CONTEXT.md
@.planning/phases/02-dataset-classes/02-RESEARCH.md
@.planning/phases/02-dataset-classes/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port transform utilities from rbspaper</name>
  <files>
    - src/tscollection/datasets/datasets/transformations.py
    - src/tscollection/datasets/utils/common.py
    - src/tscollection/datasets/utils/__init__.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/datasets/transformations.py — source of truth for transformations (convert_numpy_to_tensor, convert_data_to_np_array, expand_data_dimensionality)
    - _sources/rbspaper/src/rbspaper/data/utils/common.py — source of truth for compose, FunctionComposer, get_num_samples_from_ts
    - src/tscollection/datasets/utils/__init__.py — current stub with empty __all__
    - ruff.toml — line-length = 100, single quotes, py312 target
  </read_first>
  <action>
    Create transformations.py by porting all three functions from rbspaper (convert_numpy_to_tensor, convert_data_to_np_array, expand_data_dimensionality). Adapt imports to use tscollection.datasets namespace. Add from __future__ import annotations, module docstring, and __all__ = ['convert_data_to_np_array', 'convert_numpy_to_tensor', 'expand_data_dimensionality'].

    Create utils/common.py by porting compose(), FunctionComposer, and get_num_samples_from_ts() from rbspaper. Per D-03, port ONLY these three items — do NOT port load_json, flatten_list, separate_target_feature_from_df, find_project_root, closest_power_of_2, cartesian_product_dict, or AccumulatingTimerCallback. Add from __future__ import annotations, module docstring, and __all__ = ['FunctionComposer', 'compose', 'get_num_samples_from_ts'].

    Update utils/__init__.py to re-export all six symbols from both modules: FunctionComposer, compose, get_num_samples_from_ts, convert_data_to_np_array, convert_numpy_to_tensor, expand_data_dimensionality. Follow the enums/__init__.py pattern (imports + alphabetically sorted __all__).
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.utils import compose, FunctionComposer, get_num_samples_from_ts, convert_numpy_to_tensor, convert_data_to_np_array, expand_data_dimensionality; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - transformations.py exists with all three functions exported in __all__
    - utils/common.py exists with compose, FunctionComposer, get_num_samples_from_ts exported in __all__
    - utils/common.py does NOT contain load_json, flatten_list, or AccumulatingTimerCallback
    - utils/__init__.py re-exports all six symbols with alphabetically sorted __all__
    - All symbols importable from tscollection.datasets.utils
  </acceptance_criteria>
  <done>Utilities ported and importable from tscollection.datasets.utils</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create conftest.py with synthetic fixtures and fixture tests</name>
  <files>
    - tests/conftest.py
    - tests/test_conftest_fixtures.py
  </files>
  <read_first>
    - tests/test_package.py — existing test patterns (function names, docstring style, imports)
    - .planning/phases/02-dataset-classes/02-CONTEXT.md — D-05 decision: synthetic numpy/pandas fixtures
    - .planning/phases/02-dataset-classes/02-PATTERNS.md — conftest.py pattern section with fixture signatures and shapes
  </read_first>
  <action>
    Create conftest.py with four pytest fixtures per the patterns documented in 02-PATTERNS.md:
    - synthetic_classification_df: pd.DataFrame of shape (10, 50), dtype float32 — 10 samples, 50 timesteps
    - synthetic_classification_labels: pd.Series of length 10 with values [0, 1] * 5 — binary labels
    - synthetic_forecast_data: np.ndarray of shape (200, 7), dtype float32 — 200 timesteps, 7 features (ETTh1-like)
    - synthetic_multivariate_data: np.ndarray of shape (5, 30, 4), dtype float32 — 5 samples, 30 timesteps, 4 features (UEA-like)

    All fixtures use @pytest.fixture decorator. Module docstring describes purpose. Follow Google-style docstring convention per CLAUDE.md.

    Create tests/test_conftest_fixtures.py with 4 tests that exercise each fixture and assert shape/dtype:
    - test_classification_df_shape(synthetic_classification_df): assert shape == (10, 50) and dtype == np.float32
    - test_classification_labels_length(synthetic_classification_labels): assert len == 10
    - test_forecast_data_shape(synthetic_forecast_data): assert shape == (200, 7) and dtype == np.float32
    - test_multivariate_data_shape(synthetic_multivariate_data): assert shape == (5, 30, 4) and dtype == np.float32
  </action>
  <verify>
    <automated>uv run pytest tests/test_conftest_fixtures.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists with 4 @pytest.fixture functions
    - synthetic_classification_df returns pd.DataFrame with shape (10, 50) and dtype float32
    - synthetic_classification_labels returns pd.Series with length 10
    - synthetic_forecast_data returns np.ndarray with shape (200, 7) and dtype float32
    - synthetic_multivariate_data returns np.ndarray with shape (5, 30, 4) and dtype float32
    - tests/test_conftest_fixtures.py exists with 4 test functions
    - `uv run pytest tests/test_conftest_fixtures.py -x` exits 0 with 4 passed
  </acceptance_criteria>
  <done>conftest.py has 4 fixtures and test_conftest_fixtures.py validates shapes/dtypes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create empty test stub files</name>
  <files>
    - tests/test_fixed_dataset.py
    - tests/test_flexible_dataset.py
    - tests/test_strategies.py
    - tests/test_ucr_dataset.py
    - tests/test_ett_dataset.py
    - tests/test_transformations.py
  </files>
  <read_first>
    - tests/test_package.py — file structure pattern (module docstring, imports, test function style)
    - .planning/phases/02-dataset-classes/02-VALIDATION.md — test file names and requirement mappings
  </read_first>
  <action>
    Create 6 empty test stub files. Each file must have:
    1. A module-level docstring describing the test scope (e.g., "Tests for fixed-length dataset classes (DST-01, DST-03).")
    2. A single placeholder test function that passes: def test_placeholder(): pass

    File-to-requirement mapping:
    - test_fixed_dataset.py: "Tests for fixed-length dataset classes (DST-01, DST-03)."
    - test_flexible_dataset.py: "Tests for flexible (sliding-window) dataset classes (DST-02, DST-04)."
    - test_strategies.py: "Tests for sequence handling strategies (DST-05)."
    - test_ucr_dataset.py: "Tests for UCR univariate classification wrapper."
    - test_ett_dataset.py: "Tests for ETT forecasting wrapper."
    - test_transformations.py: "Tests for data transformation utilities."
  </action>
  <verify>
    <automated>uv run pytest tests/test_fixed_dataset.py tests/test_flexible_dataset.py tests/test_strategies.py tests/test_ucr_dataset.py tests/test_ett_dataset.py tests/test_transformations.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - All 6 test files exist under tests/
    - Each file has a module docstring referencing its scope
    - Each file has at least one passing placeholder test
    - pytest runs all 6 files without errors (all pass)
  </acceptance_criteria>
  <done>All 6 test stub files exist and pass pytest</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external data -> transforms | User-provided numpy/pandas arrays cross into transform pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Spoofing | convert_numpy_to_tensor | mitigate | Validate input is np.ndarray via isinstance check; raise TypeError for non-array inputs |
| T-02-00-02 | Denial of Service | expand_data_dimensionality | mitigate | Validate expand_dims_axis is within valid range (0 to ndim) before calling np.expand_dims |
| T-02-00-03 | Repudiation | FunctionComposer | accept | Pure functional composition with no side effects; no user data persisted |
</threat_model>

<verification>
Run full test suite: `uv run pytest tests/ -q --tb=short` — should pass all existing tests plus 6 new stubs.
</verification>

<success_criteria>
- transformations.py exports convert_numpy_to_tensor, convert_data_to_np_array, expand_data_dimensionality
- utils/common.py exports compose, FunctionComposer, get_num_samples_from_ts (3 items only)
- utils/__init__.py re-exports all 6 symbols
- conftest.py provides 4 synthetic fixtures with correct shapes
- 6 test stub files exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-dataset-classes/02-00-SUMMARY.md`
</output>
