---
phase: 02
plan: 01
type: execute
wave: 2
depends_on: [02-00]
files_modified:
  - src/tscollection/datasets/datasets/classes/strategies.py
  - src/tscollection/datasets/datasets/classes/__init__.py
  - tests/test_strategies.py
autonomous: true
requirements: [DST-05]
must_haves:
  truths:
    - "ForecastingStrategySingleFile.get_num_sequences returns correct window count for known inputs"
    - "ClassificationStrategySingleFile.get_current_label returns None when labels=None"
    - "ClassificationStrategyMultipleFiles handles list[np.ndarray] input"
  artifacts:
    - path: "src/tscollection/datasets/datasets/classes/strategies.py"
      provides: "SequenceHandlingStrategy ABC and 3 concrete implementations"
      min_lines: 130
    - path: "tests/test_strategies.py"
      provides: "Verification tests for strategy behaviors"
      min_lines: 80
  key_links:
    - from: "strategies.py"
      to: "utils/common.py"
      via: "get_num_samples_from_ts import"
      pattern: "from tscollection.datasets.utils import get_num_samples_from_ts"
---

<objective>
Port the SequenceHandlingStrategy pattern from rbspaper, decoupling sequence counting and label extraction from dataset base classes.

Purpose: DST-05 requires strategy pattern to decouple sequence counting/label extraction. Strategies are independent of dataset ABCs (they only depend on utils/common.py which Wave 1 created), enabling parallelism with Plan 02.

Output: strategies.py with 6 exported classes, updated classes/__init__.py, verification tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dataset-classes/02-CONTEXT.md
@.planning/phases/02-dataset-classes/02-RESEARCH.md
@.planning/phases/02-dataset-classes/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port strategy classes from rbspaper</name>
  <files>
    - src/tscollection/datasets/datasets/classes/strategies.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/datasets/strategies.py — full source of truth for all 6 strategy classes
    - src/tscollection/datasets/utils/__init__.py — confirms get_num_samples_from_ts is available
    - .planning/phases/02-dataset-classes/02-PATTERNS.md — strategies.py pattern section with imports, ABCs, concrete classes, and __all__
  </read_first>
  <behavior>
    - ForecastingStrategySingleFile.get_num_sequences returns correct count for known array/seq_len/step/forecast_horizon inputs
    - ForecastingStrategySingleFile.get_current_label returns data[n + seq_len : n + seq_len + forecast_horizon]
    - ClassificationStrategySingleFile.get_num_sequences returns correct count for known array/seq_len/step inputs
    - ClassificationStrategySingleFile.get_current_label returns labels[n : n + seq_len] when labels provided
    - ClassificationStrategySingleFile.get_current_label returns None when labels is None
    - ClassificationStrategyMultipleFiles.get_num_sequences returns sum of per-file counts
    - ClassificationStrategyMultipleFiles.get_num_sequences_per_file returns per-file sequence counts
  </behavior>
  <action>
    Port all 6 classes from rbspaper strategies.py to tscollection.datasets.datasets.classes.strategies:
    - SequenceHandlingStrategy (ABC) — abstract methods: get_num_sequences(data, seq_len, step) -> int, get_current_label(data, labels, n, seq_len, **kwargs) -> np.ndarray | None
    - SequenceHandlingStrategySingleFile (ABC, inherits SequenceHandlingStrategy) — no new methods
    - SequenceHandlingStrategyMultipleFiles (ABC, inherits SequenceHandlingStrategy) — adds abstract get_num_sequences_per_file(data, seq_len, step) -> list[int]
    - ForecastingStrategySingleFile (inherits SequenceHandlingStrategySingleFile) — __init__ takes forecast_horizon: int; get_num_sequences uses get_num_samples_from_ts for window counting; get_current_label returns data[n + seq_len : n + seq_len + forecast_horizon]
    - ClassificationStrategySingleFile (inherits SequenceHandlingStrategySingleFile) — get_num_sequences counts windows; get_current_label returns labels[n : n + seq_len] or None
    - ClassificationStrategyMultipleFiles (inherits SequenceHandlingStrategyMultipleFiles) — get_num_sequences_per_file iterates data list; get_num_sequences handles both list and ndarray inputs; get_current_label uses kwargs.get('current_file', 0) for file routing

    Adapt all imports from src.rbspaper to tscollection.datasets. Use from __future__ import annotations. Add module docstring. Set __all__ = ['ClassificationStrategyMultipleFiles', 'ClassificationStrategySingleFile', 'ForecastingStrategySingleFile', 'SequenceHandlingStrategy', 'SequenceHandlingStrategyMultipleFiles', 'SequenceHandlingStrategySingleFile'] (alphabetically sorted).
  </action>
  <verify>
    <automated>uv run pytest tests/test_strategies.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - strategies.py exists with 6 classes in __all__
    - SequenceHandlingStrategy is abstract (cannot be instantiated directly)
    - ForecastingStrategySingleFile(forecast_horizon=24).get_num_sequences(arr_200x7, seq_len=96, step=1) returns expected count
    - ForecastingStrategySingleFile(forecast_horizon=24).get_current_label returns correct slice from data array
    - ClassificationStrategySingleFile returns None for get_current_label when labels=None
    - ClassificationStrategyMultipleFiles.get_num_sequences handles list[np.ndarray] input
    - All classes importable from tscollection.datasets.datasets.classes.strategies
  </acceptance_criteria>
  <done>strategies.py ports 6 classes from rbspaper with correct tscollection.datasets imports</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update classes/__init__.py with strategy exports</name>
  <files>
    - src/tscollection/datasets/datasets/classes/__init__.py
  </files>
  <read_first>
    - src/tscollection/datasets/datasets/classes/__init__.py — current stub with __all__ = []
    - src/tscollection/datasets/enums/__init__.py — reference pattern for re-export + __all__ structure
  </read_first>
  <action>
    Update classes/__init__.py to import and re-export all 6 strategy classes from strategies.py. Follow enums/__init__.py pattern: explicit from imports + alphabetically sorted __all__ list. Set __all__ = ['ClassificationStrategyMultipleFiles', 'ClassificationStrategySingleFile', 'ForecastingStrategySingleFile', 'SequenceHandlingStrategy', 'SequenceHandlingStrategyMultipleFiles', 'SequenceHandlingStrategySingleFile']. Plan 02 will append dataset ABC exports to this file later.
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.datasets.classes import ForecastingStrategySingleFile, ClassificationStrategySingleFile, ClassificationStrategyMultipleFiles, SequenceHandlingStrategy, SequenceHandlingStrategySingleFile, SequenceHandlingStrategyMultipleFiles; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - classes/__init__.py imports all 6 strategy classes from strategies submodule
    - __all__ contains all 6 names in alphabetical order
    - All 6 classes importable from tscollection.datasets.datasets.classes
  </acceptance_criteria>
  <done>classes/__init__.py exports all 6 strategy classes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement strategy tests</name>
  <files>
    - tests/test_strategies.py
  </files>
  <read_first>
    - tests/conftest.py — synthetic fixtures available (from Wave 1)
    - _sources/rbspaper/src/rbspaper/data/datasets/strategies.py — reference for expected behaviors and edge cases
    - .planning/phases/02-dataset-classes/02-RESEARCH.md — Common Pitfalls section #2 (ForecastingStrategy inefficiency) and #3 (transform ordering)
    - tests/test_package.py — test naming convention and docstring patterns
  </read_first>
  <behavior>
    - test_forecasting_num_sequences: ForecastingStrategySingleFile(forecast_horizon=24) with data shape (200, 7), seq_len=96, step=1 returns 80 sequences
    - test_forecasting_label_slice: get_current_label(data, None, n=0, seq_len=96) returns data[96:120]
    - test_classification_num_sequences: ClassificationStrategySingleFile with data shape (200,), seq_len=50, step=10 returns correct count
    - test_classification_label_with_labels: get_current_label returns labels[n:n+seq_len]
    - test_classification_label_none: get_current_label returns None when labels is None
    - test_multifile_num_sequences: ClassificationStrategyMultipleFiles with list of 2 arrays returns sum of per-file counts
    - test_multifile_per_file_counts: get_num_sequences_per_file returns list of 2 integers
    - test_abstract_cannot_instantiate: SequenceHandlingStrategy raises TypeError on instantiation
  </behavior>
  <action>
    Replace placeholder test in test_strategies.py with 8 test functions covering all 5 concrete strategy behaviors:
    1. test_forecasting_num_sequences — verify count with known array shape
    2. test_forecasting_label_slice — verify label is correct data slice after window
    3. test_classification_num_sequences — verify count for single-file classification
    4. test_classification_label_with_labels — verify label extraction when labels provided
    5. test_classification_label_none — verify None returned when labels=None
    6. test_multifile_num_sequences — verify sum of per-file counts for list input
    7. test_multifile_per_file_counts — verify get_num_sequences_per_file returns correct list
    8. test_abstract_cannot_instantiate — verify SequenceHandlingStrategy is abstract

    Use synthetic fixtures from conftest.py and inline numpy arrays where needed. Docstrings reference DST-05.
  </action>
  <verify>
    <automated>uv run pytest tests/test_strategies.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_strategies.py contains 8 test functions (no placeholder)
    - All 8 tests pass when run with uv run pytest
    - Each test docstring references DST-05
  </acceptance_criteria>
  <done>test_strategies.py has 8 passing tests covering all strategy behaviors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy -> data array | Strategy methods receive user-controlled numpy arrays; bounds checking required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Spoofing | ForecastingStrategySingleFile.get_num_sequences | mitigate | Validate seq_len > 0 and step > 0 in caller; strategy assumes pre-validated inputs per D-01 (module-only API) |
| T-02-01-02 | Repudiation | ClassificationStrategyMultipleFiles.get_current_label | mitigate | kwargs.get('current_file', 0) defaults safely; validate list indices against data length |
| T-02-01-03 | Denial of Service | SequenceHandlingStrategy.get_num_sequences | accept | Pure computation with no I/O; worst case is slow list construction for very long arrays (noted in RESEARCH.md Pitfall #2) |
</threat_model>

<verification>
Run strategy tests: `uv run pytest tests/test_strategies.py -x --tb=short -v` -- all 8 tests pass.
Verify imports: `uv run python -c "from tscollection.datasets.datasets.classes import *; print(len(__all__))"` -- should print 6.
</verification>

<success_criteria>
- strategies.py has 6 classes (3 ABCs + 3 concrete) matching rbspaper source
- ForecastingStrategySingleFile correctly counts sequences and extracts post-window labels
- ClassificationStrategySingleFile handles labels=None returning None
- ClassificationStrategyMultipleFiles handles list[np.ndarray] with per-file counts
- classes/__init__.py re-exports all 6 strategy classes
- test_strategies.py has 8 passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-dataset-classes/02-01-SUMMARY.md`
</output>
