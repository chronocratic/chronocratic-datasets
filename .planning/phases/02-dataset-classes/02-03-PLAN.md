---
phase: 02
plan: 03
type: execute
wave: 4
depends_on: [02-00, 02-01, 02-02]
files_modified:
  - src/tscollection/datasets/datasets/ucr.py
  - src/tscollection/datasets/datasets/uea.py
  - src/tscollection/datasets/datasets/ett.py
  - src/tscollection/datasets/datasets/__init__.py
  - tests/test_ucr_dataset.py
  - tests/test_ett_dataset.py
  - tests/test_transformations.py
autonomous: true
requirements: [DST-01, DST-02, DST-05]
must_haves:
  truths:
    - "UCRClassificationUnivariateDataset yields (torch.Tensor, int) tuples for classification"
    - "ETTDataset yields (torch.Tensor, torch.Tensor) tuples for forecasting"
    - "All 16 classes importable from tscollection.datasets.datasets"
  artifacts:
    - path: "src/tscollection/datasets/datasets/ucr.py"
      provides: "UCR univariate classification wrapper"
      min_lines: 14
    - path: "src/tscollection/datasets/datasets/uea.py"
      provides: "UEA multivariate classification wrapper"
      min_lines: 14
    - path: "src/tscollection/datasets/datasets/ett.py"
      provides: "ETT forecasting wrapper with strategy injection"
      min_lines: 30
    - path: "src/tscollection/datasets/datasets/__init__.py"
      provides: "Convenience exports for all dataset classes"
      contains: "__all__"
  key_links:
    - from: "ucr.py"
      to: "fixed.py"
      via: "FixedTimeSeriesDatasetUnivariate inheritance"
      pattern: "class UCRClassificationUnivariateDataset\\(FixedTimeSeriesDatasetUnivariate\\)"
    - from: "ett.py"
      to: "flexible.py"
      via: "FlexibleTimeSeriesDatasetSingleFile inheritance"
      pattern: "class ETTDataset\\(FlexibleTimeSeriesDatasetSingleFile\\)"
    - from: "ett.py"
      to: "strategies.py"
      via: "ForecastingStrategySingleFile injection"
      pattern: "ForecastingStrategySingleFile\\(forecast_horizon"
---

<objective>
Create concrete dataset wrappers (UCR, UEA, ETT) and finalize export wiring.

Purpose: Thin wrappers per D-04 that set domain defaults (expand_dims_axis, strategy injection, transformations) and delegate to ABC bases. These are the user-facing classes that modules (Phase 5) will instantiate.

Output: ucr.py, uea.py, ett.py, fully populated datasets/__init__.py, wrapper and transformation tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dataset-classes/02-CONTEXT.md
@.planning/phases/02-dataset-classes/02-RESEARCH.md
@.planning/phases/02-dataset-classes/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create concrete dataset wrappers</name>
  <files>
    - src/tscollection/datasets/datasets/ucr.py
    - src/tscollection/datasets/datasets/uea.py
    - src/tscollection/datasets/datasets/ett.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/datasets/ucr_dataset.py — UCR wrapper (14 lines, verbatim port target)
    - _sources/rbspaper/src/rbspaper/data/datasets/uea_dataset.py — UEA wrapper (thin, accepts np.ndarray)
    - _sources/rbspaper/src/rbspaper/data/datasets/ett_dataset.py — ETT wrapper (injects ForecastingStrategySingleFile)
    - .planning/phases/02-dataset-classes/02-CONTEXT.md — D-04: thin wrappers set domain defaults and delegate; D-01: accept pre-loaded data only
    - .planning/phases/02-dataset-classes/02-PATTERNS.md — wrapper pattern sections with exact signatures
  </read_first>
  <behavior>
    - UCRClassificationUnivariateDataset with pd.DataFrame data yields (Tensor, int) in WITH_LABELS mode
    - UCRClassificationUnivariateDataset expands dims along axis=1 (default)
    - UEAClassificationMultivariateDataset with 3D np.ndarray yields data without dim expansion (expand_dims_axis=None)
    - ETTDataset injects ForecastingStrategySingleFile(forecast_horizon=...) in __init__
    - ETTDataset uses mode=TimeSeriesDatasetMode.FORECASTING
    - ETTDataset yields (input_tensor, target_tensor) tuples
  </behavior>
  <action>
    Create 3 thin wrapper files (port from rbspaper, adapt imports to tscollection.datasets):

    1. ucr.py — UCRClassificationUnivariateDataset inherits FixedTimeSeriesDatasetUnivariate. __init__(data: pd.DataFrame, labels: pd.Series | None, mode: TimeSeriesDatasetMode, expand_dims_axis: int = 1, transformations_sequence: tuple = (convert_numpy_to_tensor,)). Delegates to super().__init__ with all params. Per D-04: sets expand_dims_axis=1 and transformations_sequence=(convert_numpy_to_tensor,) as defaults.

    2. uea.py — UEAClassificationMultivariateDataset inherits FixedTimeSeriesDatasetMultivariate. __init__(data: np.ndarray, labels: pd.Series | None, mode: TimeSeriesDatasetMode, expand_dims_axis: int | None = None, transformations_sequence: tuple = (convert_numpy_to_tensor,)). Accepts 3D ndarray (samples, timesteps, features). No dim expansion by default.

    3. ett.py — ETTDataset inherits FlexibleTimeSeriesDatasetSingleFile. __init__(data: np.ndarray, seq_len: int, step: int, forecast_horizon: int, transformations_sequence: tuple = (convert_numpy_to_tensor, convert_data_to_np_array)). Injects ForecastingStrategySingleFile(forecast_horizon=forecast_horizon) and mode=TimeSeriesDatasetMode.FORECASTING. Hardcodes labels=None. Sets expand_dims_axis=None.

    All files: from __future__ import annotations, module docstring, __all__ with single class name. Use keyword-only enforcement where rbspaper does.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ucr_dataset.py tests/test_ett_dataset.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - ucr.py exists with UCRClassificationUnivariateDataset class (14-20 lines)
    - uea.py exists with UEAClassificationMultivariateDataset class
    - ett.py exists with ETTDataset class that creates ForecastingStrategySingleFile internally
    - Each file has module docstring and __all__ with single entry
    - All 3 classes use from __future__ import annotations
    - UCR uses expand_dims_axis=1, UEA uses expand_dims_axis=None, ETT uses expand_dims_axis=None
    - ETT sets mode=TimeSeriesDatasetMode.FORECASTING and labels=None
  </acceptance_criteria>
  <done>All 3 wrapper files created with correct inheritance, defaults, and imports</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Finalize export wiring in datasets/__init__.py</name>
  <files>
    - src/tscollection/datasets/datasets/__init__.py
  </files>
  <read_first>
    - src/tscollection/datasets/datasets/__init__.py — current stub with __all__ = []
    - src/tscollection/datasets/enums/__init__.py — reference pattern for re-exports
    - .planning/phases/02-dataset-classes/02-PATTERNS.md — datasets/__init__.py target pattern with full export list
  </read_first>
  <action>
    Update datasets/__init__.py to re-export all classes from this phase. Import from three sources:
    1. From tscollection.datasets.datasets.classes: re-export all 13 ABCs and strategies (FixedTimeSeriesDataset, FixedTimeSeriesDatasetMultivariate, FixedTimeSeriesDatasetUnivariate, FlexibleTimeSeriesDataset, FlexibleTimeSeriesDatasetMultipleFiles, FlexibleTimeSeriesDatasetSingleFile, SequenceHandlingStrategy, ForecastingStrategySingleFile, ClassificationStrategySingleFile, ClassificationStrategyMultipleFiles, TimeSeriesDataset)
    2. From tscollection.datasets.datasets.ett: ETTDataset
    3. From tscollection.datasets.datasets.ucr: UCRClassificationUnivariateDataset
    4. From tscollection.datasets.datasets.uea: UEAClassificationMultivariateDataset

    Set __all__ with 16 entries (13 from classes + 3 concrete wrappers) in alphabetical order. Keep module docstring: "Time series dataset classes (PyTorch Dataset)."
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.datasets import UCRClassificationUnivariateDataset, ETTDataset, UEAClassificationMultivariateDataset, FixedTimeSeriesDataset, FlexibleTimeSeriesDatasetSingleFile; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - datasets/__init__.py has __all__ with 16 entries in alphabetical order
    - UCRClassificationUnivariateDataset importable from tscollection.datasets.datasets
    - ETTDataset importable from tscollection.datasets.datasets
    - UEAClassificationMultivariateDataset importable from tscollection.datasets.datasets
    - FixedTimeSeriesDataset importable from tscollection.datasets.datasets
    - FlexibleTimeSeriesDatasetSingleFile importable from tscollection.datasets.datasets
  </acceptance_criteria>
  <done>datasets/__init__.py exports all 16 classes from this phase</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement wrapper and transformation tests</name>
  <files>
    - tests/test_ucr_dataset.py
    - tests/test_ett_dataset.py
    - tests/test_transformations.py
  </files>
  <read_first>
    - tests/conftest.py — synthetic fixtures available
    - tests/test_package.py — test naming and style conventions
    - src/tscollection/datasets/datasets/transformations.py — source to test (created by Plan 00)
    - .planning/phases/02-dataset-classes/02-RESEARCH.md — Common Pitfalls #3 (expand_dims_axis type inconsistency)
    - .planning/phases/02-dataset-classes/02-PATTERNS.md — test_datasets.py pattern section
  </read_first>
  <behavior>
    - UCR dataset with synthetic (10, 50) DataFrame yields (Tensor shape (1, 50), int label)
    - ETT dataset with synthetic (200, 7) data yields (Tensor shape (96, 7), Tensor shape (24, 7)) for seq_len=96, forecast_horizon=24
    - convert_numpy_to_tensor returns torch.Tensor with correct dtype
    - expand_data_dimensionality adds dimension at specified axis
    - convert_data_to_np_array converts list/tuple to numpy array
  </behavior>
  <action>
    Implement 3 test files:

    1. test_ucr_dataset.py — Replace placeholder:
       - test_ucr_yields_data_label: Create UCRClassificationUnivariateDataset with synthetic_classification_df + labels in WITH_LABELS mode. Verify ds[0] returns (torch.Tensor, int). Verify ds[0][0].shape has 2 dimensions with first dim = 1 (expand_dims_axis=1).
       - test_ucr_without_labels: Verify ds[0] returns single tensor (not tuple) in WITHOUT_LABELS mode.
       - test_ucr_length: Verify len(ds) equals number of rows in DataFrame.

    2. test_ett_dataset.py — Replace placeholder:
       - test_ett_yields_input_target: Create ETTDataset with synthetic_forecast_data, seq_len=96, step=1, forecast_horizon=24. Verify ds[0] returns (input_tensor, target_tensor). Verify input shape is (96, 7) and target shape is (24, 7).
       - test_ett_length: Verify len(ds) matches expected sequence count for known data shape.
       - test_ett_forecast_horizon: Verify target tensor has forecast_horizon timesteps.

    3. test_transformations.py — Replace placeholder:
       - test_convert_numpy_to_tensor_float: Verify output is torch.Tensor with dtype torch.float.
       - test_convert_numpy_to_tensor_long: Verify output dtype is torch.long.
       - test_expand_data_dimensionality: Verify 1D array expanded to 2D along axis=1.
       - test_convert_data_to_np_array: Verify list input converted to np.ndarray with correct dtype.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ucr_dataset.py tests/test_ett_dataset.py tests/test_transformations.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - test_ucr_dataset.py has 3 test functions that pass
    - test_ett_dataset.py has 3 test functions that pass
    - test_transformations.py has 4 test functions that pass
    - UCR dataset yields torch.Tensor with shape (1, seq_len) in WITH_LABELS mode
    - ETT dataset yields (input_tensor, target_tensor) with correct shapes
    - Each test docstring references relevant requirement ID (DST-01, DST-02)
  </acceptance_criteria>
  <done>All wrapper and transformation tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wrapper constructor -> ABC base | Concrete wrappers pass user data to base classes; type validation inherited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | UCRClassificationUnivariateDataset.__init__ | mitigate | Base class (FixedTimeSeriesDatasetUnivariate) expects pd.DataFrame; type hint enforces this. Data passed through to base which accesses .iloc — will raise AttributeError for non-DataFrame inputs |
| T-02-03-02 | Spoofing | ETTDataset.__init__ | mitigate | Base class expects np.ndarray; ForecastingStrategySingleFile receives user-controlled forecast_horizon — validate forecast_horizon > 0 |
| T-02-03-03 | Injection | transformations_sequence default | accept | Default callables are internal (convert_numpy_to_tensor, convert_data_to_np_array); user can override but this is their own code applied to their own data |
</threat_model>

<verification>
Run all Phase 2 tests: `uv run pytest tests/ -x --tb=short -v` — full suite passes.
Verify exports: `uv run python -c "from tscollection.datasets.datasets import *; print(len(__all__))"` — should print 16.
</verification>

<success_criteria>
- UCRClassificationUnivariateDataset yields (Tensor(1, seq_len), int) for WITH_LABELS mode
- UEAClassificationMultivariateDataset yields (Tensor(seq_len, features), label) for 3D input
- ETTDataset yields (input_tensor, target_tensor) with correct window shapes
- datasets/__init__.py exports 16 classes
- test_ucr_dataset.py, test_ett_dataset.py, test_transformations.py have passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-dataset-classes/02-03-SUMMARY.md`
</output>
