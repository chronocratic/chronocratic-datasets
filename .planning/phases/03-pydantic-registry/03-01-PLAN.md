---
phase: 03-pydantic-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/datasets/enums/data.py
  - src/tscollection/datasets/enums/__init__.py
  - src/tscollection/datasets/config/base.py
  - tests/conftest.py
  - tests/test_config_enums.py
  - tests/test_config_base.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]
user_setup: []
must_haves:
  truths:
    - "DatasetFamily enum has all 8 family values (ucr, uea, ett, electricity, weather, exchange, traffic, illness)"
    - "SplitMode enum has INDEXED and FRACTIONAL values"
    - "New enums are importable from tscollection.datasets.enums"
    - "DatasetConfig is abstract and frozen (ConfigDict with frozen=True)"
    - "ClassificationConfig inherits DatasetConfig with target_col_name, file_patterns, split_strategy"
    - "ForecastingConfig inherits DatasetConfig with split_mode, split_bounds, default_seq_len, default_horizon"
    - "@field_validator on DatasetConfig validates sha256 format (64-char hex or None)"
    - "@model_validator(mode='after') on ClassificationConfig ensures data_form is not None"
    - "@model_validator(mode='after') on ForecastingConfig validates split_bounds match split_mode"
    - "Nested Pydantic models (ClassificationFilePatterns, ArffFilePattern) provide deep immutability"
    - "frozen=True prevents field reassignment on all config instances"
    - "model_copy(update={...}) produces a new frozen instance with overridden fields"
  artifacts:
    - path: "src/tscollection/datasets/enums/data.py"
      provides: "DatasetFamily and SplitMode StrEnum definitions"
      contains: "class DatasetFamily", "class SplitMode"
    - path: "src/tscollection/datasets/config/base.py"
      provides: "DatasetConfig ABC, ClassificationConfig, ForecastingConfig, nested models"
      exports: ["ArffFilePattern", "ClassificationFilePatterns", "DatasetConfig", "ClassificationConfig", "ForecastingConfig"]
  key_links:
    - from: "src/tscollection/datasets/config/base.py"
      to: "src/tscollection/datasets/enums/data.py"
      via: "import DatasetFamily, SplitMode"
      pattern: "from tscollection.datasets.enums.data import"
---

<objective>
Establish enum foundations and Pydantic config class hierarchy for typed dataset metadata.
Purpose: Provide frozen, validated base classes that family-specific configs (Plan 02) inherit from. This is the structural foundation — without it, no config instances can be created.
Output: DatasetFamily/SplitMode enums, nested Pydantic models for deep immutability, three-level config hierarchy (DatasetConfig -> ClassificationConfig/ForecastingConfig -> family leaves).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-RESEARCH.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-VALIDATION.md
</execution_context>

<interfaces>
<!-- Existing enum pattern (StrEnum) from enums/data.py -->
from enum import StrEnum

class TimeSeriesDatasetMode(StrEnum):
    WITH_LABELS = 'with_labels'
    WITHOUT_LABELS = 'without_labels'
    FORECASTING = 'forecasting'

class SplittingStrategy(StrEnum):
    AS_DEFINED = 'as_defined'
    MANUAL = 'manual'

class ScalingMethod(StrEnum):
    NONE = 'none'
    MINMAX = 'minmax'
    STANDARD = 'standard'

class ForecastingMode(StrEnum):
    UNIVARIATE = 'univariate'
    MULTIVARIATE = 'multivariate'

<!-- Current enums/__init__.py exports -->
__all__ = [
    'DistanceMetric',
    'ForecastingMode',
    'ScalingMethod',
    'SplittingStrategy',
    'TimeSeriesDatasetMode',
]

<!-- Current config/__init__.py stub -->
"""Pydantic configuration models for dataset metadata."""
__all__ = []  # Populated in Phase 3
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DatasetFamily and SplitMode enums</name>
  <files>
    src/tscollection/datasets/enums/data.py
    src/tscollection/datasets/enums/__init__.py
    tests/test_config_enums.py
  </files>
  <read_first>
    - src/tscollection/datasets/enums/data.py — existing StrEnum patterns to follow
    - src/tscollection/datasets/enums/__init__.py — current exports to extend
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-03 decision for SplitMode, Deferred Ideas exclusions
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — DatasetFamily scope (all 8 families), Open Question 1
  </read_first>
  <behavior>
    - Test: DatasetFamily has 8 members: UCR='ucr', UEA='uea', ETT='ett', ELECTRICITY='electricity', WEATHER='weather', EXCHANGE='exchange', TRAFFIC='traffic', ILLNESS='illness'
    - Test: SplitMode has 2 members: INDEXED='indexed', FRACTIONAL='fractional'
    - Test: All members serialize to string correctly (str(member) == member.value)
    - Test: All members are StrEnum instances (isinstance check)
    - Test: DatasetFamily and SplitMode are importable from tscollection.datasets.enums
  </behavior>
  <action>
  Add two new StrEnum classes to enums/data.py following the existing pattern (class inherits StrEnum, uppercase member names with single-quoted string values).

  DatasetFamily (per RESEARCH.md Open Question 1, include all 8 rbspaper families to prevent validation errors):
  - UCR = 'ucr'
  - UEA = 'uea'
  - ETT = 'ett'
  - ELECTRICITY = 'electricity'
  - WEATHER = 'weather'
  - EXCHANGE = 'exchange'
  - TRAFFIC = 'traffic'
  - ILLNESS = 'illness'

  SplitMode (per D-03, dual-mode forecasting splits):
  - INDEXED = 'indexed'
  - FRACTIONAL = 'fractional'

  Update enums/__init__.py: Add DatasetFamily and SplitMode to import statement and __all__ list (alphabetical order).
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_enums.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: DatasetFamily enum has exactly 8 members matching rbspaper registry family strings
    - behavior: SplitMode enum has exactly 2 members (INDEXED, FRACTIONAL)
    - test-command: `uv run pytest tests/test_config_enums.py -x --tb=short -v` passes with 5+ tests
    - source: `grep -c 'class DatasetFamily' src/tscollection/datasets/enums/data.py` == 1
    - source: `grep -c 'class SplitMode' src/tscollection/datasets/enums/data.py` == 1
    - source: `python -c "from tscollection.datasets.enums import DatasetFamily, SplitMode; assert DatasetFamily.UCR.value == 'ucr'"` exits 0
  </acceptance_criteria>
  <done>DatasetFamily and SplitMode enums are defined, exported, and tested. Importable from tscollection.datasets.enums.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create config/base.py with hierarchy and nested models</name>
  <files>
    src/tscollection/datasets/config/base.py
    tests/test_config_base.py
  </files>
  <read_first>
    - src/tscollection/datasets/config/__init__.py — current stub to understand module setup
    - src/tscollection/datasets/enums/data.py — newly added enums to import
    - _sources/rbspaper/src/rbspaper/data/registry.py — DatasetMetadata dataclass fields (name, family, tasks) as reference for base config expansion
    - _sources/rbspaper/src/rbspaper/data/modules/abstract.py — fields consumed by modules (target_column_name, data_form) to inform config design
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-01 (layered inheritance), D-04 (validators), D-05 (frozen), D-06 (complete scope)
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — Architecture Patterns section, nested model examples, anti-patterns, Pitfall 5 (validator inheritance)
  </read_first>
  <behavior>
    - Test: ArffFilePattern(arff='test.arff') creates frozen instance; reassignment raises ValueError
    - Test: ClassificationFilePatterns(train=..., test=...) creates nested frozen instance
    - Test: DatasetConfig cannot be instantiated directly (abstract via ABC)
    - Test: Concrete subclass instance has frozen=True (field reassignment raises ValueError)
    - Test: model_copy(update={'name': 'New'}) returns new instance with updated field
    - Test: sha256 field rejects non-64-char hex strings via @field_validator
    - Test: sha256 field accepts None
    - Test: HttpUrl rejects invalid URLs (no scheme, 'not-a-url')
    - Test: ClassificationConfig raises ValidationError if instantiated without data_form
    - Test: ForecastingConfig with SplitMode.INDEXED rejects fractional split_bounds
    - Test: ForecastingConfig with SplitMode.FRACTIONAL accepts tuple of floats summing to 1.0
    - Test: cache_key property returns string derived from url and sha256
  </behavior>
  <action>
  Create config/base.py as a NEW file with the following class hierarchy (per D-01, D-04, D-05, D-06):

  **Nested models** (for deep immutability, per RESEARCH.md Pattern "Nested Model for File Patterns"):
  - ArffFilePattern(BaseModel): model_config=ConfigDict(frozen=True); field: arff: str
  - ClassificationFilePatterns(BaseModel): model_config=ConfigDict(frozen=True); fields: train: ArffFilePattern, test: ArffFilePattern

  **DatasetConfig** (abstract base, per D-01):
  - Inherits from BaseModel and ABC (abc.ABC)
  - model_config = ConfigDict(frozen=True) — per D-05, inherited by all subclasses
  - Fields (per D-06 complete scope):
    - name: str
    - family: DatasetFamily
    - url: HttpUrl (from pydantic)
    - sha256: str | None = None
    - num_classes: int = Field(default=0, ge=0)
    - data_form: str | None = None
    - tasks: tuple[str, ...]
  - @field_validator("sha256") — validates 64-char hex or None
  - @property cache_key — returns f"{str(self.url)}:{self.sha256 or 'unknown'}" (computed property, per Claude's Discretion)

  **ClassificationConfig**(DatasetConfig, per D-01):
  - Fields (per module consumption from abstract.py and ucr_datamodule.py):
    - target_col_name: str — consumed by BaseClassificationTimeSeriesDataModule at line 255
    - file_patterns: ClassificationFilePatterns — consumed by UCR/UEA modules at prepare_data() via dataset_config['main_config']['file_name_patterns']
    - split_strategy: SplittingStrategy = SplittingStrategy.AS_DEFINED
  - @model validator(mode="after") — asserts data_form is not None (per Pitfall 5: put cross-field validators on the class that owns all fields)

  **ForecastingConfig**(DatasetConfig, per D-01):
  - Fields (per D-03 dual-mode splits):
    - split_mode: SplitMode
    - split_bounds: tuple[int, ...] | tuple[float, ...] — type depends on split_mode
    - default_seq_len: int = Field(ge=1, default=128)
    - default_horizon: int = Field(ge=1, default=96)
  - @field_validator("split_bounds", mode="after") — validates bounds length is 3 (train_end, valid_end, test_end)
  - @model validator(mode="after") — if split_mode is FRACTIONAL, asserts sum(split_bounds) == 1.0 (within tolerance of 0.01); if INDEXED, asserts all values are int-compatible

  All classes: Google-style docstrings with Args section, full type hints on all fields.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_base.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: DatasetConfig is abstract (cannot instantiate directly); subclasses are concrete
    - behavior: frozen=True inherited by all subclasses — field reassignment raises ValueError
    - behavior: model_copy(update={...}) returns new frozen instance with specified overrides
    - behavior: HttpUrl rejects invalid URLs; accepts valid http/https URLs
    - behavior: sha256 validator rejects non-64-char hex strings; accepts None and valid hashes
    - behavior: ClassificationConfig validator ensures data_form is not None
    - behavior: ForecastingConfig validator ensures fractional bounds sum to ~1.0, indexed bounds are integers
    - test-command: `uv run pytest tests/test_config_base.py -x --tb=short -v` passes with 11+ tests
    - source: `grep -c 'class DatasetConfig' src/tscollection/datasets/config/base.py` == 1
    - source: `grep -c 'class ClassificationConfig' src/tscollection/datasets/config/base.py` == 1
    - source: `grep -c 'class ForecastingConfig' src/tscollection/datasets/config/base.py` == 1
    - source: `grep -c 'ConfigDict(frozen=True)' src/tscollection/datasets/config/base.py` >= 1
  </acceptance_criteria>
  <done>base.py exists with ArffFilePattern, ClassificationFilePatterns, DatasetConfig (ABC), ClassificationConfig, and ForecastingConfig. All frozen, all validated. Test suite covers field validators, model validators, immutability, and model_copy.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/conftest.py with config fixtures</name>
  <files>
    tests/conftest.py
  </files>
  <read_first>
    - tests/conftest.py — existing fixtures to extend (do not overwrite)
    - src/tscollection/datasets/config/base.py — just-created classes to fixture-ize
    - .planning/phases/03-pydantic-registry/03-VALIDATION.md — Wave 0 requirement: "tests/conftest.py — add config-specific fixtures (sample config instances)"
  </read_first>
  <behavior>
    - Fixture sample_classification_config returns a valid ClassificationConfig instance
    - Fixture sample_forecasting_config returns a valid ForecastingConfig instance (indexed mode)
    - Fixture sample_fractional_config returns a valid ForecastingConfig instance (fractional mode)
  </behavior>
  <action>
  Append config-specific pytest fixtures to the EXISTING tests/conftest.py (do not overwrite existing fixtures). Create minimal valid config instances for reuse by later test files:

  - @pytest.fixture "sample_classification_config" — ClassificationConfig with name='TestDataset', family=DatasetFamily.UCR, url='https://example.com/test.zip', num_classes=2, data_form='regular', target_col_name='Class', file_patterns=ClassificationFilePatterns(train=ArffFilePattern(arff='{dataset_name}_train.arff'), test=ArffFilePattern(arff='{dataset_name}_test.arff')), tasks=('classification', 'representation')
  - @pytest.fixture "sample_forecasting_config" — ForecastingConfig with name='TestForecast', family=DatasetFamily.ETT, url='https://example.com/test.csv', split_mode=SplitMode.INDEXED, split_bounds=(8640, 11520, 14400), default_seq_len=128, default_horizon=96, tasks=('forecasting', 'representation')
  - @pytest.fixture "sample_fractional_config" — ForecastingConfig with name='TestFractional', family=DatasetFamily.ELECTRICITY, url='https://example.com/test.csv', split_mode=SplitMode.FRACTIONAL, split_bounds=(0.6, 0.2, 0.2), default_seq_len=128, default_horizon=24, tasks=('forecasting', 'representation')
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py --collect-only -q 2>&1 | grep -c 'sample_.*config'</automated>
  </verify>
  <acceptance_criteria>
    - behavior: conftest.py loads without errors (no import cycles or syntax issues)
    - test-command: `uv run pytest tests/ --collect-only -q` succeeds without errors
    - source: `grep -c 'sample_classification_config\|sample_forecasting_config\|sample_fractional_config' tests/conftest.py` == 3
  </acceptance_criteria>
  <done>Three config fixtures added to conftest.py. Existing fixtures preserved. All fixtures yield valid, frozen config instances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config construction | HttpUrl validates external URLs at model creation (static code, no runtime user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01 | Injection | DatasetConfig.url (HttpUrl) | Mitigate | HttpUrl enforces valid http/https scheme + authority; rejects malformed URLs |
| T-03-02 | Tampering | All config instances | Mitigate | frozen=True on DatasetConfig prevents runtime field reassignment |
| T-03-03 | Repudiation | sha256 field | Mitigate | sha256 field enables Phase 4 integrity verification; validator enforces 64-char hex format |
| T-03-04 | Information Disclosure | Config data | Accept | No secrets/credentials stored; all values are public dataset metadata |
</threat_model>

<verification>
- All 8 DatasetFamily members present and serializable
- SplitMode has INDEXED and FRACTIONAL
- DatasetConfig cannot be instantiated directly (ABC)
- Concrete subclass is frozen (ValueError on reassignment)
- model_copy produces independent frozen instance
- Nested models (ClassificationFilePatterns) are deeply frozen
- sha256 field rejects invalid formats
- ForecastingConfig validates split_bounds consistency with split_mode
- conftest.py fixtures are discoverable by pytest
</verification>

<success_criteria>
- DatasetFamily enum has 8 members (all rbspaper families)
- SplitMode enum has 2 members (INDEXED, FRACTIONAL)
- base.py contains ArffFilePattern, ClassificationFilePatterns, DatasetConfig (ABC), ClassificationConfig, ForecastingConfig
- All models are frozen (frozen=True inherited)
- Field validators and model validators function correctly
- tests/test_config_enums.py passes (5+ tests)
- tests/test_config_base.py passes (11+ tests)
- conftest.py has 3 config fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/03-pydantic-registry/03-01-SUMMARY.md`
</output>
