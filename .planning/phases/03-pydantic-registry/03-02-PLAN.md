---
phase: 03-pydantic-registry
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/tscollection/datasets/config/ucr.py
  - src/tscollection/datasets/config/uea.py
  - tests/test_config_ucr.py
  - tests/test_config_uea.py
autonomous: true
requirements: [CFG-02]
user_setup: []
must_haves:
  truths:
    - "UCRConfig instances have data_form='regular' (computed property)"
    - "UEAConfig instances have data_form='nested' (computed property)"
    - "All config instances are frozen and have valid HttpUrl"
    - "UCR configs have correct num_classes per rbspaper registry"
    - "file_patterns use nested Pydantic models (deeply frozen)"
  artifacts:
    - path: "src/tscollection/datasets/config/ucr.py"
      provides: "UCRConfig class + 3 dataset instances"
      exports: ["UCRConfig", "UCR_COFFEE", "UCR_ECG200", "UCR_FACE_FOUR"]
    - path: "src/tscollection/datasets/config/uea.py"
      provides: "UEAConfig class + 2 dataset instances"
      exports: ["UEAConfig", "UEA_BASIC_MOTIONS", "UEA_ATRIAL_FIBRILLATION"]
  key_links:
    - from: "src/tscollection/datasets/config/ucr.py"
      to: "src/tscollection/datasets/config/base.py"
      via: "UCRConfig(ClassificationConfig)"
      pattern: "class UCRConfig\\(ClassificationConfig\\)"
    - from: "src/tscollection/datasets/config/uea.py"
      to: "src/tscollection/datasets/config/base.py"
      via: "UEAConfig(ClassificationConfig)"
      pattern: "class UEAConfig\\(ClassificationConfig\\)"
---

<objective>
Create classification family configs (UCR + UEA) with frozen dataset instances.
Purpose: Implement D-01 layered inheritance for classification datasets — UCRConfig and UEAConfig extend ClassificationConfig with family-specific data_form. Each instance encodes intrinsic facts (rbspaper registry expansion per CFG-01/CFG-02).
Output: 5 classification config instances across 2 families, each with correct metadata from rbspaper registry.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-RESEARCH.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-01-PLAN.md
</execution_context>

<interfaces>
<!-- ClassificationConfig from Plan 01 (base.py) -->
class ClassificationConfig(DatasetConfig):
    target_col_name: str
    file_patterns: ClassificationFilePatterns
    split_strategy: SplittingStrategy = SplittingStrategy.AS_DEFINED

<!-- ClassificationFilePatterns from Plan 01 -->
class ClassificationFilePatterns(BaseModel):
    model_config = ConfigDict(frozen=True)
    train: ArffFilePattern
    test: ArffFilePattern

class ArffFilePattern(BaseModel):
    model_config = ConfigDict(frozen=True)
    arff: str  # e.g., "{dataset_name}_train.arff"

<!-- rbspaper registry entries for classification -->
DatasetMetadata(name='Coffee', family='ucr', tasks=('classification', 'representation'))
DatasetMetadata(name='ECG200', family='ucr', tasks=('classification', 'representation'))
DatasetMetadata(name='FaceFour', family='ucr', tasks=('classification', 'representation'))
DatasetMetadata(name='BasicMotions', family='uea', tasks=('classification', 'representation'))
DatasetMetadata(name='AtrialFibrillation', family='uea', tasks=('classification', 'representation'))

<!-- UCR module consumes target_col_name and file_name_patterns -->
config = self.dataset_config['main_config']['file_name_patterns']
arff_train = config['train']['arff'].replace('{dataset_name}', self._dataset_name)
self.target_column_name = self.dataset_config['main_config']['target_col_name']
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UCR classification config with 3 instances</name>
  <files>
    src/tscollection/datasets/config/ucr.py
    tests/test_config_ucr.py
  </files>
  <read_first>
    - src/tscollection/datasets/config/base.py — ClassificationConfig, ArffFilePattern, ClassificationFilePatterns to inherit from
    - src/tscollection/datasets/enums/data.py — DatasetFamily.UCR enum value
    - _sources/rbspaper/src/rbspaper/data/registry.py — Coffee, ECG200, FaceFour metadata
    - _sources/rbspaper/src/rbspaper/data/modules/ucr_datamodule.py — data_form='regular' at line 73, target_column_name usage
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-01, D-02, D-05, D-06 decisions; Specific Ideas for UCRConfig
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — UCRConfig code example, file_name_patterns format, anti-patterns
  </read_first>
  <behavior>
    - Test: UCR_COFFEE is frozen (field reassignment raises ValueError)
    - Test: UCR_COFFEE.name == 'Coffee'
    - Test: UCR_COFFEE.family == DatasetFamily.UCR
    - Test: UCR_COFFEE.num_classes == 3
    - Test: UCR_COFFEE.target_col_name == 'Class'
    - Test: UCR_COFFEE.data_form == 'regular' (property)
    - Test: UCR_COFFEE.file_patterns.train.arff contains '{dataset_name}' placeholder
    - Test: UCR_COFFEE.model_copy(update={'name': 'Coffee2'}) returns new instance
    - Test: UCR_ECG200.num_classes == 5
    - Test: UCR_FACE_FOUR.num_classes == 4
    - Test: All instances have tasks=('classification', 'representation')
  </behavior>
  <action>
  Create config/ucr.py as a NEW file. Define UCRConfig(ClassificationConfig) — per D-01 and RESEARCH.md code example:

  Class: UCRConfig (inherits ClassificationConfig)
  - model_config = ConfigDict(frozen=True) — inherited from DatasetConfig
  - family: DatasetFamily = DatasetFamily.UCR — class-level default, per RESEARCH.md example
  - @property data_form -> returns 'regular' — computed property, per D-06 and ucr_datamodule.py line 73
  - Google-style docstring with Args section documenting all fields

  Instances (per D-02, module-level constants):

  UCR_COFFEE:
  - name='Coffee', family=DatasetFamily.UCR
  - url='https://timeseriesclassification.com/Downloads/Coffee.zip' — standard UCR archive URL
  - sha256=None — per RESEARCH.md Open Question 4 (defered to Phase 4)
  - num_classes=3 — Coffee has 3 classes (Plane, Hand, Kick, but 3 distinct in UCR)
  - target_col_name='Class' — standard UCR ARFF column name
  - file_patterns=ClassificationFilePatterns(
      train=ArffFilePattern(arff='{dataset_name}_train.arff'),
      test=ArffFilePattern(arff='{dataset_name}_test.arff'))
  - tasks=('classification', 'representation')

  UCR_ECG200:
  - name='ECG200', family=DatasetFamily.UCR
  - url='https://timeseriesclassification.com/Downloads/ECG200.zip'
  - sha256=None
  - num_classes=5 — ECG200 has 5 classes
  - target_col_name='Class'
  - file_patterns with same pattern template
  - tasks=('classification', 'representation')

  UCR_FACE_FOUR:
  - name='FaceFour', family=DatasetFamily.UCR
  - url='https://timeseriesclassification.com/Downloads/FaceFour.zip'
  - sha256=None
  - num_classes=4 — FaceFour has 4 classes
  - target_col_name='Class'
  - file_patterns with same pattern template
  - tasks=('classification', 'representation')

  Tests: Verify frozen behavior, correct field values, data_form property, model_copy, file_patterns nested structure. URL values must be verified by executor against actual server — note any discrepancies.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_ucr.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: UCRConfig inherits ClassificationConfig; data_form property returns 'regular'
    - behavior: UCR_COFFEE.num_classes == 3, UCR_ECG200.num_classes == 5, UCR_FACE_FOUR.num_classes == 4
    - behavior: All instances frozen; model_copy works
    - behavior: file_patterns uses nested Pydantic models (ClassificationFilePatterns, not dict)
    - test-command: `uv run pytest tests/test_config_ucr.py -x --tb=short -v` passes with 10+ tests
    - source: `grep -c 'UCR_COFFEE\|UCR_ECG200\|UCR_FACE_FOUR' src/tscollection/datasets/config/ucr.py` >= 6
    - source: `python -c "from tscollection.datasets.config.ucr import UCR_COFFEE; assert UCR_COFFEE.data_form == 'regular'"` exits 0
  </acceptance_criteria>
  <done>ucr.py exists with UCRConfig class and 3 frozen instances (Coffee, ECG200, FaceFour). Tests verify field values, immutability, and data_form property.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UEA classification config with 2 instances</name>
  <files>
    src/tscollection/datasets/config/uea.py
    tests/test_config_uea.py
  </files>
  <read_first>
    - src/tscollection/datasets/config/base.py — ClassificationConfig, ArffFilePattern, ClassificationFilePatterns
    - src/tscollection/datasets/config/ucr.py — just-created UCRConfig as structural reference
    - _sources/rbspaper/src/rbspaper/data/registry.py — BasicMotions, AtrialFibrillation metadata
    - _sources/rbspaper/src/rbspaper/data/modules/uea_datamodule.py — data_form='nested' at line 79
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-01, D-02, D-06 decisions
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — anti-patterns (mutable defaults)
  </read_first>
  <behavior>
    - Test: UEA_BASIC_MOTIONS.data_form == 'nested' (property)
    - Test: UEA_BASIC_MOTIONS.family == DatasetFamily.UEA
    - Test: UEA_BASIC_MOTIONS.num_classes == 4
    - Test: UEA_ATRIAL_FIBRILLATION.num_classes == 2
    - Test: Both instances are frozen
    - Test: Both instances have valid file_patterns (nested Pydantic models)
  </behavior>
  <action>
  Create config/uea.py as a NEW file. Define UEAConfig(ClassificationConfig) — per D-01, structurally identical to UCRConfig except data_form:

  Class: UEAConfig (inherits ClassificationConfig)
  - family: DatasetFamily = DatasetFamily.UEA — class-level default
  - @property data_form -> returns 'nested' — per D-06 and uea_datamodule.py line 79 (multivariate nested ARFF)
  - Google-style docstring with Args section

  Instances:

  UEA_BASIC_MOTIONS:
  - name='BasicMotions', family=DatasetFamily.UEA
  - url='https://timeseriesclassification.com/Downloads/BasicMotions.zip'
  - sha256=None — per RESEARCH.md Open Question 4
  - num_classes=4 — Walk, Jog, Flightstairs, Levelstairs
  - target_col_name='Class'
  - file_patterns=ClassificationFilePatterns(
      train=ArffFilePattern(arff='{dataset_name}_train.arff'),
      test=ArffFilePattern(arff='{dataset_name}_test.arff'))
  - tasks=('classification', 'representation')

  UEA_ATRIAL_FIBRILLATION:
  - name='AtrialFibrillation', family=DatasetFamily.UEA
  - url='https://timeseriesclassification.com/Downloads/AtrialFibrillation.zip'
  - sha256=None
  - num_classes=2 — 50 normal + 50 atrial fibrillation
  - target_col_name='Class'
  - file_patterns with same pattern template
  - tasks=('classification', 'representation')

  Tests: Verify frozen behavior, data_form='nested', correct num_classes, model_copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_uea.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: UEAConfig inherits ClassificationConfig; data_form property returns 'nested'
    - behavior: UEA_BASIC_MOTIONS.num_classes == 4, UEA_ATRIAL_FIBRILLATION.num_classes == 2
    - behavior: All instances frozen; model_copy works
    - test-command: `uv run pytest tests/test_config_uea.py -x --tb=short -v` passes with 6+ tests
    - source: `python -c "from tscollection.datasets.config.uea import UEA_BASIC_MOTIONS; assert UEA_BASIC_MOTIONS.data_form == 'nested'"` exits 0
  </acceptance_criteria>
  <done>uea.py exists with UEAConfig class and 2 frozen instances (BasicMotions, AtrialFibrillation). Tests verify field values, nested data_form, and immutability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config construction | HttpUrl validates dataset archive URLs at model creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-05 | Injection | UCR/UEA url fields (HttpUrl) | Mitigate | HttpUrl enforces valid URL; archive URLs from trusted sources (timeseriesclassification.com) |
| T-03-06 | Tampering | UCR/UEA config instances | Mitigate | frozen=True prevents runtime mutation of num_classes, file_patterns |
</threat_model>

<verification>
- UCR_COFFEE, UCR_ECG200, UCR_FACE_FOUR are frozen instances of UCRConfig
- UEA_BASIC_MOTIONS, UEA_ATRIAL_FIBRILLATION are frozen instances of UEAConfig
- UCR data_form property returns 'regular', UEA returns 'nested'
- All num_classes values match actual dataset characteristics
- file_patterns use nested Pydantic models (deeply frozen, not dicts)
- model_copy(update={...}) produces new instances correctly
</verification>

<success_criteria>
- config/ucr.py has UCRConfig + 3 instances (Coffee, ECG200, FaceFour)
- config/uea.py has UEAConfig + 2 instances (BasicMotions, AtrialFibrillation)
- All instances are frozen with correct field values
- UCR data_form='regular', UEA data_form='nested'
- tests/test_config_ucr.py passes (10+ tests)
- tests/test_config_uea.py passes (6+ tests)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pydantic-registry/03-02-SUMMARY.md`
</output>
