---
phase: 03-pydantic-registry
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/tscollection/datasets/config/ett.py
  - src/tscollection/datasets/config/electricity.py
  - src/tscollection/datasets/config/weather.py
  - tests/test_config_ett.py
  - tests/test_config_electricity.py
  - tests/test_config_weather.py
autonomous: true
requirements: [CFG-02]
user_setup: []
must_haves:
  truths:
    - "ETTConfig instances use SplitMode.INDEXED with correct split_bounds per rbspaper code"
    - "ElectricityConfig uses SplitMode.FRACTIONAL with (0.6, 0.2, 0.2) split_fractions"
    - "WeatherConfig uses SplitMode.FRACTIONAL with (0.6, 0.2, 0.2) split_fractions"
    - "All forecasting configs have default_seq_len=128 and family-specific default_horizon"
    - "ETT hourly datasets: split_bounds=(8640, 11520, 14400)"
    - "ETT 15-min datasets: split_bounds=(34560, 46080, 57600)"
  artifacts:
    - path: "src/tscollection/datasets/config/ett.py"
      provides: "ETTConfig class + 4 instances (ETTh1, ETTh2, ETTm1, ETTm2)"
      exports: ["ETTConfig", "ETT_H1", "ETT_H2", "ETT_M1", "ETT_M2"]
    - path: "src/tscollection/datasets/config/electricity.py"
      provides: "ElectricityConfig class + 1 instance"
      exports: ["ElectricityConfig", "ELECTRICITY_LOAD"]
    - path: "src/tscollection/datasets/config/weather.py"
      provides: "WeatherConfig class + 1 instance"
      exports: ["WeatherConfig", "WEATHER"]
  key_links:
    - from: "src/tscollection/datasets/config/ett.py"
      to: "src/tscollection/datasets/config/base.py"
      via: "ETTConfig(ForecastingConfig)"
      pattern: "class ETTConfig\\(ForecastingConfig\\)"
    - from: "src/tscollection/datasets/config/ett.py"
      to: "_sources/rbspaper/.../ett_datamodule.py"
      via: "split_bounds derived from 12*30*24 indices"
      pattern: "12 \\* 30 \\* 24"
---

<objective>
Create forecasting family configs (ETT + Electricity + Weather) with frozen dataset instances.
Purpose: Implement D-01 layered inheritance for forecasting datasets. ETT uses indexed split boundaries (D-03), Electricity and Weather use fractional splits (D-03). Each instance encodes time-intrinsic facts from rbspaper datamodules.
Output: 6 forecasting config instances across 3 families, with correct split boundaries and CSV parsing parameters.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-RESEARCH.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/03-pydantic-registry/03-01-PLAN.md
</execution_context>

<interfaces>
<!-- ForecastingConfig from Plan 01 (base.py) -->
class ForecastingConfig(DatasetConfig):
    split_mode: SplitMode
    split_bounds: tuple[int, ...] | tuple[float, ...]
    default_seq_len: int = Field(ge=1, default=128)
    default_horizon: int = Field(ge=1, default=96)

<!-- ETT module split indices (verified from rbspaper source) -->
# ETTh1, ETTh2 (hourly): 12*30*24=8640, 16*30*24=11520, 20*30*24=14400
self._train_slice = slice(None, 12 * 30 * 24)
self._valid_slice = slice(12 * 30 * 24, 16 * 30 * 24)
self._test_slice = slice(16 * 30 * 24, 20 * 30 * 24)

# ETTm1, ETTm2 (15-min): 12*30*24*4=34560, 16*30*24*4=46080, 20*30*24*4=57600
self._train_slice = slice(None, 12 * 30 * 24 * 4)
self._valid_slice = slice(12 * 30 * 24 * 4, 16 * 30 * 24 * 4)
self._test_slice = slice(16 * 30 * 24 * 4, 20 * 30 * 24 * 4)

<!-- Electricity module fractional split -->
self._train_slice = slice(None, int(0.6 * num_samples))
self._valid_slice = slice(int(0.6 * num_samples), int(0.8 * num_samples))
self._test_slice = slice(int(0.8 * num_samples), None)

<!-- Weather module fractional split (same pattern) -->
self._train_slice = slice(None, int(0.6 * num_samples))
self._valid_slice = slice(int(0.6 * num_samples), int(0.8 * num_samples))

<!-- ETT module univariate column selection -->
if self._mode == ForecastingTimeSeriesDatasetMode.UNIVARIATE:
    df = df[['OT']]

<!-- Electricity CSV parsing -->
df = pd.read_csv(self.dataset_file_path, parse_dates=True, sep=';', decimal=',', index_col=[0])
df = df[['MT_001']]  # univariate

<!-- Weather CSV parsing -->
df = pd.read_csv(self.dataset_file_path, parse_dates=True, index_col='date')
df = df.iloc[:, -1:]  # univariate (last column)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ETT forecasting config with 4 instances</name>
  <files>
    src/tscollection/datasets/config/ett.py
    tests/test_config_ett.py
  </files>
  <read_first>
    - src/tscollection/datasets/config/base.py — ForecastingConfig to inherit from
    - _sources/rbspaper/src/rbspaper/data/modules/ett_datamodule.py — split indices at lines 71-78, univariate column 'OT' at line 93
    - _sources/rbspaper/src/rbspaper/data/registry.py — ETTh1 metadata entry
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-03 (INDEXED splits), D-06 (complete scope: split_bounds, forecast_column)
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — ETTConfig code example, split_bounds values (verified from source), Open Question 2
  </read_first>
  <behavior>
    - Test: ETT_H1.split_mode == SplitMode.INDEXED (property)
    - Test: ETT_H1.split_bounds == (8640, 11520, 14400)
    - Test: ETT_H2.split_bounds == (8640, 11520, 14400)
    - Test: ETT_M1.split_bounds == (34560, 46080, 57600)
    - Test: ETT_M2.split_bounds == (34560, 46080, 57600)
    - Test: ETT_H1.forecast_column == 'OT'
    - Test: ETT_H1.frequency == '1h'
    - Test: ETT_M1.frequency == '15min'
    - Test: ETT_H1.num_features == 7
    - Test: All instances are frozen
    - Test: ETT_H1.model_copy(update={'default_seq_len': 64}) produces new instance
  </behavior>
  <action>
  Create config/ett.py as a NEW file. Define ETTConfig(ForecastingConfig) — per D-01 and RESEARCH.md:

  Class: ETTConfig (inherits ForecastingConfig)
  - family: DatasetFamily = DatasetFamily.ETT — class-level default
  - split_mode: SplitMode = SplitMode.INDEXED — class-level default
  - Additional fields (per D-06, module consumption):
    - forecast_column: str — univariate target column name
    - frequency: str — time step frequency ('1h' or '15min')
    - num_features: int = Field(ge=1) — total CSV columns excluding date
  - @property split_mode returns SplitMode.INDEXED — read-only, per D-03
  - Google-style docstring with Args section

  Instances (per RESEARCH.md Open Question 2, create all 4 ETT variants):

  ETT_H1:
  - name='ETTh1', family=DatasetFamily.ETT
  - url='https://raw.githubusercontent.com/zhouhao118/ETD/main/ETTh1.csv' — standard ETT data source
  - sha256=None — per RESEARCH.md Open Question 4
  - split_mode=SplitMode.INDEXED
  - split_bounds=(8640, 11520, 14400) — VERIFIED: 12*30*24, 16*30*24, 20*30*24 from ett_datamodule.py lines 72-74
  - forecast_column='OT' — from ett_datamodule.py line 93: df[['OT']]
  - frequency='1h'
  - num_features=7
  - default_seq_len=128
  - default_horizon=24 — 1 day forecast for hourly data
  - tasks=('forecasting', 'representation')

  ETT_H2:
  - name='ETTh2', family=DatasetFamily.ETT
  - url='https://raw.githubusercontent.com/zhouhao118/ETD/main/ETTh2.csv'
  - sha256=None
  - split_bounds=(8640, 11520, 14400) — same as H1 (hourly)
  - forecast_column='OT'
  - frequency='1h'
  - num_features=7
  - default_seq_len=128
  - default_horizon=24
  - tasks=('forecasting', 'representation')

  ETT_M1:
  - name='ETTm1', family=DatasetFamily.ETT
  - url='https://raw.githubusercontent.com/zhouhao118/ETD/main/ETTm1.csv'
  - sha256=None
  - split_bounds=(34560, 46080, 57600) — VERIFIED: 12*30*24*4, 16*30*24*4, 20*30*24*4 from ett_datamodule.py lines 76-78
  - forecast_column='OT'
  - frequency='15min'
  - num_features=7
  - default_seq_len=128
  - default_horizon=96 — 24 hours at 15-min intervals
  - tasks=('forecasting', 'representation')

  ETT_M2:
  - name='ETTm2', family=DatasetFamily.ETT
  - url='https://raw.githubusercontent.com/zhouhao118/ETD/main/ETTm2.csv'
  - sha256=None
  - split_bounds=(34560, 46080, 57600) — same as M1 (15-min)
  - forecast_column='OT'
  - frequency='15min'
  - num_features=7
  - default_seq_len=128
  - default_horizon=96
  - tasks=('forecasting', 'representation')

  Tests: Verify split_bounds match rbspaper source values, frozen behavior, frequency differences, model_copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_ett.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: ETTConfig inherits ForecastingConfig; split_mode property returns INDEXED
    - behavior: ETT_H1.split_bounds == (8640, 11520, 14400), ETT_M1.split_bounds == (34560, 46080, 57600)
    - behavior: ETT_H1.frequency == '1h', ETT_M1.frequency == '15min'
    - behavior: All 4 instances frozen; model_copy works
    - behavior: ETT_H1.num_features == 7
    - test-command: `uv run pytest tests/test_config_ett.py -x --tb=short -v` passes with 10+ tests
    - source: `grep -c 'ETT_H1\|ETT_H2\|ETT_M1\|ETT_M2' src/tscollection/datasets/config/ett.py` >= 8
  </acceptance_criteria>
  <done>ett.py exists with ETTConfig class and 4 frozen instances (ETTh1, ETTh2, ETTm1, ETTm2). Split bounds verified against rbspaper source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Electricity and Weather forecasting configs</name>
  <files>
    src/tscollection/datasets/config/electricity.py
    src/tscollection/datasets/config/weather.py
    tests/test_config_electricity.py
    tests/test_config_weather.py
  </files>
  <read_first>
    - src/tscollection/datasets/config/base.py — ForecastingConfig to inherit from
    - _sources/rbspaper/src/rbspaper/data/modules/electricity_load_datamodule.py — CSV parsing (sep=';', decimal=','), univariate column 'MT_001', 60/20/20 split
    - _sources/rbspaper/src/rbspaper/data/modules/weather_datamodule.py — CSV parsing (standard), univariate 'last' column (iloc[:, -1:]), 60/20/20 split
    - _sources/rbspaper/src/rbspaper/data/registry.py — electricity, weather metadata entries
    - .planning/phases/03-pydantic-registry/03-CONTEXT.md — D-03 (FRACTIONAL splits), D-06 (complete scope)
    - .planning/phases/03-pydantic-registry/03-RESEARCH.md — ElectricityConfig and WeatherConfig code examples, Open Question 6 (csv_kwargs as explicit fields)
  </read_first>
  <behavior>
    - Test: ELECTRICITY_LOAD.split_mode == SplitMode.FRACTIONAL (property)
    - Test: ELECTRICITY_LOAD.split_bounds == (0.6, 0.2, 0.2)
    - Test: sum(ELECTRICITY_LOAD.split_bounds) == 1.0
    - Test: ELECTRICITY_LOAD.csv_sep == ';'
    - Test: ELECTRICITY_LOAD.csv_decimal == ','
    - Test: ELECTRICITY_LOAD.forecast_column == 'MT_001'
    - Test: WEATHER.split_mode == SplitMode.FRACTIONAL
    - Test: WEATHER.split_bounds == (0.6, 0.2, 0.2)
    - Test: WEATHER.univariate_column == 'last'
    - Test: Both instances frozen
  </behavior>
  <action>
  Create config/electricity.py and config/weather.py as NEW files. Both inherit ForecastingConfig — per D-01.

  **ElectricityConfig** (config/electricity.py):
  - family: DatasetFamily = DatasetFamily.ELECTRICITY
  - split_mode: SplitMode = SplitMode.FRACTIONAL — class-level default
  - Additional fields (per D-06 and electricity_load_datamodule.py):
    - forecast_column: str = 'MT_001' — from line 97: df[['MT_001']]
    - csv_sep: str = ';' — from line 88: sep=';'
    - csv_decimal: str = ',' — from line 88: decimal=','
  - @property split_mode returns SplitMode.FRACTIONAL — read-only, per D-03
  - Google-style docstring with Args section

  ELECTRICITY_LOAD:
  - name='electricity', family=DatasetFamily.ELECTRICITY
  - url='https://raw.githubusercontent.com/rashmitrivedi/NeuralForecast/master/dataset/electricity/electricity.csv' — standard Monash TF source
  - sha256=None
  - split_mode=SplitMode.FRACTIONAL
  - split_bounds=(0.6, 0.2, 0.2) — from lines 73-75: int(0.6*n), int(0.8*n)
  - forecast_column='MT_001'
  - csv_sep=';'
  - csv_decimal=','
  - default_seq_len=128
  - default_horizon=24 — 1 day for hourly data
  - tasks=('forecasting', 'representation')

  **WeatherConfig** (config/weather.py):
  - family: DatasetFamily = DatasetFamily.WEATHER
  - split_mode: SplitMode = SplitMode.FRACTIONAL — class-level default
  - Additional fields (per D-06 and weather_datamodule.py):
    - univariate_column: str = 'last' — from line 89: df.iloc[:, -1:]
  - @property split_mode returns SplitMode.FRACTIONAL — read-only, per D-03
  - Google-style docstring with Args section

  WEATHER:
  - name='weather', family=DatasetFamily.WEATHER
  - url='https://raw.githubusercontent.com/rashmitrivedi/NeuralForecast/master/dataset/weather/weather.csv' — standard Monash TF source
  - sha256=None
  - split_mode=SplitMode.FRACTIONAL
  - split_bounds=(0.6, 0.2, 0.2) — from lines 68-70: int(0.6*n), int(0.8*n)
  - univariate_column='last'
  - default_seq_len=128
  - default_horizon=24
  - tasks=('forecasting', 'representation')

  Tests: Verify fractional split_bounds sum to 1.0, CSV parsing parameters match rbspaper source, frozen behavior, model_copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_electricity.py tests/test_config_weather.py -x --tb=short -v</automated>
  </verify>
  <acceptance_criteria>
    - behavior: ElectricityConfig inherits ForecastingConfig; split_mode returns FRACTIONAL
    - behavior: WeatherConfig inherits ForecastingConfig; split_mode returns FRACTIONAL
    - behavior: ELECTRICITY_LOAD.split_bounds == (0.6, 0.2, 0.2)
    - behavior: ELECTRICITY_LOAD.csv_sep == ';', csv_decimal == ','
    - behavior: WEATHER.univariate_column == 'last'
    - behavior: Both instances frozen; model_copy works
    - test-command: `uv run pytest tests/test_config_electricity.py tests/test_config_weather.py -x --tb=short -v` passes with 8+ tests total
    - source: `grep -c 'ELECTRICITY_LOAD' src/tscollection/datasets/config/electricity.py` >= 1
    - source: `grep -c 'WEATHER' src/tscollection/datasets/config/weather.py` >= 1
  </acceptance_criteria>
  <done>electricity.py and weather.py exist with ElectricityConfig, WeatherConfig classes and 1 instance each. Split bounds are fractional (0.6, 0.2, 0.2). CSV parameters match rbspaper source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config construction | HttpUrl validates dataset CSV URLs at model creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-07 | Injection | ETT/Electricity/Weather url fields | Mitigate | HttpUrl enforces valid URL format; URLs from trusted GitHub/raw sources |
| T-03-08 | Tampering | split_bounds on all instances | Mitigate | frozen=True prevents mutation of split boundaries |
</threat_model>

<verification>
- ETT_H1 through ETT_M2: 4 frozen instances with correct split_bounds from rbspaper source
- ELECTRICITY_LOAD: frozen instance with fractional splits, csv_sep=';', csv_decimal=','
- WEATHER: frozen instance with fractional splits, univariate_column='last'
- All split_mode properties return correct values (INDEXED for ETT, FRACTIONAL for Electricity/Weather)
- Fractional split_bounds sum to 1.0
- Indexed split_bounds match exact rbspaper calculations
</verification>

<success_criteria>
- config/ett.py has ETTConfig + 4 instances (ETTh1/2 hourly, ETTm1/2 15-min)
- config/electricity.py has ElectricityConfig + ELECTRICITY_LOAD
- config/weather.py has WeatherConfig + WEATHER
- All instances frozen with correct split_bounds
- ETT default_horizon=24 (hourly) / 96 (15-min)
- Electricity default_horizon=24, Weather default_horizon=24
- All 3 test files pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-pydantic-registry/03-03-SUMMARY.md`
</output>
