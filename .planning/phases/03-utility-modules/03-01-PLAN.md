---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/datasets/enums/data.py
  - src/tscollection/datasets/utils/common.py
  - src/tscollection/datasets/utils/arff.py
  - src/tscollection/datasets/utils/features.py
autonomous: true
requirements: [UTI-01, UTI-03]
user_setup: []
must_haves:
  truths:
    - "DataForm enum has REGULAR, NESTED, MULTI_FILES members with StrEnum base"
    - "flatten_list_of_np_arrays concatenates list of arrays into single 1-D array"
    - "read_arff_as_df returns tuple of (DataFrame, metadata) from valid ARFF file"
    - "extract_time_features returns (N, 7) float32 array from DatetimeIndex"
  artifacts:
    - path: "src/tscollection/datasets/enums/data.py"
      provides: "DataForm enum (REGULAR, NESTED, MULTI_FILES)"
      contains: "class DataForm(StrEnum)"
    - path: "src/tscollection/datasets/utils/common.py"
      provides: "flatten_list_of_np_arrays function, top-level numpy import"
      exports: ["flatten_list_of_np_arrays"]
    - path: "src/tscollection/datasets/utils/arff.py"
      provides: "read_arff_as_df, process_df_according_to_dtypes"
      min_lines: 30
    - path: "src/tscollection/datasets/utils/features.py"
      provides: "extract_time_features"
      min_lines: 25
  key_links:
    - from: "common.py"
      to: "numpy"
      via: "top-level import numpy as np (NOT behind TYPE_CHECKING)"
      pattern: "^import numpy as np"
    - from: "arff.py"
      to: "scipy.io.arff"
      via: "lazy import inside read_arff_as_df"
      pattern: "from scipy\\.io import arff"
    - from: "features.py"
      to: "pandas, numpy"
      via: "top-level imports"
      pattern: "import (numpy|pandas)"
---

<objective>
Add DataForm enum, flatten_list_of_np_arrays to common.py, and create self-contained utility modules (arff.py, features.py).

Purpose: Foundation items (DataForm, flatten_list_of_np_arrays) unblock scaling.py in Wave 2. Arff.py and features.py are self-contained and can ship now.

Output: 4 files created/modified, all with CLAUDE.md style compliance (D-09 through D-11).
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-utility-modules/03-CONTEXT.md
@.planning/phases/03-utility-modules/03-RESEARCH.md
@.planning/phases/03-utility-modules/03-PATTERNS.md
@src/tscollection/datasets/enums/data.py
@src/tscollection/datasets/utils/common.py
@src/tscollection/datasets/datasets/transformations.py
@_sources/rbspaper/src/rbspaper/data/utils/arff.py
@_sources/rbspaper/src/rbspaper/data/utils/features.py
@_sources/rbspaper/src/rbspaper/data/utils/common.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DataForm enum to enums/data.py and flatten_list_of_np_arrays to common.py</name>
  <files>
    - src/tscollection/datasets/enums/data.py
    - src/tscollection/datasets/utils/common.py
  </files>
  <read_first>
    - src/tscollection/datasets/enums/data.py — current enum definitions; insert DataForm after ForecastingMode (alphabetical), before DistanceMetric
    - src/tscollection/datasets/utils/common.py — current state: has from __future__ import annotations, numpy behind TYPE_CHECKING, __all__ list, compose/FunctionComposer/get_num_samples_from_ts functions
    - _sources/rbspaper/src/rbspaper/data/utils/common.py lines 107-116 — source for flatten_list_of_np_arrays implementation
    - .planning/phases/03-utility-modules/03-PATTERNS.md — common.py MODIFY section for import restructure and DataForm enum pattern
  </read_first>
  <behavior>
    - DataForm has three members: REGULAR='regular', NESTED='nested', MULTI_FILES='multi_files'
    - DataForm inherits from StrEnum (not str, Enum)
    - flatten_list_of_np_arrays([np.array([1,2]), np.array([3,4])]) returns np.array([1,2,3,4])
    - flatten_list_of_np_arrays uses np.concatenate then ravel()
  </behavior>
  <action>
  Add DataForm enum to enums/data.py (per D-07): insert class DataForm(StrEnum) after ForecastingMode class, with Google-style docstring listing Attributes. Members: REGULAR='regular', NESTED='nested', MULTI_FILES='multi_files'.

  Modify common.py (per D-02, D-04, D-10):
  - Keep from __future__ import annotations (existing file, D-02 says stays as-is except for additions)
  - Move "import numpy as np" OUT of TYPE_CHECKING block to top-level (Pitfall 3: np.concatenate runs at runtime)
  - Add flatten_list_of_np_arrays to __all__ list (alphabetical order: after 'FunctionComposer', before 'compose' — no, 'flatten' comes after 'compose'. Order: 'FunctionComposer', 'compose', 'flatten_list_of_np_arrays', 'get_num_samples_from_ts')
  - Append flatten_list_of_np_arrays function at end of file: def flatten_list_of_np_arrays(list_of_np_arrays: list[np.ndarray]) -> np.ndarray, uses np.concatenate(list_of_np_arrays).ravel(), Google-style docstring
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.enums.data import DataForm; assert DataForm.REGULAR == 'regular'; assert DataForm.NESTED == 'nested'; assert DataForm.MULTI_FILES == 'multi_files'"</automated>
    <automated>uv run python -c "from tscollection.datasets.utils.common import flatten_list_of_np_arrays; import numpy as np; result = flatten_list_of_np_arrays([np.array([1,2]), np.array([3,4])]); assert list(result) == [1,2,3,4]"</automated>
    <automated>grep -n "^import numpy as np" src/tscollection/datasets/utils/common.py</automated>
  </verify>
  <done>
    DataForm enum exists in enums/data.py with 3 StrEnum members. flatten_list_of_np_arrays in common.py with top-level numpy import, added to __all__, callable and returns correct 1-D array.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create arff.py utility module</name>
  <files>
    - src/tscollection/datasets/utils/arff.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/utils/arff.py — full source for read_arff_as_df and process_df_according_to_dtypes
    - src/tscollection/datasets/datasets/transformations.py — analog for module structure (docstring, __all__, imports, function style)
    - .planning/phases/03-utility-modules/03-PATTERNS.md — arff.py section for imports, __all__, TYPE_CHECKING, lazy import pattern
  </read_first>
  <behavior>
    - read_arff_as_df returns tuple of (pd.DataFrame, metadata) given valid ARFF path
    - scipy.io.arff is lazy-imported inside read_arff_as_df (not top-level)
    - process_df_according_to_dtypes iterates meta.names(), applies dtypes_functions_map[col_type] to each column
    - Module has __all__ = ['process_df_according_to_dtypes', 'read_arff_as_df']
    - No from __future__ import annotations (D-10)
  </behavior>
  <action>
  Create src/tscollection/datasets/utils/arff.py ported from _sources/rbspaper/src/rbspaper/data/utils/arff.py with CLAUDE.md style compliance:

  - Module docstring: "ARFF file reading utilities for time series datasets."
  - Imports: TYPE_CHECKING guard for Any from typing; pathlib.Path; pandas as pd; collections.abc.Callable (top-level, needed for type hint on process_df_according_to_dtypes)
  - No from __future__ import annotations (D-10)
  - __all__ = ['process_df_according_to_dtypes', 'read_arff_as_df'] (alphabetical)
  - read_arff_as_df(arff_file_path: Path | str) -> tuple[pd.DataFrame, Any]: lazy-import scipy.io.arff inside function, call arff.loadarff, wrap data in pd.DataFrame, return (df, meta). Google-style docstring noting scipy returns bytes for nominal columns (Pitfall 2).
  - process_df_according_to_dtypes(df_data: pd.DataFrame, meta: Any, dtypes_functions_map: dict[str, Callable]) -> pd.DataFrame: iterate meta.names(), apply dtypes_functions_map[col_type](df_data[col_name]). Google-style docstring.
  - Per D-11: no src.rbspaper imports (this file has none in source).
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.utils.arff import read_arff_as_df, process_df_according_to_dtypes; print('imports OK')"</automated>
    <automated>grep -c "from __future__" src/tscollection/datasets/utils/arff.py || true</automated>
    <automated>grep "__all__" src/tscollection/datasets/utils/arff.py</automated>
  </verify>
  <done>
    arff.py exists with read_arff_as_df and process_df_according_to_dtypes, correct __all__, no future annotations, lazy scipy import, Google-style docstrings, keyword-only where appropriate.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create features.py utility module</name>
  <files>
    - src/tscollection/datasets/utils/features.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/utils/features.py — full source for extract_time_features
    - src/tscollection/datasets/utils/common.py — analog for utils package style
    - .planning/phases/03-utility-modules/03-PATTERNS.md — features.py section for imports, __all__, function signature
  </read_first>
  <behavior>
    - extract_time_features(pd.DatetimeIndex) returns np.ndarray of shape (N, 7) with dtype float32
    - Columns: minute, hour, dayofweek, day, dayofyear, month, week (from isocalendar())
    - Uses series.dt.isocalendar().week.to_numpy() (pandas 3.0 compatible)
    - Module has __all__ = ['extract_time_features']
    - No from __future__ import annotations (D-10)
  </behavior>
  <action>
  Create src/tscollection/datasets/utils/features.py ported from _sources/rbspaper/src/rbspaper/data/utils/features.py with CLAUDE.md style compliance:

  - Module docstring: "Time feature extraction utilities for forecasting datasets."
  - Top-level imports: numpy as np, pandas as pd
  - No from __future__ import annotations (D-10)
  - __all__ = ['extract_time_features']
  - extract_time_features(datetime_index: pd.DatetimeIndex) -> np.ndarray: single required positional arg (no keyword-only separator for single-param function per RESEARCH.md). Uses datetime_index.to_series() then np.stack of 7 feature arrays. Cast to np.float32. Google-style docstring.
  - Per D-11: no src.rbspaper imports (this file has none in source).
  </action>
  <verify>
    <automated>uv run python -c "import pandas as pd, numpy as np; from tscollection.datasets.utils.features import extract_time_features; dti = pd.date_range('2020-01-01', periods=10, freq='h'); r = extract_time_features(dti); assert r.shape == (10, 7); assert r.dtype == np.float32"</automated>
    <automated>grep -c "from __future__" src/tscollection/datasets/utils/features.py || true</automated>
  </verify>
  <done>
    features.py exists with extract_time_features returning (N,7) float32 array, correct __all__, no future annotations, Google-style docstring, verified with pandas 3.0 lowercase freq='h'.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arff_file_path -> scipy.io.arff.loadarff | Untrusted file path crosses into file I/O; caller controls paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-01 | Information Disclosure | read_arff_as_df | Accept | No PII in ARFF data; scipy handles parsing safely; caller controls file paths |
| T-03-02 | Tampering | process_df_according_to_dtypes | Accept | Pure function; dtypes_functions_map is caller-controlled; no external trust boundary crossed |

</threat_model>

<verification>
- DataForm enum importable from enums.data with correct StrEnum members
- flatten_list_of_np_arrays returns correct 1-D array, numpy is top-level import in common.py
- arff.py imports without loading scipy (lazy import pattern)
- features.py extract_time_features produces (N,7) float32 with pandas 3.0
- All files have Google-style docstrings, __all__ exports, no from __future__ import annotations
</verification>

<success_criteria>
- enums/data.py contains DataForm(StrEnum) with REGULAR, NESTED, MULTI_FILES
- common.py has top-level numpy import and flatten_list_of_np_arrays in __all__
- arff.py exists with read_arff_as_df and process_df_according_to_dtypes
- features.py exists with extract_time_features
- All imports resolve without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-utility-modules/03-01-SUMMARY.md`
</output>
