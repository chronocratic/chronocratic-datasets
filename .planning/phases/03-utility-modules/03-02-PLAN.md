---
phase: 03
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/tscollection/datasets/utils/scaling.py
  - src/tscollection/datasets/utils/general.py
autonomous: true
requirements: [UTI-02, UTI-04]
user_setup: []
must_haves:
  truths:
    - "create_data_scaler with ScalingMethod.MINMAX returns MinMaxScaler-based closure"
    - "create_data_scaler with DataForm.NESTED preserves 3-D array shape"
    - "create_data_scaler with scale=False returns data unchanged"
    - "_get_scaler compares against ScalingMethod enum members (not source strings)"
    - "custom_collate_fn pads short batches by cycling samples"
    - "centralize_variable_length_series centers NaN-padded data"
  artifacts:
    - path: "src/tscollection/datasets/utils/scaling.py"
      provides: "create_data_scaler factory with enum-typed parameters"
      min_lines: 120
    - path: "src/tscollection/datasets/utils/general.py"
      provides: "custom_collate_fn, centralize_variable_length_series, process_data_with_varying_sequence_lengths_single"
      min_lines: 60
  key_links:
    - from: "scaling.py"
      to: "enums/data.py"
      via: "from tscollection.datasets.enums.data import DataForm, ScalingMethod"
      pattern: "from tscollection\\.datasets\\.enums\\.data import"
    - from: "scaling.py"
      to: "common.py"
      via: "from tscollection.datasets.utils.common import flatten_list_of_np_arrays"
      pattern: "from tscollection\\.datasets\\.utils\\.common import flatten_list_of_np_arrays"
    - from: "general.py"
      to: "torch.utils.data.dataloader"
      via: "from torch.utils.data.dataloader import default_collate"
      pattern: "from torch\\.utils\\.data\\.dataloader import default_collate"
---

<objective>
Create scaling.py (with enum wiring per D-05, D-06, D-07) and general.py utility modules.

Purpose: scaling.py depends on DataForm enum (created by Plan 01) and flatten_list_of_np_arrays from common.py. general.py is self-contained but grouped here to maintain right-sized plans. Both address core utility requirements.

Output: 2 new files with CLAUDE.md compliance and enum-wired scaling.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-utility-modules/03-CONTEXT.md
@.planning/phases/03-utility-modules/03-RESEARCH.md
@.planning/phases/03-utility-modules/03-PATTERNS.md
@src/tscollection/datasets/enums/data.py
@src/tscollection/datasets/utils/common.py
@_sources/rbspaper/src/rbspaper/data/utils/scaling.py
@_sources/rbspaper/src/rbspaper/data/utils/general.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scaling.py with enum-wired create_data_scaler</name>
  <files>
    - src/tscollection/datasets/utils/scaling.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/utils/scaling.py — full source (264 lines): create_data_scaler, _get_scaler, _scale_regular_data, _scale_regular_data_and_return_same_type, _scale_multi_file_data, _scale_nested_data_all_dimensions, DataFormEnum class
    - src/tscollection/datasets/enums/data.py — confirms ScalingMethod members (MINMAX='minmax', STANDARD='standard') and DataForm members (REGULAR='regular', NESTED='nested', MULTI_FILES='multi_files')
    - src/tscollection/datasets/utils/common.py — confirms flatten_list_of_np_arrays exists
    - .planning/phases/03-utility-modules/03-PATTERNS.md — scaling.py section for enum wiring, import pattern, __all__
    - .planning/phases/03-utility-modules/03-RESEARCH.md — Pitfall 1 (ScalingMethod value mismatch), Pitfall 6 (DataFormEnum name change), code examples for _get_scaler and create_data_scaler signatures
  </read_first>
  <behavior>
    - create_data_scaler(scale=True, scaling_range=(0.0, 1.0), scaling_method=ScalingMethod.MINMAX, data_form=DataForm.REGULAR) returns callable
    - _get_scaler(ScalingMethod.MINMAX, (0,1)) returns MinMaxScaler instance
    - _get_scaler(ScalingMethod.STANDARD, (0,1)) returns StandardScaler instance
    - create_data_scaler uses DataForm and ScalingMethod enums (not strings) per D-05
    - _get_scaler compares scaling_method == ScalingMethod.MINMAX (not 'min_max') per D-06
    - No internal DataFormEnum class defined (imported from enums/data.py per D-07)
  </behavior>
  <action>
  Create src/tscollection/datasets/utils/scaling.py ported from _sources/rbspaper/src/rbspaper/data/utils/scaling.py with these CRITICAL changes:

  - Module docstring: "Data scaling strategies for time series datasets."
  - No from __future__ import annotations (D-10)
  - Top-level imports: numpy as np, pandas as pd, MinMaxScaler and StandardScaler from sklearn.preprocessing
  - Import DataForm and ScalingMethod from tscollection.datasets.enums.data (per D-05, D-07) — NOT defining DataFormEnum locally
  - Import flatten_list_of_np_arrays from tscollection.datasets.utils.common (per D-04, D-11)
  - TYPE_CHECKING guard for Callable and Any from collections.abc/typing
  - __all__ = ['create_data_scaler'] — private helpers (_get_scaler, _scale_*) NOT exported

  create_data_scaler signature (per D-05, D-06):
  - def create_data_scaler(*, scale: bool, scaling_range: tuple[float, float], scaling_method: ScalingMethod = ScalingMethod.MINMAX, data_form: DataForm = DataForm.REGULAR) -> Callable
  - CRITICAL: default is ScalingMethod.MINMAX (not string 'min_max')

  _get_scaler changes (per D-06):
  - Compare scaling_method == ScalingMethod.MINMAX (NOT 'min_max')
  - Compare scaling_method == ScalingMethod.STANDARD (NOT 'standardization')
  - Signature: _get_scaler(scaling_method: ScalingMethod, scaling_range: tuple[float, float]) -> MinMaxScaler | StandardScaler

  In scale_data closure, compare data_form == DataForm.REGULAR (NOT DataFormEnum.REGULAR.value), etc. per D-07.

  Port all private helpers verbatim from source (with updated type hints using enums):
  - _scale_regular_data
  - _scale_regular_data_and_return_same_type
  - _scale_multi_file_data (uses flatten_list_of_np_arrays)
  - _scale_nested_data_all_dimensions
  - All with Google-style docstrings, keyword-only args where appropriate (D-09)
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.enums.data import ScalingMethod, DataForm; from tscollection.datasets.utils.scaling import create_data_scaler; fn = create_data_scaler(scale=True, scaling_range=(0.0, 1.0), scaling_method=ScalingMethod.MINMAX, data_form=DataForm.REGULAR); print('enum wiring OK')"</automated>
    <automated>grep -c "DataFormEnum" src/tscollection/datasets/utils/scaling.py || true</automated>
    <automated>grep "__all__" src/tscollection/datasets/utils/scaling.py</automated>
  </verify>
  <done>
    scaling.py exists with create_data_scaler using ScalingMethod and DataForm enums (not strings), no DataFormEnum class defined locally, _get_scaler compares enum members, flatten_list_of_np_arrays imported from common.py, __all__ exports only create_data_scaler, private helpers ported with Google-style docstrings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create general.py utility module</name>
  <files>
    - src/tscollection/datasets/utils/general.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/utils/general.py — full source (108 lines): custom_collate_fn, centralize_variable_length_series, process_data_with_varying_sequence_lengths_single
    - src/tscollection/datasets/datasets/transformations.py — analog for module structure
    - .planning/phases/03-utility-modules/03-PATTERNS.md — general.py section for imports, __all__, keyword-only separator
  </read_first>
  <behavior>
    - custom_collate_fn pads batch to desired_batch_size by cycling backwards
    - custom_collate_fn uses keyword-only separator: def custom_collate_fn(batch: list[Any], *, desired_batch_size: int) -> Any (D-09)
    - centralize_variable_length_series centers NaN-padded sequences in 3-D batch
    - process_data_with_varying_sequence_lengths_single handles 2-D and 3-D data
    - Module has __all__ with 3 public functions (alphabetical)
  </behavior>
  <action>
  Create src/tscollection/datasets/utils/general.py ported from _sources/rbspaper/src/rbspaper/data/utils/general.py with CLAUDE.md style compliance:

  - Module docstring: "General data utilities: collation, variable-length handling."
  - Top-level imports: numpy as np, pandas as pd, default_collate from torch.utils.data.dataloader (runtime-used, NOT behind TYPE_CHECKING per RESEARCH.md)
  - No from __future__ import annotations (D-10)
  - TYPE_CHECKING guard for Any from typing
  - __all__ = ['centralize_variable_length_series', 'custom_collate_fn', 'process_data_with_varying_sequence_lengths_single'] (alphabetical)

  custom_collate_fn: Add keyword-only separator — def custom_collate_fn(batch: list[Any], *, desired_batch_size: int) -> Any (D-09). Cycles backwards through batch to pad, then calls default_collate.

  centralize_variable_length_series: def centralize_variable_length_series(series_batch: np.ndarray) -> np.ndarray. Single required arg, no separator needed. Finds valid index range, computes offset, shifts via numpy indexing.

  process_data_with_varying_sequence_lengths_single: def process_data_with_varying_sequence_lengths_single(data: np.ndarray | pd.DataFrame) -> np.ndarray | pd.DataFrame. Handles 2-D/3-D, expands/squeezes axis, checks temporal_missing, calls centralize_variable_length_series.

  All functions with Google-style docstrings (D-09). Per D-11: no src.rbspaper imports.
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.utils.general import custom_collate_fn, centralize_variable_length_series, process_data_with_varying_sequence_lengths_single; print('imports OK')"</automated>
    <automated>grep "def custom_collate_fn" src/tscollection/datasets/utils/general.py</automated>
    <automated>grep "__all__" src/tscollection/datasets/utils/general.py</automated>
  </verify>
  <done>
    general.py exists with custom_collate_fn (keyword-only desired_batch_size), centralize_variable_length_series, process_data_with_varying_sequence_lengths_single. Correct __all__, default_collate top-level import, Google-style docstrings, no future annotations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scaling.py input data -> sklearn scalers | Untrusted numeric data passed to fit/transform; no auth boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-03 | Spof | create_data_scaler | Accept | Single scaler per call; no shared mutable state across invocations; closure is call-site scoped |
| T-03-04 | Information Disclosure | custom_collate_fn | Accept | Operates on in-memory tensors; no external data exposure; PyTorch handles device placement |

</threat_model>

<verification>
- create_data_scaler accepts ScalingMethod and DataForm enums (not strings)
- _get_scaler uses ScalingMethod.MINMAX / ScalingMethod.STANDARD comparisons (not 'min_max'/'standardization')
- No DataFormEnum class defined in scaling.py (imported from enums/data.py)
- flatten_list_of_np_arrays imported from tscollection.datasets.utils.common
- custom_collate_fn has keyword-only desired_batch_size parameter
- All files have Google-style docstrings, __all__ exports, no from __future__ import annotations
</verification>

<success_criteria>
- scaling.py exists with enum-wired create_data_scaler, private helpers not in __all__
- _get_scaler compares ScalingMethod enum members (not source strings)
- general.py exists with 3 functions, keyword-only desired_batch_size on custom_collate_fn
- All imports resolve without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-utility-modules/03-02-SUMMARY.md`
</output>
