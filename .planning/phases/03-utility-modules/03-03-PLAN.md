---
phase: 03
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/tscollection/datasets/utils/__init__.py
  - src/tscollection/datasets/enums/__init__.py
  - src/tscollection/datasets/__init__.py
  - tests/test_utils_arff.py
  - tests/test_utils_scaling.py
  - tests/test_utils_features.py
  - tests/test_utils_general.py
autonomous: true
requirements: [UTI-05]
user_setup: []
must_haves:
  truths:
    - "All 11 utility symbols importable from tscollection.datasets.utils"
    - "DataForm importable from tscollection.datasets.enums and tscollection.datasets root"
    - "test_utils_arff.py tests pass with pytest"
    - "test_utils_scaling.py tests pass with enum-wired create_data_scaler"
    - "test_utils_features.py tests pass with (N,7) float32 assertions"
    - "test_utils_general.py tests pass for collation and centering"
  artifacts:
    - path: "src/tscollection/datasets/utils/__init__.py"
      provides: "Re-exports all utility functions from 4 modules + common.py"
      min_lines: 15
    - path: "src/tscollection/datasets/enums/__init__.py"
      provides: "DataForm added to imports and __all__"
      contains: "DataForm"
    - path: "src/tscollection/datasets/__init__.py"
      provides: "DataForm re-exported at package root"
      contains: "DataForm"
    - path: "tests/test_utils_arff.py"
      provides: "Tests for read_arff_as_df, process_df_according_to_dtypes"
      min_lines: 25
    - path: "tests/test_utils_scaling.py"
      provides: "Tests for create_data_scaler with enum wiring"
      min_lines: 40
    - path: "tests/test_utils_features.py"
      provides: "Tests for extract_time_features shape and dtype"
      min_lines: 20
    - path: "tests/test_utils_general.py"
      provides: "Tests for custom_collate_fn, centralize_variable_length_series"
      min_lines: 30
  key_links:
    - from: "utils/__init__.py"
      to: "utils/arff.py"
      via: "from tscollection.datasets.utils.arff import ..."
      pattern: "from tscollection\\.datasets\\.utils\\.arff import"
    - from: "utils/__init__.py"
      to: "utils/scaling.py"
      via: "from tscollection.datasets.utils.scaling import create_data_scaler"
      pattern: "from tscollection\\.datasets\\.utils\\.scaling import"
    - from: "enums/__init__.py"
      to: "enums/data.py"
      via: "DataForm added to import block and __all__"
      pattern: "DataForm"
    - from: "datasets/__init__.py"
      to: "enums/__init__.py"
      via: "DataForm added to enum re-exports"
      pattern: "DataForm"
---

<objective>
Wire all __init__.py exports (UTI-05) and create test files for all utility modules.

Purpose: UTI-05 requires each utility in a separate file with proper __all__ exports. This plan wires the public API surface and validates with tests. All 4 utility modules + DataForm enum must be importable.

Output: 3 __init__.py files updated, 4 test files created, full test suite green.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-utility-modules/03-CONTEXT.md
@.planning/phases/03-utility-modules/03-RESEARCH.md
@.planning/phases/03-utility-modules/03-PATTERNS.md
@.planning/phases/03-utility-modules/03-VALIDATION.md
@src/tscollection/datasets/utils/__init__.py
@src/tscollection/datasets/enums/__init__.py
@src/tscollection/datasets/__init__.py
@tests/test_transformations.py
@tests/test_strategies.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all __init__.py exports</name>
  <files>
    - src/tscollection/datasets/utils/__init__.py
    - src/tscollection/datasets/enums/__init__.py
    - src/tscollection/datasets/__init__.py
  </files>
  <read_first>
    - src/tscollection/datasets/utils/__init__.py — current state: imports compose, FunctionComposer, get_num_samples_from_ts from common.py
    - src/tscollection/datasets/enums/__init__.py — current state: imports 7 enums (no DataForm)
    - src/tscollection/datasets/__init__.py — current state: re-exports 7 enums + __version__ (no DataForm)
    - .planning/phases/03-utility-modules/03-PATTERNS.md — utils/__init__.py MODIFY section for full wiring pattern, enums/__init__.py and datasets/__init__.py additions
  </read_first>
  <action>
  Update utils/__init__.py (per D-01, D-03, UTI-05):
  - Add imports from tscollection.datasets.utils.arff: process_df_according_to_dtypes, read_arff_as_df
  - Add flatten_list_of_np_arrays to common.py import block
  - Add import from tscollection.datasets.utils.features: extract_time_features
  - Add imports from tscollection.datasets.utils.general: centralize_variable_length_series, custom_collate_fn, process_data_with_varying_sequence_lengths_single
  - Add import from tscollection.datasets.utils.scaling: create_data_scaler
  - Update __all__ to include all symbols (alphabetical): FunctionComposer, centralize_variable_length_series, compose, create_data_scaler, custom_collate_fn, extract_time_features, flatten_list_of_np_arrays, get_num_samples_from_ts, process_data_with_varying_sequence_lengths_single, process_df_according_to_dtypes, read_arff_as_df
  - Keep import groups organized by source module

  Update enums/__init__.py (per D-07):
  - Add DataForm to import block from tscollection.datasets.enums.data (alphabetical: before DatasetFamily)
  - Add 'DataForm' to __all__ list (alphabetical: before 'DatasetFamily')

  Update datasets/__init__.py (per RESEARCH.md open question 2):
  - Add DataForm to import block from tscollection.datasets.enums (alphabetical: before DatasetFamily)
  - Add 'DataForm' to __all__ list (alphabetical: before 'DatasetFamily')
  </action>
  <verify>
    <automated>uv run python -c "from tscollection.datasets.utils import read_arff_as_df, process_df_according_to_dtypes, create_data_scaler, extract_time_features, custom_collate_fn, centralize_variable_length_series, process_data_with_varying_sequence_lengths_single, flatten_list_of_np_arrays; print('utils exports OK')"</automated>
    <automated>uv run python -c "from tscollection.datasets import DataForm; assert DataForm.REGULAR == 'regular'; print('DataForm root export OK')"</automated>
    <automated>uv run python -c "from tscollection.datasets.enums import DataForm; print('DataForm enum export OK')"</automated>
  </verify>
  <done>
    All 3 __init__.py files updated. 11 utility symbols importable from tscollection.datasets.utils. DataForm importable from both tscollection.datasets.enums and tscollection.datasets root.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test files for all utility modules</name>
  <files>
    - tests/test_utils_arff.py
    - tests/test_utils_scaling.py
    - tests/test_utils_features.py
    - tests/test_utils_general.py
  </files>
  <read_first>
    - tests/test_transformations.py — analog for module docstring, import style, test function patterns, requirement ID prefixes
    - tests/test_strategies.py — analog for section separators, multiple assertion patterns, -> None return type
    - tests/conftest.py — existing fixtures (synthetic_classification_df, synthetic_forecast_data, etc.)
    - .planning/phases/03-utility-modules/03-PATTERNS.md — test sections for each utility (imports, structure, docstring patterns)
    - .planning/phases/03-utility-modules/03-RESEARCH.md — Pitfall 4 (pandas 3.0 lowercase freq aliases), code examples for enum wiring tests
    - src/tscollection/datasets/utils/arff.py — source for arff tests
    - src/tscollection/datasets/utils/scaling.py — source for scaling tests
    - src/tscollection/datasets/utils/features.py — source for features tests
    - src/tscollection/datasets/utils/general.py — source for general tests
  </read_first>
  <behavior>
    - test_utils_arff.py: read_arff_as_df returns DataFrame + metadata from synthetic ARFF file; process_df_according_to_dtypes applies transformations
    - test_utils_scaling.py: create_data_scaler with ScalingMethod.MINMAX + DataForm.REGULAR scales to range; DataForm.NESTED preserves 3-D shape; scale=False returns data unchanged
    - test_utils_features.py: extract_time_features returns (N, 7) float32 array; uses freq='h' (lowercase, pandas 3.0)
    - test_utils_general.py: custom_collate_fn pads short batch; centralize_variable_length_series centers data
    - All tests use requirement ID prefix in docstrings (UTI-01:, UTI-02:, etc.)
  </behavior>
  <action>
  Create 4 test files following patterns from test_transformations.py and test_strategies.py:

  tests/test_utils_arff.py:
  - Module docstring listing UTI-01 and tested functions
  - Import from tscollection.datasets.utils.arff: read_arff_as_df, process_df_according_to_dtypes
  - test_read_arff_as_df_returns_dataframe_and_metadata: Create temporary ARFF file using tmp_path fixture (write minimal ARFF with @relation, @attribute numeric, @data lines). Call read_arff_as_df, assert isinstance(result[0], pd.DataFrame) and result[1] is not None. Docstring: "UTI-01: read_arff_as_df returns DataFrame and ARFF metadata."
  - test_process_df_according_to_dtypes_applies_map: Create DataFrame + mock meta with names() returning column list. Call with dtypes_functions_map mapping column types to functions. Assert columns transformed. Docstring: "UTI-01: process_df_according_to_dtypes applies transformations from dtypes_functions_map."

  tests/test_utils_scaling.py:
  - Module docstring listing UTI-02 and tested functions
  - Import from tscollection.datasets.enums.data: DataForm, ScalingMethod; from tscollection.datasets.utils.scaling: create_data_scaler
  - Section separator comments per test group
  - REGULAR group: test_create_data_scaler_regular_minmax: numpy arrays for train/valid/test, scale=True, range=(0,1), method=ScalingMethod.MINMAX. Assert scaled values in [0,1]. Docstring: "UTI-02: create_data_scaler with REGULAR data and MinMax scaling."
  - test_create_data_scaler_no_scale: scale=False, assert data returned unchanged. Docstring: "UTI-02: scale=False returns data unchanged."
  - test_scaling_method_enum_wiring: assert ScalingMethod.MINMAX used (not string). Docstring: "UTI-02/D-05: ScalingMethod enum wired correctly."
  - NESTED group: test_create_data_scaler_nested: 3-D arrays (samples, timesteps, features). Assert output shape == input shape. Docstring: "UTI-02: NESTED preserves 3-D shape."
  - MULTI_FILES group: test_create_data_scaler_multi_files: list of 1-D arrays. Assert returns list of scaled arrays. Docstring: "UTI-02: MULTI_FILES scales list of arrays."
  - Use keyword arguments for all create_data_scaler calls per CLAUDE.md

  tests/test_utils_features.py:
  - Module docstring listing UTI-03 and tested functions
  - Import from tscollection.datasets.utils.features: extract_time_features
  - test_extract_time_features_shape: dti = pd.date_range('2020-01-01', periods=10, freq='h') — NOTE: lowercase 'h' per Pitfall 4 (pandas 3.0). Assert result.shape == (10, 7) and result.dtype == np.float32. Docstring: "UTI-03: extract_time_features returns (N, 7) float32 array."
  - test_extract_time_features_values: assert minute/hour columns have correct values for known datetimes. Docstring: "UTI-03: Feature columns contain correct extracted values."

  tests/test_utils_general.py:
  - Module docstring listing UTI-04 and tested functions
  - Import from tscollection.datasets.utils.general: custom_collate_fn, centralize_variable_length_series, process_data_with_varying_sequence_lengths_single
  - Section separator comments per test group
  - custom_collate_fn group: test_custom_collate_fn_pads_last_batch: batch=[torch.tensor([1.0]), torch.tensor([2.0])], desired_batch_size=4. Assert result.shape[0] == 4. Docstring: "UTI-04: custom_collate_fn pads short batches by cycling."
  - centralize_variable_length_series group: test_centralize_shifts_valid_data: 3-D array with NaN padding at start/end. Assert valid data shifted to center. Docstring: "UTI-04: Centering shifts valid data to middle of sequence."
  - process_data_with_varying_sequence_lengths_single group: test_process_handles_2d: 2-D array expanded and squeezed correctly. Docstring: "UTI-04: process_data handles 2-D input correctly."
  - Use keyword arguments for custom_collate_fn (desired_batch_size is keyword-only per D-09)
  </action>
  <verify>
    <automated>uv run pytest tests/test_utils_arff.py tests/test_utils_scaling.py tests/test_utils_features.py tests/test_utils_general.py -x -q</automated>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    4 test files created. All utility tests pass. Full test suite (including existing tests) passes. Tests use requirement ID prefixes in docstrings, keyword arguments, lowercase pandas freq aliases.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by this plan. Export wiring is pure module-level imports. Tests use synthetic data only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-05 | Tampering | utils/__init__.py | Accept | Module-level imports from same package; no external input |
| T-03-06 | Information Disclosure | Test fixtures | Accept | Synthetic data only; no real datasets or secrets in tests |

</threat_model>

<verification>
- All 11 utility symbols importable from tscollection.datasets.utils
- DataForm importable from tscollection.datasets root and tscollection.datasets.enums
- test_utils_arff.py: 2 tests pass (read_arff_as_df, process_df_according_to_dtypes)
- test_utils_scaling.py: 5 tests pass (REGULAR minmax, no-scale, enum wiring, NESTED, MULTI_FILES)
- test_utils_features.py: 2 tests pass (shape, values)
- test_utils_general.py: 3 tests pass (collation, centering, 2-D handling)
- Full pytest suite (existing + new) passes with uv run pytest tests/ -x -q
</verification>

<success_criteria>
- utils/__init__.py exports all 11 utility symbols (alphabetical __all__)
- enums/__init__.py and datasets/__init__.py export DataForm
- All 4 test files created with correct patterns
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-utility-modules/03-03-SUMMARY.md`
</output>
