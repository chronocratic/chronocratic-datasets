---
phase: 04-data-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tscollection/datasets/enums/data.py
  - src/tscollection/datasets/enums/__init__.py
  - src/tscollection/datasets/__init__.py
  - src/tscollection/datasets/utils/common.py
  - src/tscollection/datasets/utils/__init__.py
  - src/tscollection/datasets/modules/classes/base.py
autonomous: true
requirements: [MOD-01, MOD-05, MOD-06]
user_setup: []

must_haves:
  truths:
    - "ClassificationSplittingStrategy enum exists and is importable from tscollection.datasets.enums"
    - "separate_target_feature_from_df is importable from tscollection.datasets.utils"
    - "BaseTimeSeriesDataModule inherits from pl.LightningDataModule and ABC"
    - "BaseTimeSeriesDataModule exposes sequence_length, num_features as read-only properties"
  artifacts:
    - path: "src/tscollection/datasets/enums/data.py"
      provides: "ClassificationSplittingStrategy enum (renamed from SplittingStrategy)"
      contains: "class ClassificationSplittingStrategy"
    - path: "src/tscollection/datasets/utils/common.py"
      provides: "separate_target_feature_from_df utility function"
      contains: "def separate_target_feature_from_df"
    - path: "src/tscollection/datasets/modules/classes/base.py"
      provides: "BaseTimeSeriesDataModule with dataloader construction and scaling setup"
      min_lines: 100
  key_links:
    - from: "modules/classes/base.py"
      to: "utils/scaling.py"
      via: "create_data_scaler import"
      pattern: "from tscollection.datasets.utils.scaling import create_data_scaler"
    - from: "modules/classes/base.py"
      to: "utils/general.py"
      via: "custom_collate_fn import"
      pattern: "from tscollection.datasets.utils.general import custom_collate_fn"
---

<objective>
Rename SplittingStrategy enum, port missing utility, and create BaseTimeSeriesDataModule.

Purpose: Establish the foundation for all data module classes by (1) performing the enum rename required by D-04, (2) adding the missing separate_target_feature_from_df utility identified as Pitfall 2 in research, and (3) creating the shared base class with dataloader construction, scaling setup, and property definitions.

Output: Updated enums, updated utils/common.py, new modules/classes/base.py.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/STATE.md

# Source references
@/Users/skaf/VSCodeProjects/tsdatasets/.planning/phases/04-data-modules/04-CONTEXT.md
@/Users/skaf/VSCodeProjects/tsdatasets/_sources/rbspaper/src/rbspaper/data/modules/abstract.py
@/Users/skaf/VSCodeProjects/tsdatasets/_sources/rbspaper/src/rbspaper/data/utils/common.py

# Existing code to modify
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/enums/data.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/enums/__init__.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/__init__.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/utils/common.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/utils/__init__.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/modules/classes/__init__.py

# Reference implementations
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/utils/scaling.py
@/Users/skaf/VSCodeProjects/tsdatasets/src/tscollection/datasets/utils/general.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename SplittingStrategy to ClassificationSplittingStrategy and port separate_target_feature_from_df</name>
  <files>
    src/tscollection/datasets/enums/data.py
    src/tscollection/datasets/enums/__init__.py
    src/tscollection/datasets/__init__.py
    src/tscollection/datasets/utils/common.py
    src/tscollection/datasets/utils/__init__.py
  </files>
  <read_first>
    - src/tscollection/datasets/enums/data.py — current SplittingStrategy class to rename per D-04
    - src/tscollection/datasets/enums/__init__.py — re-exports SplittingStrategy; must update to ClassificationSplittingStrategy
    - src/tscollection/datasets/__init__.py — root exports SplittingStrategy; must update
    - src/tscollection/datasets/utils/common.py — current file; need to add separate_target_feature_from_df
    - src/tscollection/datasets/utils/__init__.py — current exports; need to add separate_target_feature_from_df
    - _sources/rbspaper/src/rbspaper/data/utils/common.py — source for separate_target_feature_from_df signature
    - .planning/phases/04-data-modules/04-CONTEXT.md — D-04 (rename) and research Pitfall 2 (missing utility)
  </read_first>
  <behavior>
    - Test 1: ClassificationSplittingStrategy.AS_DEFINED == 'as_defined'
    - Test 2: ClassificationSplittingStrategy.MANUAL == 'manual'
    - Test 3: separate_target_feature_from_df(df, 'label') returns (features_df, target_series) where features_df has one fewer column
    - Test 4: Import from tscollection.datasets.enums succeeds for ClassificationSplittingStrategy
    - Test 5: Import from tscollection.datasets.utils succeeds for separate_target_feature_from_df
  </behavior>
  <action>
    Per D-04: In enums/data.py, rename class SplittingStrategy to ClassificationSplittingStrategy. Update docstring to "Strategy for classification train/test data splitting." Keep enum values AS_DEFINED='as_defined' and MANUAL='manual'.

    Per D-04: In enums/__init__.py, replace SplittingStrategy import with ClassificationSplittingStrategy from enums.data. Update __all__ to list 'ClassificationSplittingStrategy' instead of 'SplittingStrategy'.

    Per D-04: In root __init__.py (src/tscollection/datasets/__init__.py), replace SplittingStrategy import with ClassificationSplittingStrategy. Update __all__ accordingly.

    Per research Pitfall 2: In utils/common.py, add separate_target_feature_from_df function ported from rbspaper source. Signature: def separate_target_feature_from_df(df: pd.DataFrame, target_feature_name: str) -> tuple[pd.DataFrame, pd.Series]. Implementation: extract target column as Series, drop it from DataFrame, return (features, target). Use pandas keyword arguments (axis=1 for drop). Add to __all__ list.

    Per research Pitfall 2: In utils/__init__.py, import separate_target_feature_from_df from .common and add to __all__ (alphabetically sorted).

    IMPORTANT: Add import for pandas (pd) to utils/common.py since the existing file only imports numpy. Use "import pandas as pd" in standard imports section.
  </action>
  <verify>
    <automated>python -c "from tscollection.datasets.enums import ClassificationSplittingStrategy; assert ClassificationSplittingStrategy.AS_DEFINED == 'as_defined'"</automated>
    <automated>python -c "from tscollection.datasets.utils import separate_target_feature_from_df; import pandas as pd; df=pd.DataFrame({'a':[1],'b':[2]}); f,t=separate_target_feature_from_df(df,'a'); assert list(f.columns)==['b']"</automated>
    <automated>grep -c "class ClassificationSplittingStrategy" src/tscollection/datasets/enums/data.py</automated>
    <automated>grep -c "class SplittingStrategy" src/tscollection/datasets/enums/data.py; test $(grep -c "class SplittingStrategy" src/tscollection/datasets/enums/data.py) -eq 0</automated>
  </verify>
  <done>
    ClassificationSplittingStrategy enum exists with AS_DEFINED and MANUAL values. SplittingStrategy class no longer exists. separate_target_feature_from_df importable from tscollection.datasets.utils and returns (DataFrame, Series) tuple.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create BaseTimeSeriesDataModule in modules/classes/base.py</name>
  <files>
    src/tscollection/datasets/modules/classes/base.py
  </files>
  <read_first>
    - _sources/rbspaper/src/rbspaper/data/modules/abstract.py — rbspaper BaseTimeSeriesDataModule source (lines 41-199) for pattern reference
    - src/tscollection/datasets/utils/scaling.py — create_data_scaler signature for setup() implementation
    - src/tscollection/datasets/utils/general.py — custom_collate_fn signature for _get_custom_collate_fn
    - src/tscollection/datasets/enums/data.py — ScalingMethod and DataForm enums for type hints
    - .planning/phases/04-data-modules/04-CONTEXT.md — D-08, D-09, D-10, D-11 decisions
    - src/tscollection/datasets/modules/classes/__init__.py — current stub; will be wired in Plan 04
  </read_first>
  <behavior>
    - Test 1: BaseTimeSeriesDataModule is subclass of pl.LightningDataModule
    - Test 2: Constructor accepts batch_size, seq_len, valid_size, test_size, shuffle, scale_data, data_scaling_method, data_scaling_range, num_workers, data_form as keyword args
    - Test 3: sequence_length property returns _seq_len value
    - Test 4: num_features property returns _num_features value
    - Test 5: setup() calls create_data_scaler and scales train/valid/test samples when scale_data=True
    - Test 6: _process_train_dataloader returns DataLoader instance
    - Test 7: _process_test_dataloader returns DataLoader with shuffle=False
    - Test 8: _process_valid_dataloader returns None when valid_size==0.0
  </behavior>
  <action>
    Create modules/classes/base.py with BaseTimeSeriesDataModule class.

    Per D-08: This is the shared base for ALL data modules.

    Per D-09: setup() handles scaling (not prepare_data). prepare_data() is abstract — subclasses implement file validation + loading.

    Per D-10: Classification branch uses create_data_scaler() from utils (this base). Forecasting branch overrides setup() entirely with sklearn direct scaling.

    Per D-11: Properties named sequence_length, num_features, num_classes (full names). Internal attributes: _seq_len, _num_features, _num_classes.

    Key deviations from rbspaper source:
    - data_scaling_method typed as ScalingMethod (D-03), not str
    - data_form typed as DataForm (D-02), not str
    - File paths typed as Path only (D-07), not str
    - Constructor uses keyword-only args (after *) per project conventions
    - Properties: sequence_length (not sequence_len), num_features (not n_features)

    Constructor signature:
    - batch_size: int
    - seq_len: int | None — None for classification (computed from data), int for forecasting (user-provided)
    - valid_size: float
    - test_size: float
    - shuffle: bool
    - scale_data: bool
    - data_scaling_method: ScalingMethod — default ScalingMethod.MINMAX (D-03)
    - data_scaling_range: tuple[float, float] — default (0, 1)
    - num_workers: int — default 0
    - data_form: DataForm — default DataForm.REGULAR

    setup(stage: str): Call create_data_scaler() with self.scale_data, self.data_scaling_range, self.data_scaling_method, self._data_form. Invoke returned callable on (_train_data_samples, _valid_data_samples, _test_data_samples). Assign results back.

    _process_train_dataloader(*, dataset_object, shuffle=None, strict_batch_size=False, extra_args=None): Build DataLoader. Use self.shuffle if shuffle is None. Guard persistent_workers behind num_workers > 0 check (Pitfall 6). If strict_batch_size, set collate_fn to partial(custom_collate_fn, desired_batch_size=batch_size).

    _process_test_dataloader(*, dataset_object, strict_batch_size=False, extra_args=None): Same as train but shuffle=False always.

    _process_valid_dataloader(*, dataset_object, strict_batch_size=False, extra_args=None): Return None if valid_size == 0.0. Otherwise delegate to _process_test_dataloader.

    _get_custom_collate_fn(desired_batch_size=None): Return partial(custom_collate_fn, desired_batch_size=desired_batch_size or self.batch_size).

    Properties: name (-> _dataset_name), sequence_length (-> _seq_len), num_features (-> _num_features), train_data_samples, test_data_samples, valid_data_samples.

    Add __all__ = ['BaseTimeSeriesDataModule'] at module level.
  </action>
  <verify>
    <automated>python -c "from tscollection.datasets.modules.classes.base import BaseTimeSeriesDataModule; import lightning.pytorch as pl; assert issubclass(BaseTimeSeriesDataModule, pl.LightningDataModule)"</automated>
    <automated>grep -c "def sequence_length" src/tscollection/datasets/modules/classes/base.py; grep -c "def num_features" src/tscollection/datasets/modules/classes/base.py</automated>
    <automated>grep -c "ScalingMethod" src/tscollection/datasets/modules/classes/base.py</automated>
    <automated>grep -c "persistent_workers" src/tscollection/datasets/modules/classes/base.py</automated>
  </verify>
  <done>
    BaseTimeSeriesDataModule class exists in modules/classes/base.py with correct constructor (ScalingMethod enum, DataForm enum, Path-only types), sequence_length/num_features properties, setup() with create_data_scaler, _process_train/test/valid_dataloader with persistent_workers guard, and __all__ export.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → Module constructor | File paths, scaling params, batch sizes — trusted user, validate types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | ClassificationSplittingStrategy enum | Accept | Renamed enum (D-04) is internal-only; no external trust boundary. User controls input directly. |
| T-04-01-02 | Tampering | separate_target_feature_from_df | Mitigate | Validate target_feature_name exists in df.columns; raise KeyError with descriptive message if missing. |
| T-04-01-03 | Information Disclosure | BaseTimeSeriesDataModule properties | Mitigate | Properties return internal state only; no sensitive data exposed. Read-only @property decorators prevent external mutation. |
</threat_model>

<verification>
- ClassificationSplittingStrategy importable from tscollection.datasets.enums (not SplittingStrategy)
- separate_target_feature_from_df importable from tscollection.datasets.utils
- BaseTimeSeriesDataModule inherits LightningDataModule + ABC
- setup() uses create_data_scaler with enum-wired parameters
- persistent_workers guarded behind num_workers > 0
</verification>

<success_criteria>
- ClassificationSplittingStrategy enum exists with AS_DEFINED and MANUAL values
- separate_target_feature_from_df returns (DataFrame, Series) from (DataFrame, column_name)
- BaseTimeSeriesDataModule has sequence_length, num_features properties
- _process_train_dataloader returns DataLoader with correct shuffle/collate/wokers config
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-modules/04-01-SUMMARY.md`
</output>
