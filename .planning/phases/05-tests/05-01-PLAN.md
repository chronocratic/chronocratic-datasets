---
phase: 05-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_modules_forecasting.py
autonomous: true
requirements: [TST-01, TST-02]
user_setup: []

must_haves:
  truths:
    - "ETTDataModule with CSV fixture completes prepare_data() without error"
    - "ETTDataModule setup('fit') produces non-None _train_data_samples, _valid_data_samples, _test_data_samples"
    - "train_dataloader() returns DataLoader with batches of shape (1, seq_len, features)"
    - "setup() with numpy _full_data (no DatetimeIndex) produces num_time_series_features == 0"
    - "setup() with ScalingMethod.STANDARD uses StandardScaler"
  artifacts:
    - path: "tests/test_modules_forecasting.py"
      provides: "ETT integration tests, setup edge cases"
      contains: "class TestETTCsvFixture"
  key_links:
    - from: "tests/test_modules_forecasting.py"
      to: "src/tscollection/datasets/modules/ett.py"
      via: "ETTDataModule instance with tmp_path CSV"
      pattern: "ETTDataModule\\("
    - from: "tests/test_modules_forecasting.py"
      to: "src/tscollection/datasets/modules/classes/forecasting.py"
      via: "setup('fit') call"
      pattern: "\\.setup\\(stage='fit'\\)"
---

## Objective

Add ETT golden-path integration tests (D-01) and setup() edge-case unit tests (D-02) to `test_modules_forecasting.py`, covering the full forecasting pipeline: `prepare_data() -> setup('fit') -> train_dataloader()`.

## Purpose

The dominant coverage gap is `forecasting.py` at 49% — specifically the `setup()` body (lines 146-193) which exercises sklearn scaling, time feature extraction, data transformation, and train/valid/test splitting. Integration tests on ETTDataModule alone cover all of these in one shot.

## Context

@.planning/ROADMAP.md
@.planning/phases/05-tests/05-CONTEXT.md
@.planning/phases/05-tests/05-RESEARCH.md

### Source Files Under Test

@src/tscollection/datasets/modules/ett.py
@src/tscollection/datasets/modules/classes/forecasting.py
@src/tscollection/datasets/modules/classes/base.py

### Existing Test Patterns

@tests/test_modules_forecasting.py
@tests/conftest.py

### Key Interfaces

From `src/tscollection/datasets/modules/ett.py`:
- `ETTDataModule.__init__(*, dataset_file_path: Path, variant: str, seq_len: int = 128, mode: ForecastingMode, batch_size: int = 32, ...)`
- `ETTDataModule.prepare_data()` — reads CSV with `parse_dates=True, index_col='date'`; selects `['OT']` for univariate
- `ETTDataModule.train_dataloader(*, mode, shuffle, strict_batch_size, extra_args) -> DataLoader`

From `src/tscollection/datasets/modules/classes/forecasting.py`:
- `BaseForecastingTimeSeriesDataModule.setup(stage: str)` — scaling, time features, split
- `_prepare_data_scaler() -> MinMaxScaler | StandardScaler` — enum-wired (ScalingMethod)
- Properties: `num_time_series_features`, `num_features`, `train_data_samples`

From `src/tscollection/datasets/enums/data.py`:
- `ScalingMethod.MINMAX`, `ScalingMethod.STANDARD`
- `ForecastingMode.UNIVARIATE`, `ForecastingMode.MULTIVARIATE`

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: ETT golden-path integration tests (univariate + multivariate)</name>
  <files>tests/test_modules_forecasting.py</files>

  <read_first>
    - tests/test_modules_forecasting.py — existing file; add new test classes after TestForecastingSlices (line 386+)
    - src/tscollection/datasets/modules/ett.py — ETTDataModule constructor, prepare_data(), train_dataloader()
    - src/tscollection/datasets/modules/classes/forecasting.py — setup() body (lines 135-193)
    - src/tscollection/datasets/modules/classes/base.py — _process_train_dataloader(), dataloader args
    - tests/conftest.py — existing fixtures for reference
  </read_first>

  <behavior>
    - Test 1: ETT univariate golden path — CSV with 500 rows, DatetimeIndex, columns ['date', 'HUFL', 'HT', 'OT', 'Wsp'], variant='ETTh1', mode=UNIVARIATE. After prepare_data() + setup('fit'): _train_data_samples is not None, _valid_data_samples is not None, _test_data_samples is not None, num_features is not None, num_time_series_features > 0.
    - Test 2: ETT multivariate golden path — same CSV, variant='ETTh1', mode=MULTIVARIATE. Same assertions plus num_features equals total columns + time features.
    - Test 3: ETT train_dataloader returns DataLoader — batch from iter(train_dl) has shape (1, seq_len, features) or (batch_size, 1, features) depending on TensorDataset wrapping.
    - Test 4: ETT variant ETTm1 golden path — same as Test 1 but variant='ETTm1' to exercise 15-min slice boundaries.
  </behavior>

  <action>
  Add `TestETTGoldenPathIntegration` class to `tests/test_modules_forecasting.py` (after the last existing class). Create a `ett_csv_file` fixture using `tmp_path` that writes a CSV with 500+ rows and DatetimeIndex: columns 'date' (pd.date_range with freq='h'), 'HUFL', 'HT', 'OT', 'Wsp' (np.random.randn values). Write via `df.to_csv(csv_file, index=False)` per D-07.

  Implement 4 tests:

  1. `test_ett_univariate_golden_path(ett_csv_file)` — Instantiate `ETTDataModule(dataset_file_path=ett_csv_file, variant='ETTh1', seq_len=96, batch_size=16, mode=ForecastingMode.UNIVARIATE, scale_data=True, data_scaling_method=ScalingMethod.MINMAX)`. Call `module.prepare_data()` then `module.setup(stage='fit')`. Assert `module._train_data_samples is not None`, `module._valid_data_samples is not None`, `module._test_data_samples is not None`. Assert `module.num_features is not None` and `module.num_time_series_features > 0` (per D-01: exercises time feature extraction).

  2. `test_ett_multivariate_golden_path(ett_csv_file)` — Same as above but `mode=ForecastingMode.MULTIVARIATE`. Assert `_train_data_samples.shape[-1] > 1` (more than OT column). Assert `module.num_features` reflects all columns.

  3. `test_ett_train_dataloader_returns_batches(ett_csv_file)` — After prepare_data() + setup('fit') on univariate module, call `train_dl = module.train_dataloader()`. Assert `isinstance(train_dl, DataLoader)`. Extract batch: `batch = next(iter(train_dl))`. Assert `batch.shape[-1] == module.num_features` and batch has expected dimensions (TensorDataset wraps 1D tensor, so batch is single tensor of shape `(batch_size, features)` after expand_dims produces `(1, N, features)` then sliced).

  4. `test_ett_15min_variant_golden_path(ett_csv_file)` — Same as Test 1 with `variant='ETTm1'`. Verifies 15-min slice boundaries (4x multiplier in `_set_data_slices`) work with synthetic data.

  Per D-08, add a `synthetic_forecasting_csv` fixture to the same class — DataFrame with DatetimeIndex and 2-3 feature columns.

  Verify with: `uv run pytest tests/test_modules_forecasting.py -k "GoldenPath" -x`
  </action>

  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -k "GoldenPath" -x --tb=short</automated>
  </verify>

  <done>
  4 integration tests in TestETTGoldenPathIntegration class pass. `uv run pytest tests/test_modules_forecasting.py -k "GoldenPath"` exits 0. Coverage on forecasting.py rises from 49% (lines 146-193 now hit), ett.py rises from 67% (prepare_data, _transform_data, dataloaders exercised).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forecasting setup() edge-case unit tests</name>
  <files>tests/test_modules_forecasting.py</files>

  <read_first>
    - tests/test_modules_forecasting.py — file modified by Task 1; add after Task 1 classes
    - src/tscollection/datasets/modules/classes/forecasting.py — setup() body (lines 146-193), _prepare_data_scaler(), _calculate_num_features(), _split_data()
    - src/tscollection/datasets/modules/ett.py — ETTDataModule for instantiation
    - tests/test_modules_classification_forecasting.py — ConcreteForecasting pattern (lines 149-157) for reference
  </read_first>

  <behavior>
    - Test 5: setup() with numpy _full_data (no DatetimeIndex) — pre-populate module._full_data = np.random.randn(100, 5).astype(np.float32), set slices, call setup(). Assert num_time_series_features == 0 (line 167 branch hit).
    - Test 6: setup() with ScalingMethod.STANDARD — construct module with data_scaling_method=ScalingMethod.STANDARD. After setup(), verify scaler is StandardScaler. Assert _train_data_samples is not None.
    - Test 7: setup() with scale_data=False — construct module with scale_data=False. Verify setup() still runs without error (D-02). Note: research open question whether scale_data=False actually skips scaling in forecasting setup() — test current behavior per RESEARCH.md Open Question 1.
  </behavior>

  <action>
  Add `TestForecastingSetupEdgeCases` class to `tests/test_modules_forecasting.py`.

  Implement 3 tests:

  1. `test_setup_numpy_full_data_skips_time_features()` — Create ETTDataModule with dummy path, variant='ETTh1', seq_len=96. Pre-populate `module._full_data = np.random.randn(100, 5).astype(np.float32)` (per D-02: pure numpy, no DatetimeIndex). Set `module._train_slice = slice(None, 60)`, `module._valid_slice = slice(60, 80)`, `module._test_slice = slice(80, None)`. Call `module.setup(stage='fit')`. Assert `module.num_time_series_features == 0`. Assert `module._train_data_samples is not None`. Assert `module._train_data_samples.shape == (1, 60, 5)` (expand_dims adds axis 0, then sliced).

  2. `test_setup_standard_scaling()` — Create ETTDataModule with dummy path, variant='ETTh1', seq_len=96, `data_scaling_method=ScalingMethod.STANDARD`. Pre-populate `module._full_data` with numpy array (100, 5). Set slices. Call `module.setup(stage='fit')`. Assert `module._train_data_samples is not None`. Assert `module._num_time_series_features == 0` (no DatetimeIndex with numpy). Verifies line 214 branch (StandardScaler path).

  3. `test_setup_scale_data_false()` — Create ETTDataModule with dummy path, variant='ETTh1', seq_len=96, `scale_data=False`. Pre-populate `module._full_data` with numpy array (100, 5). Set slices. Call `module.setup(stage='fit')`. Assert no crash. Per RESEARCH.md Open Question 1: if scale_data=False is honored, data should be unscaled; if not, data will be scaled anyway — test current behavior either way. The key assertion is setup() completes without error and _train_data_samples is set.

  Verify with: `uv run pytest tests/test_modules_forecasting.py -k "SetupEdgeCases" -x`
  </action>

  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -k "SetupEdgeCases" -x --tb=short</automated>
  </verify>

  <done>
  3 edge-case tests in TestForecastingSetupEdgeCases class pass. `uv run pytest tests/test_modules_forecasting.py -k "SetupEdgeCases"` exits 0. Lines 165-167 (no-DatetimeIndex branch), 214-215 (StandardScaler branch), and scale_data=False path in setup() are covered. forecasting.py coverage rises toward 85%+ target.
  </done>
</task>

## Verification

```bash
# Run new tests only
uv run pytest tests/test_modules_forecasting.py -k "GoldenPath or SetupEdgeCases" -x --tb=short

# Run full test suite with coverage
uv run pytest --cov=tscollection.datasets --cov-report=term-missing
```

## Success Criteria

- All 163 existing tests pass (no regressions).
- 7 new tests pass (4 golden-path integration + 3 setup edge-cases).
- `forecasting.py` coverage rises from 49% to 85%+ (setup() body lines 146-193 now exercised).
- `ett.py` coverage rises from 67% to 85%+ (prepare_data, _transform_data, dataloaders exercised).
- No test below 85% coverage for modules touched.

## Threat Model

Not applicable — this phase adds tests only. No authentication, session management, access control, or cryptography concerns. Test fixtures use random synthetic data (no credentials, no PII).

<output>
After completion, create `.planning/phases/05-tests/05-01-SUMMARY.md`
</output>
