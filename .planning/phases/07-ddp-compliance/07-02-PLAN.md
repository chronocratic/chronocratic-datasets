---
phase: 07
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
autonomous: true
requirements: [MOD-01]
user_setup: []

must_haves:
  truths:
    - "synthetic_cache_dir fixture returns Path with npz file containing 'data' and 'index' arrays"
    - "synthetic_cache_dir fixture returns Path with valid metadata.json (version=1)"
    - "synthetic_cache_dir fixture returns Path with saved scaler.pt file"
  artifacts:
    - path: "tests/conftest.py"
      provides: "synthetic_cache_dir pytest fixture"
      contains: "def synthetic_cache_dir"
  key_links:
    - from: "tests/conftest.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "fixture imports cache helpers to populate files"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Add `synthetic_cache_dir` fixture to `tests/conftest.py`. The fixture creates a temp directory with pre-populated cache files (npz with raw data + index, metadata.json with version 1, scaler.pt) so downstream tests can exercise `setup()` and `prepare_dimensions()` without calling `prepare_data()`.

Purpose: Enable cache-based tests in Wave 2+ without requiring real CSV/ARFF files or running `prepare_data()`.

Output: New `synthetic_cache_dir` fixture in `tests/conftest.py` with automated test confirming fixture usability.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/conftest.py
@src/tscollection/datasets/utils/cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add synthetic_cache_dir fixture to conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py — current fixture patterns, import structure
    - src/tscollection/datasets/utils/cache.py — cache helper signatures for use in fixture
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-08 metadata.json schema fields
  </read_first>
  <action>
    Add `synthetic_cache_dir` fixture to tests/conftest.py using the tmp_path fixture. The fixture must:

    1. Import cache helpers from `tscollection.datasets.utils.cache` (resolve_cache_dir, atomic_save_npz, atomic_save_metadata, save_scaler).
    2. Create synthetic data: np.random.default_rng(42).standard_normal((500, 7)).astype(np.float32) — shape (500 timesteps, 7 features), ETTh1-style.
    3. Create synthetic time index: pd.date_range('2016-01-01', periods=500, freq='h'), serialized as int64 nanoseconds via `.index.astype(np.int64).to_numpy()`.
    4. Write npz file via atomic_save_npz with keys 'data' and 'index'.
    5. Write metadata.json via atomic_save_metadata with schema per D-08: version=1, dataset_name="SyntheticETT", n_features=7, seq_len=128, splits={"train": [0, 300], "valid": [300, 400], "test": [400, 500]}, has_datetime_index=true, data_scaling_method="MINMAX", data_scaling_range=[0, 1].
    6. Fit MinMaxScaler(feature_range=(0, 1)) on data, save via save_scaler to `<cache_key>_data_scaler.pt`.
    7. Return tmp_path.

    Follow existing fixture patterns: docstring specifying shape/dtype, np.random.default_rng() for reproducibility.
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py -x --tb=short -k "cache" or: uv run python -c "from pathlib import Path; import sys; sys.path.insert(0, 'tests'); from conftest import *; print('imports OK')"</automated>
  </verify>
  <done>
    synthetic_cache_dir fixture exists, is importable, and returns a Path with npz + metadata.json + scaler.pt files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test verifying synthetic_cache_dir fixture produces valid cache</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py — current state after Task 1
    - tests/test_cache.py — test patterns for cache validation
  </read_first>
  <action>
    Add a standalone test function `test_synthetic_cache_dir_fixture()` to tests/conftest.py that uses the synthetic_cache_dir fixture and verifies:

    1. The returned path contains at least 3 files (npz, json, pt).
    2. npz file loads successfully via np.load() with 'data' key having shape (500, 7) and dtype float32.
    3. npz file has 'index' key with shape (500,) and dtype int64.
    4. metadata.json loads via load_metadata() and has version=1, dataset_name="SyntheticETT".
    5. scaler.pt loads via load_scaler() and is a MinMaxScaler instance.

    Use the fixture parameter directly: `def test_synthetic_cache_dir_fixture(synthetic_cache_dir: Path) -> None:`.
  </action>
  <verify>
    <automated>uv run pytest tests/conftest.py::test_synthetic_cache_dir_fixture -x --tb=short</automated>
  </verify>
  <done>
    Test passes and verifies all cache files have correct content and structure.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/conftest.py -x --tb=short` passes
- `uv run ruff check tests/conftest.py` reports 0 issues
- Full suite: `uv run pytest tests/test_cache.py tests/conftest.py -x`
</verification>

<success_criteria>
- synthetic_cache_dir fixture returns Path with valid npz (data + index), metadata.json (version=1), and scaler.pt
- test_synthetic_cache_dir_fixture passes verifying all file contents
- ruff check clean on conftest.py
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-02-SUMMARY.md` when done
</output>
