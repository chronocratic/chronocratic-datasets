---
phase: 07
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/tscollection/datasets/modules/_base/base.py
  - tests/test_modules_base.py
autonomous: true
requirements: [MOD-01, MOD-05]
user_setup: []

must_haves:
  truths:
    - "BaseTimeSeriesDataModule.__init__ accepts cache_dir: Path | None = None"
    - "BaseTimeSeriesDataModule has prepare_data_per_node: bool = True class attribute"
    - "prepare_dimensions() reads metadata.json via load_metadata() when _num_features is None"
    - "prepare_dimensions() raises FileNotFoundError when metadata.json missing"
    - "reset() clears _full_data_raw, _time_index, _full_data_scaled, scaler caches, data samples"
  artifacts:
    - path: "src/tscollection/datasets/modules/_base/base.py"
      provides: "Base module with cache_dir param, prepare_data_per_node, metadata-based prepare_dimensions, extended reset"
      contains: "cache_dir: Path | None"
  key_links:
    - from: "src/tscollection/datasets/modules/_base/base.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import load_metadata, resolve_cache_dir"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Refactor `BaseTimeSeriesDataModule` (grandparent base) to add cache infrastructure: `cache_dir` constructor param (D-02), `prepare_data_per_node = True` class attribute (D-05), replace `prepare_dimensions()` interim with metadata.json read (D-07), and extend `reset()` to clear new cache-related attrs (Pitfall 6).

Purpose: Establish the cache layer foundation in the base class so all subclasses inherit DDP-safe behavior.

Output: Modified `base.py` with tests in `test_modules_base.py`.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/_base/base.py
@src/tscollection/datasets/utils/cache.py
@tests/test_modules_base.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cache_dir param, prepare_data_per_node, new typed attrs, extended reset to base.py</name>
  <files>src/tscollection/datasets/modules/_base/base.py</files>
  <read_first>
    - src/tscollection/datasets/modules/_base/base.py — current constructor signature, __init__ body, reset() method
    - src/tscollection/datasets/utils/cache.py — resolve_cache_dir signature
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-02, D-05 locked decisions
  </read_first>
  <behavior>
    - Test 1: Constructor accepts cache_dir=None, resolves to default ~/.cache/tsdatasets/<name>
    - Test 2: Constructor accepts cache_dir=custom_path, stores resolved path
    - Test 3: Class has prepare_data_per_node = True
    - Test 4: Instance has _full_data_raw = None, _time_index = None, _full_data_scaled = None after init
    - Test 5: reset() clears all new attrs (_full_data_raw, _time_index, _full_data_scaled, scaler caches, data samples)
  </behavior>
  <action>
    Modify src/tscollection/datasets/modules/_base/base.py:

    1. Add `from tscollection.datasets.utils.cache import resolve_cache_dir` import.
    2. Add `prepare_data_per_node: bool = True` class attribute after docstring, before __init__ (per D-05).
    3. Add `cache_dir: Path | None = None` parameter to __init__ after `data_form` parameter (per D-02). Store resolved path in `self._resolved_cache_dir: Path` using `resolve_cache_dir(cache_dir=cache_dir, dataset_name=self._dataset_name or 'default')`. Note: dataset_name may be None at init time; resolve lazily or use a placeholder that gets updated when _dataset_name is set.
    4. Initialize new typed attrs in __init__: `self._full_data_raw: np.ndarray | None = None`, `self._time_index: pd.DatetimeIndex | None = None`, `self._full_data_scaled: np.ndarray | None = None` (per D-01). These replace the old `_full_data` concept but the old attr still exists on subclasses until Wave 3.
    5. Add `_cache_key: str | None = None` attr (set by subclasses in __init__).
    6. Extend `reset()` to clear: `_full_data_raw`, `_time_index`, `_full_data_scaled`, `_data_scaler_cache`, `_ts_feature_scaler_cache`, `_train_data_samples`, `_valid_data_samples`, `_test_data_samples` (Pitfall 6). Keep `_setup_completed_stages.clear()` and `_prepare_data_called = False`.

    IMPORTANT: The `_resolved_cache_dir` must be recomputed when `_dataset_name` changes (set by _do_prepare_data). Add a property or lazy resolution method. Use a `_get_cache_dir()` helper that resolves using current `_dataset_name`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_base.py -x --tb=short -k "cache_dir or prepare_data_per_node or reset"</automated>
  </verify>
  <done>
    Constructor accepts cache_dir, class has prepare_data_per_node=True, reset() clears all new attrs, _full_data_raw/_time_index/_full_data_scaled initialized to None.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace prepare_dimensions() with metadata.json read (D-07) and add tests</name>
  <files>src/tscollection/datasets/modules/_base/base.py, tests/test_modules_base.py</files>
  <read_first>
    - src/tscollection/datasets/modules/_base/base.py — current prepare_dimensions() and _compute_dimensions()
    - src/tscollection/datasets/utils/cache.py — load_metadata signature
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-07, D-08 locked decisions
    - tests/test_modules_base.py — TestPrepareDimensionsCache patterns
  </read_first>
  <behavior>
    - Test 1: prepare_dimensions() with _num_features already set returns cached (short-circuit)
    - Test 2: prepare_dimensions() with _num_features=None reads metadata.json via load_metadata
    - Test 3: prepare_dimensions() raises FileNotFoundError when metadata.json missing
    - Test 4: prepare_dimensions() raises ValueError when metadata version != 1
    - Test 5: prepare_dimensions() returns (n_features, seq_len) from metadata
  </behavior>
  <action>
    Modify src/tscollection/datasets/modules/_base/base.py:

    1. Add `from tscollection.datasets.utils.cache import load_metadata` import.
    2. Rewrite `prepare_dimensions()` (per D-07):
       - Short-circuit: if `self._num_features is not None`, return `(self._num_features, self._seq_len)` — unchanged.
       - NEW: Attempt to read metadata.json from cache dir. Compute metadata path as `_get_cache_dir() / f'{self._cache_key}_metadata.json'`.
       - Call `load_metadata(metadata_path)` which validates version=1.
       - Extract `n_features = meta['n_features']`, `seq_len = meta['seq_len']`.
       - Cache: `self._num_features = n_features`.
       - Return `(n_features, seq_len)`.
       - If FileNotFoundError from load_metadata → re-raise with message "Cache metadata not found. Call prepare_data() first or delete stale cache."
    3. Keep `_compute_dimensions()` as a no-op that calls prepare_dimensions logic (subclasses may still override).

    Add tests in tests/test_modules_base.py:
    - `TestPrepareDimensionsCache` class with tests for short-circuit, metadata read, missing metadata, version mismatch.
    - Use `patch('tscollection.datasets.modules._base.base.load_metadata')` for mocking.
    - Use synthetic_cache_dir fixture for integration-style test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_base.py::TestPrepareDimensionsCache -x --tb=short</automated>
  </verify>
  <done>
    prepare_dimensions() reads metadata.json when _num_features is None, raises clear errors for missing/invalid metadata. Tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_modules_base.py -x --tb=short`
- `uv run ruff check src/tscollection/datasets/modules/_base/base.py`
</verification>

<success_criteria>
- BaseTimeSeriesDataModule accepts cache_dir param (D-02), has prepare_data_per_node=True (D-05)
- New typed attrs (_full_data_raw, _time_index, _full_data_scaled) initialized to None
- reset() clears all cache-related attrs (Pitfall 6)
- prepare_dimensions() reads metadata.json (D-07), raises on missing/invalid (D-07)
- All tests pass, ruff clean
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-03-SUMMARY.md` when done
</output>
