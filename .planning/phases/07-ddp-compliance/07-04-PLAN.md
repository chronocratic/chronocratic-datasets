---
phase: 07
plan: 04
type: execute
wave: 3
depends_on: [07-03]
files_modified:
  - src/tscollection/datasets/modules/_base/forecasting.py
  - tests/test_modules_classification_forecasting.py
autonomous: true
requirements: [MOD-01, MOD-05]
user_setup: []

must_haves:
  truths:
    - "BaseForecastingTimeSeriesDataModule has _full_data_raw, _time_index, _full_data_scaled (not _full_data)"
    - "setup() loads npz from cache via np.load(), sets _full_data_raw and _time_index"
    - "setup() calls _set_data_slices() after cache read (Pitfall 5 fix)"
    - "setup() fits scaler on train slice of _full_data_raw, saves via save_scaler()"
    - "setup() rebuilds _full_data_scaled from _full_data_raw each call (idempotent)"
    - "No isinstance(self._full_data, pd.DataFrame) branches remain in forecasting.py"
    - "_split_data() uses _full_data_scaled instead of _full_data"
    - "_finalize_prepare_data() is no-op for forecasting (slice computation moved to setup)"
    - "test/predict stage loads cached scaler via load_scaler() instead of refitting"
  artifacts:
    - path: "src/tscollection/datasets/modules/_base/forecasting.py"
      provides: "DDP-safe forecasting base with cache-read setup, typed attrs, scaler persistence"
      contains: "_full_data_raw"
  key_links:
    - from: "src/tscollection/datasets/modules/_base/forecasting.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import load_metadata, save_scaler, load_scaler"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Rewrite `BaseForecastingTimeSeriesDataModule` to split `_full_data` into typed attrs (D-01), make `setup()` DDP-safe by reading from cache, implement scaler persistence via torch.save/load (D-06), and move `_set_data_slices()` from `_finalize_prepare_data()` to `setup()` (Pitfall 5).

Purpose: Eliminate all 8 `isinstance` branches in forecasting.py and make setup() work for every rank reading the same cache files written by rank-0.

Output: Modified `forecasting.py` with updated tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/_base/forecasting.py
@src/tscollection/datasets/modules/_base/base.py
@src/tscollection/datasets/utils/cache.py
@tests/test_modules_classification_forecasting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split _full_data into typed attrs, add cache-aware helpers, remove isinstance branches</name>
  <files>src/tscollection/datasets/modules/_base/forecasting.py</files>
  <read_first>
    - src/tscollection/datasets/modules/_base/forecasting.py — full current implementation
    - src/tscollection/datasets/modules/_base/base.py — parent class attrs inherited (cache_dir, _resolved_cache_dir, prepare_data_per_node)
    - src/tscollection/datasets/utils/cache.py — load_metadata, save_scaler, load_scaler signatures
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-01, D-04, D-06, D-08 locked decisions
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — forecasting.py pattern assignments (constructor, setup, _compute_dimensions, _finalize_prepare_data, _prepare_data_scaler, _split_data)
  </read_first>
  <action>
    Modify src/tscollection/datasets/modules/_base/forecasting.py:

    **Constructor changes** (lines 51-83):
    1. Remove `self._full_data: np.ndarray | pd.DataFrame | None = None` (line 80).
    2. Add three typed attrs (per D-01): `self._full_data_raw: np.ndarray | None = None`, `self._time_index: pd.DatetimeIndex | None = None`, `self._full_data_scaled: np.ndarray | None = None`. Keep existing `_data_scaler_cache`, `_ts_feature_scaler_cache`.
    3. Add `_cache_key: str | None = None` (already on base). Keep as override if needed for typing clarity.

    **Remove _compute_dimensions override** (lines 134-154):
    - Delete the entire `_compute_dimensions()` method. The base class now reads metadata.json (D-07). No isinstance branch needed.

    **Make _finalize_prepare_data a no-op** (lines 340-347):
    - Replace body with `return` (per Pitfall 5 — slice computation moved to setup()).

    **Add cache I/O helpers**:
    - `_resolve_cache_dir() -> Path`: calls `resolve_cache_dir(cache_dir=self._cache_dir, dataset_name=self._dataset_name or 'default')`. Returns absolute path.
    - `_save_scaler_to_cache(scaler, kind: str) -> None`: saves fitted scaler to `<cache_dir>/<cache_key>_{kind}_scaler.pt` via `save_scaler()` (per D-06).
    - `_load_scaler_from_cache(kind: str) -> MinMaxScaler | StandardScaler | None`: loads scaler from path via `load_scaler()`. Returns None if file missing.

    **Remove isinstance branches from setup()** (lines 206-211):
    - Replace `if isinstance(self._full_data, pd.DataFrame):` with direct use of typed attrs: `time_index = self._time_index`, `full_array = self._full_data_raw`.

    **Update _calculate_num_features** (line 318):
    - Use `self._full_data_scaled` instead of `self._full_data`.

    **Update _split_data** (lines 321-338):
    - Use `self._full_data_scaled` instead of `self._full_data`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_classification_forecasting.py -x --tb=short -k "Forecasting" 2>&1 | head -40</automated>
  </verify>
  <done>
    No `_full_data` attr on forecasting base. Three typed attrs exist. _compute_dimensions removed. _finalize_prepare_data is no-op. Cache helpers added. No isinstance(_full_data, pd.DataFrame) in forecasting.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite setup() to be DDP-safe (cache read + scaler persistence)</name>
  <files>src/tscollection/datasets/modules/_base/forecasting.py, tests/test_modules_classification_forecasting.py</files>
  <read_first>
    - src/tscollection/datasets/modules/_base/forecasting.py — current setup() after Task 1 changes
    - src/tscollection/datasets/utils/cache.py — atomic_save_npz, load_metadata signatures
    - .planning/phases/07-ddp-compliance/07-RESEARCH.md — Example B (setup with split attrs), Pattern 3 (scaler persistence)
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — forecasting.py setup pattern
    - tests/test_modules_classification_forecasting.py — existing TestForecastingStageGating patterns
  </read_first>
  <action>
    Rewrite setup() in forecasting.py to follow the DDP-safe pattern (per RESEARCH.md Example B):

    **Stage validation + idempotency guard**: Keep existing logic (lines 186-195).

    **Cache read path** (NEW — replaces assert self._full_data is not None):
    1. Load npz from cache: `cache_path = self._resolve_cache_dir() / f'{self._cache_key}.npz'`.
    2. `loaded = np.load(str(cache_path))`.
    3. `self._full_data_raw = loaded['data'].astype(np.float32)` (immutable after load, per D-01).
    4. If 'index' in loaded: `self._time_index = pd.DatetimeIndex(loaded['index'])`; else None (per DatetimeIndex round-trip pattern).
    5. Call `self._set_data_slices()` (moved from _finalize_prepare_data per Pitfall 5 — now safe because _full_data_raw is set).

    **Validate stage**: No-op, mark sentinel.

    **Fit stage** (stage in ('fit', None)):
    1. Fit data_scaler on `self._full_data_raw[self._train_slice]`.
    2. Cache: `self._data_scaler_cache = data_scaler`.
    3. Persist: `self._save_scaler_to_cache(data_scaler, 'data')` (per D-06).
    4. Transform: `self._full_data_scaled = data_scaler.transform(self._full_data_raw)`.
    5. Call `self._transform_data()` (subclass hook — operates on _full_data_scaled).
    6. Time features: if `self._time_index is not None`, extract, fit ts_scaler, persist, transform, concatenate to _full_data_scaled.
    7. `self._calculate_num_features()`, `self._split_data()`.

    **Test/predict stage**:
    1. Try loading cached scalers: `self._data_scaler_cache = self._load_scaler_from_cache('data')`.
    2. If scaler exists: transform _full_data_raw, call _transform_data(), handle time features, split.
    3. If no scaler (fit already ran): check `self._train_data_samples is not None` — if so, skip.
    4. Warn if no scaler available (keep existing warning pattern).

    **No scaling path** (scale_data=False):
    - Set `_full_data_scaled = self._full_data_raw.copy()`, call _transform_data(), _calculate_num_features(), _split_data().

    Update tests in test_modules_classification_forecasting.py:
    - Add TestFullDataSplit class: tests _full_data_raw immutability, _time_index persistence, _full_data_scaled rebuild.
    - Update existing stage-gating tests to use _full_data_raw injection (not _full_data).
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_classification_forecasting.py -x --tb=short -k "Forecasting or FullData"</automated>
  </verify>
  <done>
    setup() reads cache, sets typed attrs, fits/persists scalers (D-06), calls _set_data_slices() after cache read (Pitfall 5). No isinstance(_full_data) branches. Tests pass.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_modules_classification_forecasting.py -x --tb=short`
- `grep -rn "isinstance.*_full_data" src/tscollection/datasets/modules/_base/forecasting.py` returns 0 matches
- `uv run ruff check src/tscollection/datasets/modules/_base/forecasting.py`
</verification>

<success_criteria>
- No _full_data attr on forecasting base — replaced by _full_data_raw, _time_index, _full_data_scaled (D-01)
- setup() reads cache (npz + metadata), sets typed attrs, calls _set_data_slices() after read (Pitfall 5)
- Scaler persistence via torch.save/load (D-06) in _save_scaler_to_cache / _load_scaler_from_cache
- No isinstance(self._full_data, pd.DataFrame) branches in forecasting.py
- _compute_dimensions() removed (base reads metadata.json per D-07)
- _finalize_prepare_data() is no-op
- All tests pass, ruff clean, grep confirms zero isinstance branches
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-04-SUMMARY.md` when done
</output>
