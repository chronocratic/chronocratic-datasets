---
phase: 07
plan: 05
type: execute
wave: 4
depends_on: [07-04]
files_modified:
  - src/tscollection/datasets/modules/ett.py
  - tests/test_modules_forecasting.py
autonomous: true
requirements: [MOD-01, MOD-05, DDP-02]
user_setup: []

must_haves:
  truths:
    - "ETTDataModule._do_prepare_data() writes cache (npz + metadata.json) instead of self._full_data"
    - "ETTDataModule._transform_data() operates on _full_data_raw (no isinstance check)"
    - "ETTDataModule has no _full_data class attribute"
    - "ETTDataModule computes _cache_key in __init__ from variant, seq_len, mode, scaling params"
    - "ETT golden-path test (prepare_data -> setup -> train_dataloader) passes with cache"
  artifacts:
    - path: "src/tscollection/datasets/modules/ett.py"
      provides: "Cache-aware ETT module with typed attrs"
      contains: "_cache_key"
  key_links:
    - from: "src/tscollection/datasets/modules/ett.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import atomic_save_npz, atomic_save_metadata, build_cache_key"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Adapt `ETTDataModule` to the DDP-safe cache pattern: `_do_prepare_data()` writes npz + metadata.json to cache (instead of `self._full_data = df`), `_transform_data()` operates on typed `_full_data_raw` (no isinstance branch), and `_set_data_slices()` remains variant-based but is called from `setup()` now.

Purpose: Make ETT DDP-safe by following the cache-write-then-read pattern established in the base classes.

Output: Modified `ett.py` with updated integration tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/ett.py
@src/tscollection/datasets/modules/_base/forecasting.py
@src/tscollection/datasets/utils/cache.py
@tests/test_modules_forecasting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt ETTDataModule to cache pattern — _do_prepare_data writes cache, _transform_data uses typed attrs</name>
  <files>src/tscollection/datasets/modules/ett.py</files>
  <read_first>
    - src/tscollection/datasets/modules/ett.py — full current implementation
    - src/tscollection/datasets/modules/_base/forecasting.py — refactored base (after Plan 04)
    - src/tscollection/datasets/utils/cache.py — build_cache_key, atomic_save_npz, atomic_save_metadata signatures
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-03 (cache key params), D-04 (cache format), D-08 (metadata schema)
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — ett.py pattern assignments (constructor, _set_data_slices, _transform_data, _do_prepare_data)
  </read_first>
  <action>
    Modify src/tscollection/datasets/modules/ett.py:

    1. Remove `_full_data: pd.DataFrame | np.ndarray | None = None` class attribute (line 60).

    2. Add cache imports: `from tscollection.datasets.utils.cache import (atomic_save_npz, atomic_save_metadata, build_cache_key)`.

    3. In `__init__` (lines 62-95):
       - Compute `_cache_key` using `build_cache_key(dataset_name=variant, params={"seq_len": seq_len, "mode": mode.value, "data_scaling_method": data_scaling_method.value, "data_scaling_range": list(data_scaling_range)})` (per D-03, cache key params from RESEARCH.md open question 4).
       - Store `self._cache_key = cache_key`.
       - No change to parent constructor call pattern — just add cache key computation.

    4. Rewrite `_do_prepare_data()` (lines 131-150):
       - Keep CSV read logic unchanged (validate path, read CSV, parse_dates=True, index_col='date', univariate column selection).
       - Replace `self._full_data = df` with cache writes:
         a. `data = df.to_numpy().astype(np.float32)`
         b. `index_ns = df.index.astype(np.int64).to_numpy()`
         c. `cache_dir = self._resolve_cache_dir()` (inherited from base)
         d. `cache_path = cache_dir / f'{self._cache_key}.npz'`
         e. `cache_dir.mkdir(parents=True, exist_ok=True)`
         f. `atomic_save_npz(cache_path, data=data, index=index_ns)`
         g. Compute variant-based split ranges from the existing `_set_data_slices()` logic (D-08). Call `_set_data_slices()` to populate `self._train_slice`, `self._valid_slice`, `self._test_slice`, then derive ranges: `splits = {"train": [self._train_slice.start, self._train_slice.stop], "valid": [self._valid_slice.start, self._valid_slice.stop], "test": [self._test_slice.start, self._test_slice.stop]}`. Write metadata.json with these actual ranges per D-08: version=1, dataset_name=variant, n_features=data.shape[1], seq_len=self._seq_len, splits=splits, has_datetime_index=True, data_scaling_method, data_scaling_range.
         h. `atomic_save_metadata(cache_dir / f'{self._cache_key}_metadata.json', metadata)`
         i. Set `self._dataset_name = self.variant` (keep existing).
         j. Set `self._time_index = pd.DatetimeIndex(df.index)` (per D-01 — cache index for later use).

    5. Rewrite `_transform_data()` (lines 116-125):
       - Replace isinstance checks with typed operation: `self._full_data_scaled = np.expand_dims(self._full_data_raw, axis=0)`.
       - Remove assertion `self._full_data is not None`.

    6. Keep `_set_data_slices()` unchanged — variant-based (lines 101-114). No data-length dependency. Called from both `_do_prepare_data()` (to compute metadata splits) and from `setup()` (base handles this per Pitfall 5).

    7. Update dataloader methods — no change needed. They use `_train_data_samples` set by `_split_data()`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "ETT" 2>&1 | tail -5</automated>
  </verify>
  <done>
    ETTDataModule writes cache in _do_prepare_data(), operates on _full_data_raw in _transform_data(), no _full_data class attr, no isinstance branches. Metadata includes actual split ranges. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ETT integration tests for cache-based flow</name>
  <files>tests/test_modules_forecasting.py</files>
  <read_first>
    - tests/test_modules_forecasting.py — current ETT tests (TestETTGoldenPathIntegration, TestETTSetDataSlices, etc.)
    - src/tscollection/datasets/modules/ett.py — refactored module after Task 1
  </read_first>
  <action>
    Update tests/test_modules_forecasting.py for cache-based ETT:

    1. In `TestETTGoldenPathIntegration`:
       - Tests that use attribute injection (`module._full_data = rng.standard_normal(...)`) must switch to cache-based setup. Create cache files manually or use synthetic_cache_dir fixture.
       - Golden path test: `prepare_data()` writes cache, `setup('fit')` reads cache + scales + splits, verify `_train_data_samples` shape.

    2. Update `test_setup_numpy_full_data_skips_time_features` (lines 646-676):
       - Change to inject `_full_data_raw` and `_time_index = None` instead of `_full_data`.

    3. Add `TestETTCacheIntegration` class:
       - Test: prepare_data writes npz file to cache dir.
       - Test: prepare_data writes metadata.json with version=1 and actual split ranges.
       - Test: setup('fit') reads npz, sets _full_data_raw with correct shape.
       - Test: _transform_data produces _full_data_scaled with shape (1, samples, features).

    4. Update any test that references `_full_data` to use `_full_data_raw` or `_full_data_scaled` as appropriate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "ETT"</automated>
  </verify>
  <done>
    All ETT tests pass with cache-based flow. No tests reference `_full_data` on ETT module. Metadata split ranges are verified.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "ETT"`
- `grep -rn "isinstance.*_full_data" src/tscollection/datasets/modules/ett.py` returns 0
- `uv run ruff check src/tscollection/datasets/modules/ett.py`
</verification>

<success_criteria>
- ETTDataModule._do_prepare_data() writes npz + metadata.json cache (D-04), not self._full_data
- ETTDataModule._transform_data() uses _full_data_raw (no isinstance, per D-01)
- Cache key computed from variant, seq_len, mode, scaling params (D-03)
- metadata.json includes actual variant-based split ranges (D-08)
- No _full_data class attribute on ETTDataModule
- All ETT tests pass with cache-based flow
- ruff clean, zero isinstance(_full_data) in ett.py
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-05-SUMMARY.md` when done
</output>
