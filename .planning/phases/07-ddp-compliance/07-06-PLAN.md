---
phase: 07
plan: 06
type: execute
wave: 5
depends_on: [07-04, 07-05]
files_modified:
  - src/tscollection/datasets/modules/weather.py
  - src/tscollection/datasets/modules/electricity.py
  - tests/test_modules_forecasting.py
autonomous: true
requirements: [MOD-01, MOD-05, DDP-02]
user_setup: []

must_haves:
  truths:
    - "WeatherModule._do_prepare_data() writes cache (npz + metadata.json) instead of self._full_data"
    - "WeatherModule._transform_data() uses _full_data_raw (no isinstance check)"
    - "ElectricityLoadModule._do_prepare_data() writes cache (npz + metadata.json)"
    - "ElectricityLoadModule._transform_data() uses _full_data_raw (no isinstance check)"
    - "Weather and Electricity _set_data_slices() use len(_full_data_raw) — called from setup() after cache read"
    - "No _full_data class attributes on WeatherModule or ElectricityLoadModule"
  artifacts:
    - path: "src/tscollection/datasets/modules/weather.py"
      provides: "Cache-aware Weather module"
      contains: "_cache_key"
    - path: "src/tscollection/datasets/modules/electricity.py"
      provides: "Cache-aware Electricity module"
      contains: "_cache_key"
  key_links:
    - from: "src/tscollection/datasets/modules/weather.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import atomic_save_npz, atomic_save_metadata, build_cache_key"
      pattern: "from tscollection.datasets.utils.cache import"
    - from: "src/tscollection/datasets/modules/electricity.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import atomic_save_npz, atomic_save_metadata, build_cache_key"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Adapt WeatherModule and ElectricityLoadModule to the DDP-safe cache pattern. Both modules share the same structure: CSV read -> cache write in `_do_prepare_data()`, typed `_transform_data()` using `_full_data_raw`, and `_set_data_slices()` that depends on `len(_full_data_raw)` (moved to `setup()` per Pitfall 5).

Purpose: Make Weather and Electricity DDP-safe, following the same pattern as ETT.

Output: Modified `weather.py`, `electricity.py` with updated integration tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/weather.py
@src/tscollection/datasets/modules/electricity.py
@src/tscollection/datasets/modules/_base/forecasting.py
@src/tscollection/datasets/utils/cache.py
@tests/test_modules_forecasting.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt WeatherModule and ElectricityLoadModule to cache pattern</name>
  <files>src/tscollection/datasets/modules/weather.py, src/tscollection/datasets/modules/electricity.py</files>
  <read_first>
    - src/tscollection/datasets/modules/weather.py — full current implementation
    - src/tscollection/datasets/modules/electricity.py — full current implementation
    - src/tscollection/datasets/modules/ett.py — reference for cache pattern (after Plan 05)
    - src/tscollection/datasets/modules/_base/forecasting.py — refactored base (after Plan 04)
    - src/tscollection/datasets/utils/cache.py — cache helper signatures
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-03, D-04, D-08
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — weather.py and electricity.py pattern assignments
  </read_first>
  <action>
    Apply the same cache pattern to both WeatherModule and ElectricityLoadModule:

    **Both modules — common changes:**
    1. Remove `_full_data: pd.DataFrame | np.ndarray | None = None` class attribute.
    2. Add cache imports: `from tscollection.datasets.utils.cache import (atomic_save_npz, atomic_save_metadata, build_cache_key)`.
    3. Compute `_cache_key` in `__init__` using `build_cache_key(dataset_name=<name>, params={"seq_len": seq_len, "mode": mode.value, "data_scaling_method": data_scaling_method.value, "data_scaling_range": list(data_scaling_range)})`.
       - Weather: dataset_name = filename stem (derived in _do_prepare_data)
       - Electricity: dataset_name = "ElectricityLoad" (hardcoded)

    **WeatherModule specific** (weather.py):
    4. `_do_prepare_data()` (lines 110-127):
       - Keep CSV read logic (parse_dates=True, index_col='date', univariate last column).
       - Replace `self._full_data = df` with:
         a. `data = df.to_numpy().astype(np.float32)`
         b. `index_ns = df.index.astype(np.int64).to_numpy()`
         c. Write npz: `atomic_save_npz(cache_path, data=data, index=index_ns)`
         d. Compute splits from data length (60/20/20): `train_end = int(0.6 * len(data))`, `valid_end = int(0.8 * len(data))`, splits = `{"train": [0, train_end], "valid": [train_end, valid_end], "test": [valid_end, len(data)]}`.
         e. Set `self._dataset_name = self.dataset_file_path.name` (keep existing)
         f. Set `self._time_index = pd.DatetimeIndex(df.index)`
         g. Write metadata: `atomic_save_metadata(metadata_path, metadata_dict)` per D-08 with computed splits.

    5. `_set_data_slices()` (lines 86-92):
       - Change `len(self._full_data)` to `len(self._full_data_raw)` (per D-01).
       - Remove assertion `self._full_data is not None` — cache guarantees data exists.
       - Method signature unchanged; now called from `setup()` after cache read (per Pitfall 5, base handles timing).

    6. `_transform_data()` (lines 94-104):
       - Replace isinstance checks: `self._full_data_scaled = np.expand_dims(self._full_data_raw, axis=0)`.
       - Remove assertion `self._full_data is not None`.

    **ElectricityLoadModule specific** (electricity.py):
    7. `_do_prepare_data()` (lines 111-135):
       - Keep CSV read logic (semicolon sep, comma decimal, resample 1h, filter zeros, 2012: slice, univariate MT_001).
       - Replace `self._full_data = df` with same cache write pattern as Weather.
       - Note: `_dataset_name = 'ElectricityLoad'` (hardcoded, not from filename).
       - Compute splits from data length (60/20/20) same as Weather: `train_end = int(0.6 * len(data))`, `valid_end = int(0.8 * len(data))`.
       - Write metadata with computed splits to metadata.json.

    8. `_set_data_slices()` (lines 86-92):
       - Change `len(self._full_data)` to `len(self._full_data_raw)`.
       - Remove assertion.

    9. `_transform_data()` (lines 94-105):
       - Replace isinstance checks: `self._full_data_scaled = np.expand_dims(self._full_data_raw.T, axis=-1)`.
       - Note: Electricity transposes + expand_dims(axis=-1), different from Weather's expand_dims(axis=0).
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "Weather or Electricity"</automated>
  </verify>
  <done>
    WeatherModule and ElectricityLoadModule write cache in _do_prepare_data(), operate on _full_data_raw in _transform_data(), _set_data_slices uses len(_full_data_raw), no _full_data class attr, no isinstance branches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Weather and Electricity integration tests</name>
  <files>tests/test_modules_forecasting.py</files>
  <read_first>
    - tests/test_modules_forecasting.py — current Weather/Electricity tests
    - src/tscollection/datasets/modules/weather.py — refactored after Task 1
    - src/tscollection/datasets/modules/electricity.py — refactored after Task 1
  </read_first>
  <action>
    Update tests/test_modules_forecasting.py for cache-based Weather and Electricity:

    1. Update Weather tests:
       - Tests injecting `module._full_data` must switch to cache-based setup (create cache files or use synthetic_cache_dir).
       - Golden path: `prepare_data()` writes cache, `setup('fit')` reads + scales + splits.
       - Verify `_train_data_samples` shape after setup.

    2. Update Electricity tests:
       - Same pattern as Weather updates.
       - Note: Electricity CSV uses semicolon + comma decimal — tests must create matching synthetic files.
       - Verify transpose + expand_dims pattern produces correct shape.

    3. Add cache integration tests:
       - Weather/Electricity prepare_data writes npz + metadata.json.
       - setup('fit') reads cache, sets _full_data_raw, calls _set_data_slices.

    4. Update any test referencing `_full_data` to use `_full_data_raw` / `_full_data_scaled`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "Weather or Electricity"</automated>
  </verify>
  <done>
    All Weather and Electricity tests pass with cache-based flow. No tests reference _full_data on these modules.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_modules_forecasting.py -x --tb=short -k "Weather or Electricity"`
- `grep -rn "isinstance.*_full_data" src/tscollection/datasets/modules/weather.py src/tscollection/datasets/modules/electricity.py` returns 0
- `uv run ruff check src/tscollection/datasets/modules/weather.py src/tscollection/datasets/modules/electricity.py`
</verification>

<success_criteria>
- WeatherModule and ElectricityLoadModule write cache in _do_prepare_data() (D-04), not self._full_data
- _transform_data() uses _full_data_raw (no isinstance, per D-01)
- _set_data_slices() uses len(_full_data_raw) (Pitfall 5)
- Cache keys computed from seq_len, mode, scaling params (D-03)
- No _full_data class attributes
- All Weather/Electricity tests pass
- ruff clean, zero isinstance(_full_data) in both files
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-06-SUMMARY.md` when done
</output>
