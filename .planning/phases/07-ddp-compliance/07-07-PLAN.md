---
phase: 07
plan: 07
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - src/tscollection/datasets/modules/_base/classification.py
  - src/tscollection/datasets/modules/ucr.py
  - src/tscollection/datasets/modules/uea.py
  - tests/test_modules_ucr.py
  - tests/test_modules_uea.py
autonomous: true
requirements: [MOD-01]
user_setup: []

must_haves:
  truths:
    - "UCRClassificationDataModule._do_prepare_data() writes post-processed cache (npz + metadata.json)"
    - "UEAClassificationDataModule._do_prepare_data() writes post-processed cache (npz + metadata.json)"
    - "Classification setup() reads cached split arrays from npz"
    - "Classification _compute_dimensions() reads metadata.json via base prepare_dimensions()"
    - "UCR cache contains train/test/valid samples and labels"
    - "UEA cache contains 3-D train/test/valid samples and 1-D labels"
  artifacts:
    - path: "src/tscollection/datasets/modules/ucr.py"
      provides: "Cache-aware UCR classification module"
      contains: "_cache_key"
    - path: "src/tscollection/datasets/modules/uea.py"
      provides: "Cache-aware UEA classification module"
      contains: "_cache_key"
  key_links:
    - from: "src/tscollection/datasets/modules/ucr.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import atomic_save_npz, atomic_save_metadata, build_cache_key"
      pattern: "from tscollection.datasets.utils.cache import"
    - from: "src/tscollection/datasets/modules/uea.py"
      to: "src/tscollection/datasets/utils/cache.py"
      via: "import atomic_save_npz, atomic_save_metadata, build_cache_key"
      pattern: "from tscollection.datasets.utils.cache import"
---

<objective>
Adapt UCRClassificationDataModule and UEAClassificationDataModule to the DDP-safe cache pattern. Classification caches post-processed, already-split data (samples + labels per split) in npz, plus metadata.json with dims. `setup()` reads cached splits, applies scaler via base class logic.

Purpose: Make classification modules DDP-safe by writing processed data to cache in `prepare_data()` and reading in `setup()`.

Output: Modified `classification.py`, `ucr.py`, `uea.py` with updated tests.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/_base/classification.py
@src/tscollection/datasets/modules/ucr.py
@src/tscollection/datasets/modules/uea.py
@src/tscollection/datasets/utils/cache.py
@tests/test_modules_ucr.py
@tests/test_modules_uea.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt classification base — add setup() that reads cache</name>
  <files>src/tscollection/datasets/modules/_base/classification.py</files>
  <read_first>
    - src/tscollection/datasets/modules/_base/classification.py — full current implementation
    - src/tscollection/datasets/modules/_base/base.py — parent class (after Plan 03: cache_dir, prepare_dimensions from metadata)
    - src/tscollection/datasets/utils/cache.py — load_metadata, resolve_cache_dir signatures
    - .planning/phases/07-ddp-compliance/07-RESEARCH.md — Open Question 2 (classification cache pattern), Pitfall 7
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — classification.py pattern assignments
  </read_first>
  <action>
    Modify src/tscollection/datasets/modules/_base/classification.py:

    1. Add `_compute_dimensions()` override (per D-07):
       - Remove current implementation that checks `self._train_data_samples is None`.
       - New: delegate to base `prepare_dimensions()` which reads metadata.json. The base class handles the FileNotFoundError and ValueError cases.
       - If base returns (None, None), raise RuntimeError with clear message.

    2. Override `setup()` to add cache-read step BEFORE base scaling logic:
       - Stage validation and idempotency guard (keep existing base pattern).
       - NEW cache read: Load npz from cache dir. Keys depend on module (UCR uses DataFrame samples, UEA uses ndarray).
       - For the base, define the cache-read pattern as an abstract method `_load_cached_data()` that subclasses implement. The base `setup()` calls `_load_cached_data()` to populate `_train_data_samples`, `_train_data_labels`, etc., then proceeds with scaler logic from the grandparent base.
       - Actually, simpler approach: keep the base `setup()` from `BaseTimeSeriesDataModule` (which handles scaling). The classification setup does NOT need to override if the data is loaded in `_do_prepare_data()` and the base handles scaling. Wait — the issue is that under DDP, `_do_prepare_data()` runs on rank-0 only and writes to `self._train_data_samples` (in-memory). Ranks 1+ never see it.
       - CORRECT approach: Classification `_do_prepare_data()` writes cache (post-processed splits). Classification `setup()` reads cache into in-memory attrs, then applies scaling via base setup logic.
       - Add `_load_cached_data()` as abstract method (subclasses implement cache reading for their data format).
       - Override `setup(stage)` to: (a) call `_load_cached_data()`, (b) call `super().setup(stage)` for scaling.

    3. Add `_cache_key: str | None = None` attr.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_ucr.py tests/test_modules_uea.py -x --tb=short -k "setup" 2>&1 | tail -5</automated>
  </verify>
  <done>
    Classification base has _load_cached_data() abstract method, setup() reads cache then scales, _compute_dimensions() delegates to base metadata read.
  </done>
</task>

<task type="auto">
  <name>Task 2: Adapt UCR and UEA to cache pattern</name>
  <files>src/tscollection/datasets/modules/ucr.py, src/tscollection/datasets/modules/uea.py</files>
  <read_first>
    - src/tscollection/datasets/modules/ucr.py — full current implementation
    - src/tscollection/datasets/modules/uea.py — full current implementation
    - src/tscollection/datasets/modules/_base/classification.py — refactored base after Task 1
    - src/tscollection/datasets/utils/cache.py — atomic_save_npz, atomic_save_metadata, build_cache_key
    - .planning/phases/07-ddp-compliance/07-CONTEXT.md — D-03, D-04, D-08
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — ucr.py and uea.py pattern assignments
  </read_first>
  <action>
    **UCRClassificationDataModule** (ucr.py):

    1. Add cache imports: `from tscollection.datasets.utils.cache import (atomic_save_npz, atomic_save_metadata, build_cache_key)`.

    2. In `__init__`: Compute `_cache_key` using `build_cache_key(dataset_name=<folder_name>, params={"splitting_strategy": splitting_strategy.value, "test_size": test_size, "valid_size": valid_size, "data_scaling_method": data_scaling_method.value})`.

    3. Rewrite `_do_prepare_data()` (lines 151-240):
       - Keep ALL ARFF read + processing logic unchanged (read ARFF, clean, split, separate target, create validation split, variable-length processing).
       - After processing (line 240), instead of keeping data on `self`, write to cache:
         a. Serialize train_samples as numpy array: `train_samples.values` if DataFrame, else as-is.
         b. Serialize train_labels as numpy array: `train_labels.to_numpy()`.
         c. Same for test and valid splits.
         d. `atomic_save_npz(cache_path, train_samples=train_samples_arr, train_labels=train_labels_arr, test_samples=test_samples_arr, test_labels=test_labels_arr, valid_samples=valid_samples_arr if valid exists else np.empty((0,)), valid_labels=valid_labels_arr if valid exists else np.empty((0,)))`.
         e. Write metadata.json per D-08: version=1, dataset_name, n_features=1, seq_len, has_datetime_index=false, data_scaling_method, data_scaling_range.
         f. Still set `self._dataset_name`, `self._num_classes`, `self._seq_len`, `self._num_features` (needed for prepare_dimensions short-circuit).

    4. Implement `_load_cached_data()`:
       - Read npz file: `np.load(str(cache_path))`.
       - Reconstruct DataFrames/Series from arrays: `self._train_data_samples = pd.DataFrame(loaded['train_samples'])`, `self._train_data_labels = pd.Series(loaded['train_labels'], dtype='category')`.
       - Same for test and valid splits.

    **UEAClassificationDataModule** (uea.py):

    5. Add same cache imports.

    6. In `__init__`: Compute `_cache_key` (same param pattern as UCR).

    7. Rewrite `_do_prepare_data()` (lines 165-267):
       - Keep ALL ARFF read + processing logic unchanged (scipy arff.loadarff, _process_stacked_data, split, validation split, variable-length processing, label conversion).
       - After processing (line 267), write to cache:
         a. UEA data is 3-D ndarray: `(samples, timesteps, features)`. Save directly.
         b. Labels are pd.Series category — save as numpy.
         c. `atomic_save_npz(cache_path, train_samples=..., train_labels=..., test_samples=..., test_labels=..., valid_samples=..., valid_labels=...)`.
         d. Write metadata.json.

    8. Implement `_load_cached_data()`:
       - Read npz. Reconstruct 3-D arrays and label Series.
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_ucr.py tests/test_modules_uea.py -x --tb=short</automated>
  </verify>
  <done>
    UCR and UEA write cache in _do_prepare_data(), read cache in _load_cached_data(), setup() loads cache then scales. All classification tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update classification tests for cache-based flow</name>
  <files>tests/test_modules_ucr.py, tests/test_modules_uea.py</files>
  <read_first>
    - tests/test_modules_ucr.py — current tests
    - tests/test_modules_uea.py — current tests
    - src/tscollection/datasets/modules/ucr.py — refactored after Task 2
    - src/tscollection/datasets/modules/uea.py — refactored after Task 2
  </read_first>
  <action>
    Update classification tests:

    1. In test_modules_ucr.py:
       - Tests that inject `_train_data_samples` directly (bypassing prepare_data) may need to also inject cache files or adapt.
       - `test_setup_idempotent` — verify with cache-based flow.
       - Add cache round-trip test: prepare_data writes cache, setup reads cache, verify samples match.

    2. In test_modules_uea.py:
       - Same pattern as UCR updates.
       - Verify 3-D array preservation through cache round-trip.

    3. Ensure tests use mocking/patching for file I/O where needed (avoid actual ARFF reads).
  </action>
  <verify>
    <automated>uv run pytest tests/test_modules_ucr.py tests/test_modules_uea.py -x --tb=short</automated>
  </verify>
  <done>
    All UCR and UEA tests pass with cache-based flow.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_modules_ucr.py tests/test_modules_uea.py -x --tb=short`
- `uv run ruff check src/tscollection/datasets/modules/ucr.py src/tscollection/datasets/modules/uea.py src/tscollection/datasets/modules/_base/classification.py`
</verification>

<success_criteria>
- UCR._do_prepare_data() writes post-processed cache (npz + metadata.json) (D-04)
- UEA._do_prepare_data() writes post-processed cache (npz + metadata.json) (D-04)
- Classification setup() reads cache via _load_cached_data(), then applies scaling
- _compute_dimensions() reads metadata.json (D-07)
- All UCR/UEA tests pass
- ruff clean
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-07-SUMMARY.md` when done
</output>
