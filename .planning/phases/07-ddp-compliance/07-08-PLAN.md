---
phase: 07
plan: 08
type: execute
wave: 5
depends_on: [07-05, 07-06, 07-07]
files_modified:
  - tests/test_ddp_compliance.py
  - tests/test_modules_forecasting.py
autonomous: true
requirements: [MOD-01, MOD-05, DDP-01, DDP-04]
user_setup: []

must_haves:
  truths:
    - "DDP smoke test (gloo, 2 ranks): rank-0 writes cache, all ranks read identical state"
    - "isinstance(_full_data, pd.DataFrame) returns 0 matches across all module files"
    - "Second setup() call produces identical _train_data_samples (idempotent)"
    - "All existing tests still pass (full regression suite)"
    - "ruff check clean on all modified files"
  artifacts:
    - path: "tests/test_ddp_compliance.py"
      provides: "DDP smoke tests with mp.spawn, gloo backend, 2 ranks"
      min_lines: 80
  key_links:
    - from: "tests/test_ddp_compliance.py"
      to: "src/tscollection/datasets/modules/ett.py"
      via: "instantiates ETTDataModule, calls prepare_data + setup under DDP"
      pattern: "ETTDataModule.*prepare_data"
---

<objective>
Verify DDP compliance with multi-rank smoke tests, confirm isinstance branch elimination, and run full regression suite. This is the final gate for phase 7.

Purpose: Prove the cache layer works under real DDP conditions and no regressions were introduced.

Output: New `test_ddp_compliance.py` with passing DDP smoke test, full regression suite green.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/tsdatasets/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/tscollection/datasets/modules/ett.py
@src/tscollection/datasets/modules/weather.py
@src/tscollection/datasets/modules/electricity.py
@src/tscollection/datasets/modules/ucr.py
@src/tscollection/datasets/modules/uea.py
@src/tscollection/datasets/modules/_base/forecasting.py
@src/tscollection/datasets/modules/_base/classification.py
@tests/test_modules_forecasting.py
@.planning/phases/07-ddp-compliance/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DDP smoke test and isinstance elimination verification</name>
  <files>tests/test_ddp_compliance.py</files>
  <read_first>
    - .planning/phases/07-ddp-compliance/07-RESEARCH.md — Example C (DDP smoke test pattern), Pitfall 3 (mp.spawn requires top-level callable), Validation Architecture test map
    - .planning/phases/07-ddp-compliance/07-PATTERNS.md — test_ddp_compliance.py analog patterns (class-based integration tests, module-level worker)
    - tests/test_modules_forecasting.py — synthetic_csv_file fixture, integration test patterns
    - src/tscollection/datasets/modules/ett.py — ETTDataModule constructor and cache pattern
    - src/tscollection/datasets/modules/weather.py — WeatherModule constructor
    - src/tscollection/datasets/utils/cache.py — cache helper imports
  </read_first>
  <action>
    Create tests/test_ddp_compliance.py with:

    1. **Module-level `_ddp_forecasting_worker` function** (must be top-level for mp.spawn pickling, per Pitfall 3):
       - Signature: `def _ddp_forecasting_worker(rank: int, world_size: int, results_dir: str, csv_path: str) -> None:`
       - Initialize process group: `dist.init_process_group('gloo', rank=rank, world_size=world_size)`.
       - Rank 0: Instantiate ETTDataModule with synthetic CSV, call `prepare_data()` (writes cache).
       - Barrier: `dist.barrier()` to ensure cache written before other ranks read.
       - All ranks: Instantiate ETTDataModule (same params), call `setup(stage='fit')` (reads cache).
       - All ranks: Verify `_full_data_raw` is not None, `_train_data_samples` shape is identical across ranks.
       - All ranks: Write rank result to results_dir (shape, dtype) for verification.
       - Barrier: `dist.barrier()`.
       - Cleanup: `dist.destroy_process_group()`.

    2. **`test_ddp_forecasting_cache_round_trip`** — Uses `tmp_path` and `synthetic_csv_file` fixture:
       - Creates synthetic CSV via fixture pattern.
       - Calls `mp.spawn(_ddp_forecasting_worker, args=(2, str(tmp_path), str(csv_path)), nprocs=2, start_method='spawn')`.
       - After spawn completes, reads rank results from disk and verifies identical shapes.

    3. **`test_ddp_classification_cache_round_trip`** — Similar to forecasting test but uses UCR module:
       - Uses synthetic ARFF-like data or mocks.
       - Verifies cache write/read under DDP.

    4. **`test_isinstance_branch_elimination`** — Static verification:
       - Grep source files for `isinstance.*_full_data`.
       - Assert 0 matches.

    5. **`test_setup_idempotent_with_cache`** — Integration test:
       - Prepare data (writes cache), setup('fit'), record _train_data_samples.
       - Reset module, setup('fit') again.
       - Verify _train_data_samples identical (rebuilt from immutable _full_data_raw).

    Follow patterns from test_modules_forecasting.py (class-based integration tests, tmp_path fixtures).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ddp_compliance.py -x --tb=short</automated>
  </verify>
  <done>
    test_ddp_compliance.py exists with DDP smoke test, isinstance elimination check, and idempotency test. All pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full regression suite and update remaining tests</name>
  <files>tests/test_modules_forecasting.py</files>
  <read_first>
    - tests/test_modules_forecasting.py — full test file after Plans 05/06 updates
    - tests/test_modules_ucr.py — after Plan 07 updates
    - tests/test_modules_uea.py — after Plan 07 updates
    - tests/test_modules_base.py — after Plan 03 updates
    - tests/test_cache.py — after Plan 01 updates
  </read_first>
  <action>
    1. Fix any remaining test references to `_full_data` across all test files:
       - `tests/test_modules_forecasting.py`: Replace `_full_data` injections with `_full_data_raw` / `_full_data_scaled` as appropriate.
       - Any test that creates synthetic data for module testing must create cache files instead of injecting `_full_data`.

    2. Run full regression suite:
       - `uv run pytest tests/ -x --tb=short` — all tests must pass.

    3. Run lint check:
       - `uv run ruff check src/tscollection/datasets/` — must be clean.

    4. Verify isinstance elimination:
       - `grep -rn "isinstance.*_full_data" src/tscollection/datasets/ --include="*.py"` — must return 0 matches.

    5. If any tests fail, debug and fix (the plan assumes all Wave 4 plans completed correctly; this task catches integration issues).
  </action>
  <verify>
    <automated>uv run pytest tests/ -x --tb=short</automated>
  </verify>
  <done>
    Full regression suite passes (all tests green), ruff check clean, zero isinstance(_full_data) matches in source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache files (disk) | Cache files written by rank-0, read by all ranks — shared filesystem trust boundary |
| torch.load (scaler) | Deserialization of sklearn scalers via pickle — trusted source (self-written cache) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | metadata.json | Mitigated | Version check (load_metadata raises ValueError for version != 1) |
| T-07-02 | Tampering | cache npz files | Accept | Local development cache; no PII; cache key includes hash of params |
| T-07-03 | Repudiation | DDP rank results | Accept | Smoke test only; production DDP logging via Lightning Trainer |
| T-07-04 | Info Disclosure | torch.load scaler | Accept | Sklearn scalers contain no secrets; weights_only=False is required for sklearn objects |
| T-07-05 | Denial of Service | Cache growth | Accept | Manual cache cleanup; no automated eviction in v1 |
| T-07-SC | Tampering | pip/uv installs | Mitigated | No new packages; all deps already audited |
</threat_model>

<verification>
- DDP smoke test: `uv run pytest tests/test_ddp_compliance.py::test_ddp_forecasting_cache_round_trip -x`
- isinstance elimination: `grep -rn "isinstance.*_full_data" src/tscollection/datasets/ --include="*.py"` returns 0
- Full regression: `uv run pytest tests/ -x`
- Lint: `uv run ruff check src/tscollection/datasets/`
- Type check: `uv run ty check src/tscollection/datasets/` (if available)
</verification>

<success_criteria>
- DDP smoke test passes: 2 ranks (gloo) read identical state from cache written by rank-0
- isinstance(_full_data) returns 0 matches across all source files (ROADMAP DDP-02)
- prepare_dimensions() reads metadata.json without loading arrays (ROADMAP DDP-03)
- Second setup() produces identical output (ROADMAP DDP-04)
- Full regression suite: all tests pass
- ruff check clean on all modified files
</success_criteria>

<output>
Create `.planning/phases/07-ddp-compliance/07-08-SUMMARY.md` when done
</output>
