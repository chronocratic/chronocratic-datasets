---
phase: 08-code-quality-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/rbspaper/models/ts2vec/__init__.py
  - src/rbspaper/models/augmentation/__init__.py
  - src/rbspaper/models/encoding.py
  - src/rbspaper/pipeline/loggers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "uv run ty check reports zero errors in encoding.py (no unsupported-operator, invalid-type-form)"
    - "uv run ty check reports zero errors in pipeline/setup/model.py (no call-non-callable for TS2Vec)"
    - "uv run ty check reports no possibly-missing-submodule warnings for loggers.py"
    - "Direct import 'from src.rbspaper.models.ts2vec import TS2Vec' works at runtime"
    - "Direct import 'from src.rbspaper.models.augmentation import AugmentationMethod' works at runtime"
  artifacts:
    - path: "src/rbspaper/models/ts2vec/__init__.py"
      provides: "Direct TS2Vec import (no __getattr__)"
      contains: "from src.rbspaper.models.ts2vec.model import TS2Vec"
    - path: "src/rbspaper/models/augmentation/__init__.py"
      provides: "Direct AugmentationMethod import (no __getattr__)"
      contains: "from src.rbspaper.models.augmentation.strategies import AugmentationMethod"
    - path: "src/rbspaper/models/encoding.py"
      provides: "Widened _ModelType = pl.LightningModule"
      contains: "_ModelType = pl.LightningModule"
    - path: "src/rbspaper/pipeline/loggers.py"
      provides: "Correct pl.loggers.Logger references"
      min_lines: 236
  key_links:
    - from: "src/rbspaper/models/ts2vec/__init__.py"
      to: "src/rbspaper/models/ts2vec/model.py"
      via: "direct import"
      pattern: "from src\\.rbspaper\\.models\\.ts2vec\\.model import TS2Vec"
    - from: "src/rbspaper/models/encoding.py"
      to: "src/rbspaper/models/ts2vec/__init__.py"
      via: "import TS2Vec"
      pattern: "from src\\.rbspaper\\.models\\.ts2vec import TS2Vec"
---

<objective>
Remove lazy `__getattr__` imports and widen `_ModelType` to resolve cascading ty errors.
Purpose: The `ts2vec/__init__.py` lazy import is the root cause — it makes `TS2Vec` resolve as `object` at type-checking time, breaking unions, isinstance checks, and all parameter annotations downstream. Removing it + widening to `pl.LightningModule` eliminates 12 ty diagnostics.
Output: Clean ty/ruff on ts2vec/__init__.py, augmentation/__init__.py, encoding.py, setup/model.py, and loggers.py.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/phases/08-code-quality-audit/08-CONTEXT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/phases/08-code-quality-audit/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove lazy imports from ts2vec and augmentation __init__.py</name>
  <read_first>
    - src/rbspaper/models/ts2vec/__init__.py (current: __getattr__ lazy import)
    - src/rbspaper/models/augmentation/__init__.py (current: __getattr__ lazy import)
    - src/rbspaper/models/ts2vec/model.py (line 1-30: TS2Vec class definition to confirm direct import target)
    - src/rbspaper/models/augmentation/strategies.py (line 1-30: AugmentationMethod class definition)
  </read_first>
  <files>
    src/rbspaper/models/ts2vec/__init__.py
    src/rbspaper/models/augmentation/__init__.py
  </files>
  <action>
    Replace `ts2vec/__init__.py`:
    1. Delete the entire `__getattr__` function (lines 6-13).
    2. Add direct import: `from src.rbspaper.models.ts2vec.model import TS2Vec`
    3. Keep `__all__ = ['TS2Vec']`

    Replace `augmentation/__init__.py`:
    1. Delete the entire `__getattr__` function (lines 6-13).
    2. Add direct import: `from src.rbspaper.models.augmentation.strategies import AugmentationMethod`
    3. Keep `__all__ = ['AugmentationMethod']`

    Per D-01: The circular dependency that motivated lazy imports no longer exists — verified at runtime. The augmentation/PLC0415 ruff error inside CropShiftAugmentation.augment() uses a lazy import within a method body, NOT at module level, so it is unaffected by this change.
  </action>
  <verify>
    <automated>uv run python -c "from src.rbspaper.models.ts2vec import TS2Vec; print(TS2Vec)" && uv run python -c "from src.rbspaper.models.augmentation import AugmentationMethod; print(AugmentationMethod)"</automated>
    - grep -c '__getattr__' src/rbspaper/models/ts2vec/__init__.py == 0
    - grep -c '__getattr__' src/rbspaper/models/augmentation/__init__.py == 0
    - uv run ruff check src/rbspaper/models/ts2vec/__init__.py src/rbspaper/models/augmentation/__init__.py (no PLC0415 errors)
  </verify>
  <acceptance_criteria>
    1. `src/rbspaper/models/ts2vec/__init__.py` contains exactly: `__all__`, `from src.rbspaper.models.ts2vec.model import TS2Vec` — no `__getattr__`
    2. `src/rbspaper/models/augmentation/__init__.py` contains exactly: module docstring, `__all__`, `from src.rbspaper.models.augmentation.strategies import AugmentationMethod` — no `__getattr__`
    3. Runtime import `from src.rbspaper.models.ts2vec import TS2Vec` succeeds (exit 0)
    4. Runtime import `from src.rbspaper.models.augmentation import AugmentationMethod` succeeds (exit 0)
    5. `uv run ruff check` on both files reports zero PLC0415 errors
  </acceptance_criteria>
  <done>Both __init__.py files use direct imports, runtime verification passes, ruff clean on PLC0415</done>
</task>

<task type="auto">
  <name>Task 2: Widen _ModelType to pl.LightningModule in encoding.py</name>
  <read_first>
    - src/rbspaper/models/encoding.py (full file: current _ModelType union, all function signatures)
    - src/rbspaper/pipeline/core.py lines 520-550 (encode_data call sites showing pl.LightningModule typed callers)
  </read_first>
  <files>
    src/rbspaper/models/encoding.py
  </files>
  <action>
    Per D-01: Widen `_ModelType` from concrete union to `pl.LightningModule`.

    1. Add import: `import lightning.pytorch as pl`
    2. Replace `_ModelType = TS2Vec | AutoTCL | CoST` with `_ModelType = pl.LightningModule`
    3. Remove now-unused imports: `from src.rbspaper.models.ts2vec import TS2Vec`, `from src.rbspaper.models.autotcl import AutoTCL`, `from src.rbspaper.models.cost import CoST`

    Keep TS2Vec, AutoTCL, CoST as type hints in the internal dispatch functions (_ts2vec_encode, etc.) — they still work because the direct import from Task 1 resolves them properly. Only the public-facing `_ModelType` is widened.

    NOTE: The internal functions like `_ts2vec_encode(model: TS2Vec, ...)` are called ONLY after `isinstance(model, TS2Vec)` narrows the type. These keep their specific annotations for documentation and type precision. The widening applies only to the public `encode_data()` entry point and the generic helper functions.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/models/encoding.py src/rbspaper/pipeline/setup/model.py 2>&1 | grep -c 'error\|warning' || true</automated>
    - encoding.py contains `import lightning.pytorch as pl`
    - encoding.py contains `_ModelType = pl.LightningModule`
    - encoding.py still imports TS2Vec, AutoTCL, CoST for internal dispatch function signatures (they are used in isinstance checks and typed params)
    - `uv run ty check src/rbspaper/models/encoding.py` reports zero errors (no unsupported-operator, invalid-type-form, invalid-argument-type, invalid-method-override)
  </verify>
  <acceptance_criteria>
    1. `grep '_ModelType = pl.LightningModule' src/rbspaper/models/encoding.py` returns exactly 1 match
    2. `grep 'import lightning.pytorch as pl' src/rbspaper/models/encoding.py` returns exactly 1 match
    3. `uv run ty check src/rbspaper/models/encoding.py` reports zero errors (previously 7: unsupported-operator + 4x invalid-type-form + 2x invalid-argument-type)
    4. `uv run ty check src/rbspaper/pipeline/setup/model.py` reports zero errors (previously 1: call-non-callable)
    5. TS2Vec, AutoTCL, CoST imports retained in encoding.py for internal dispatch function annotations
  </acceptance_criteria>
  <done>_ModelType widened to pl.LightningModule, encoding.py and setup/model.py ty-clean</done>
</task>

<task type="auto">
  <name>Task 3: Fix pl.loggers.LightningLogger -> pl.loggers.Logger in loggers.py</name>
  <read_first>
    - src/rbspaper/pipeline/loggers.py (full file: find `pl.loggers.LightningLogger` occurrences)
  </read_first>
  <files>
    src/rbspaper/pipeline/loggers.py
  </files>
  <action>
    Per Lightning 2.x API: `pl.loggers.LightningLogger` was renamed to `pl.loggers.Logger`.

    Replace all 3 occurrences:
    - In `_find_wandb_logger`: `tuple[pl.loggers.LightningLogger, ...]` -> `tuple[pl.loggers.Logger, ...]`
    - In `_log_config_to_wandb`: `tuple[pl.loggers.LightningLogger, ...]` -> `tuple[pl.loggers.Logger, ...]`
    - In `_log_results_to_wandb`: `tuple[pl.loggers.LightningLogger, ...]` -> `tuple[pl.loggers.Logger, ...]`

    Use `sed` or targeted edits. Do NOT change anything else in loggers.py (BLE001 fixes are in a later plan).
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/pipeline/loggers.py 2>&1 | grep 'possibly-missing-submodule' | wc -l</automated>
    - grep -c 'LightningLogger' src/rbspaper/pipeline/loggers.py == 0
    - grep -c 'pl.loggers.Logger' src/rbspaper/pipeline/loggers.py >= 3
  </verify>
  <acceptance_criteria>
    1. `grep -c 'LightningLogger' src/rbspaper/pipeline/loggers.py` returns 0 (zero old references)
    2. `grep 'pl.loggers.Logger' src/rbspaper/pipeline/loggers.py` returns exactly 3 matches (function signatures)
    3. `uv run ty check src/rbspaper/pipeline/loggers.py` reports zero possibly-missing-submodule warnings
  </acceptance_criteria>
  <done>All 3 LightningLogger references replaced with Logger, ty clean on possibly-missing-submodule</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/models/encoding.py src/rbspaper/pipeline/setup/model.py src/rbspaper/models/ts2vec/__init__.py src/rbspaper/models/augmentation/__init__.py src/rbspaper/pipeline/loggers.py` — zero errors
- `uv run ruff check src/rbspaper/models/ts2vec/__init__.py src/rbspaper/models/augmentation/__init__.py` — zero PLC0415 errors
- `uv run python -c "from src.rbspaper.models.encoding import encode_data; print('OK')"` — import chain works
</verification>

<success_criteria>
1. ts2vec/__init__.py uses direct import (no __getattr__)
2. augmentation/__init__.py uses direct import (no __getattr__)
3. encoding.py _ModelType = pl.LightningModule
4. loggers.py references pl.loggers.Logger (not LightningLogger)
5. ty check on all modified files reports zero errors
6. ruff check on both __init__.py files reports zero PLC0415
7. Runtime import of TS2Vec, AugmentationMethod, encode_data all succeed
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — this plan modifies only static type annotations and import paths. No runtime behavior changes, no external service interactions.

## STRIDE Threat Register
Not applicable. Pure type contract cleanup with zero behavioral changes.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-01-SUMMARY.md`
</output>
