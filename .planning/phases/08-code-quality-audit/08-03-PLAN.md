---
phase: 08-code-quality-audit
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/rbspaper/models/augmentation/strategies.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "AugmentationMethod.augment() abstract signature accepts **kwargs"
    - "CropShiftAugmentation.augment() no longer has invalid-method-override ty error"
  artifacts:
    - path: "src/rbspaper/models/augmentation/strategies.py"
      provides: "Widened abstract augment method"
      contains: "def augment(self, data: torch.Tensor, **kwargs)"
  key_links:
    - from: "src/rbspaper/models/augmentation/strategies.py"
      to: "src/rbspaper/models/augmentation/strategies.py"
      via: "CropShiftAugmentation overrides AugmentationMethod.augment"
      pattern: "def augment.*\\*\\*kwargs"
---

<objective>
Widen AugmentationMethod.augment() abstract return type and add **kwargs to fix Liskov violation.
Purpose: Per D-10, CropShiftAugmentation.augment() returns `tuple[torch.Tensor, torch.Tensor, int]` and takes `temporal_unit: int = 0`, violating the abstract method signature. Widening the abstract method to accept `**kwargs` and returning `tuple[torch.Tensor, ...]` covers all subclasses.
Output: ty-clean augmentation/strategies.py for invalid-method-override.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen AugmentationMethod.augment() abstract signature</name>
  <read_first>
    - src/rbspaper/models/augmentation/strategies.py (full file: AugmentationMethod abstract class, all subclass overrides of augment)
  </read_first>
  <files>
    src/rbspaper/models/augmentation/strategies.py
  </files>
  <action>
    Per D-10: Widen the abstract `augment()` method to allow subclasses to extend parameters.

    Step 1 — Change the abstract method signature in `AugmentationMethod` class:
    Before (line 27):
    ```python
    def augment(self, data: torch.Tensor) -> torch.Tensor | tuple[torch.Tensor, ...]:
    ```
    After:
    ```python
    def augment(self, data: torch.Tensor, **kwargs: object) -> torch.Tensor | tuple[torch.Tensor, ...]:
    ```

    Step 2 — Remove the `ty: ignore[invalid-method-override]` comment from `CropShiftAugmentation.augment()` (line 93):
    Before:
    ```python
    ) -> tuple[torch.Tensor, torch.Tensor, int]:  # ty: ignore[invalid-method-override]
    ```
    After:
    ```python
    ) -> tuple[torch.Tensor, torch.Tensor, int]:
    ```

    Step 3 — Update `AutoTCLNeuralNetworkAugmentation.augment()` and `CosTRandomFunctionAugmentation.augment()` to also accept `**kwargs` for consistency (they don't use extra params but must match the widened abstract signature):
    Before:
    ```python
    def augment(self, data: torch.Tensor) -> torch.Tensor:
    ```
    After:
    ```python
    def augment(self, data: torch.Tensor, **kwargs: object) -> torch.Tensor:
    ```

    IMPORTANT: The `**kwargs` parameter must not break any existing callers. CropShiftAugmentation is called with `temporal_unit` keyword only from TS2Vec's training loop. All other callers pass just `data`. The `**kwargs` absorbs the difference without changing call sites.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/models/augmentation/strategies.py 2>&1 | grep 'invalid-method-override' | wc -l</automated>
    - grep 'ty: ignore' src/rbspaper/models/augmentation/strategies.py | grep 'invalid-method-override' returns 0 matches
    - grep 'def augment(self, data: torch.Tensor, \*\*kwargs: object)' src/rbspaper/models/augmentation/strategies.py returns 4 matches (1 abstract + 3 concrete)
  </verify>
  <acceptance_criteria>
    1. `AugmentationMethod.augment()` signature is `def augment(self, data: torch.Tensor, **kwargs: object) -> torch.Tensor | tuple[torch.Tensor, ...]`
    2. `CropShiftAugmentation.augment()` has no `ty: ignore[invalid-method-override]` comment
    3. `AutoTCLNeuralNetworkAugmentation.augment()` accepts `**kwargs: object`
    4. `CosTRandomFunctionAugmentation.augment()` accepts `**kwargs: object`
    5. `uv run ty check src/rbspaper/models/augmentation/strategies.py` reports zero invalid-method-override errors
  </acceptance_criteria>
  <done>All augment methods accept **kwargs, no ty ignore on invalid-method-override, ty clean</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/models/augmentation/strategies.py` — zero errors
- `uv run ruff check src/rbspaper/models/augmentation/strategies.py` — zero errors (existing noqa for PLC0415, S311 remain — later plan)
</verification>

<success_criteria>
1. AugmentationMethod.augment() accepts `**kwargs: object`
2. All concrete overrides accept `**kwargs: object`
3. No `ty: ignore[invalid-method-override]` in strategies.py
4. ty check clean on this file
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — abstract method widening, no behavioral changes.

## STRIDE Threat Register
Not applicable. Pure type contract change.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-03-SUMMARY.md`
</output>
