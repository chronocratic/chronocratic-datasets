---
phase: 08-code-quality-audit
plan: 06
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/rbspaper/pipeline/config.py
  - src/rbspaper/pipeline/core.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "DataConfig.data_module type is pl.LightningDataModule"
    - "No ty: ignore comments in core.py lines 530, 537, 544, 671"
  artifacts:
    - path: "src/rbspaper/pipeline/config.py"
      provides: "Widened data_module type"
      contains: "data_module: pl.LightningDataModule"
    - path: "src/rbspaper/pipeline/core.py"
      provides: "Clean encode_data call sites"
      min_lines: 1223
  key_links:
    - from: "src/rbspaper/pipeline/config.py"
      to: "src/rbspaper/pipeline/core.py"
      via: "DataConfig used in ExperimentPipelineConfig"
      pattern: "data_module: pl\\.LightningDataModule"
---

<objective>
Widen DataConfig.data_module type and remove unused ty: ignore from core.py.
Purpose: Per D-02, widen `data_module` from `BaseTimeSeriesDataModule` to `pl.LightningDataModule`. Per D-13, remove 4 unused `ty: ignore[invalid-argument-type]` from core.py (3 became unused after Wave 1 fixes, 1 is resolved by the _ModelType widening).
Output: ty-clean config.py and core.py for data_module + encode_data calls.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen data_module type in config.py and clean core.py ty: ignore</name>
  <read_first>
    - src/rbspaper/pipeline/config.py (find `data_module` field in DataConfig, lines 1-20 for TYPE_CHECKING block)
    - src/rbspaper/pipeline/core.py (find `ty: ignore` comments on encode_data call sites)
  </read_first>
  <files>
    src/rbspaper/pipeline/config.py
    src/rbspaper/pipeline/core.py
  </files>
  <action>
    **config.py changes (D-02):**
    1. In `DataConfig` dataclass, change `data_module` field type:
       Before (line 271): `data_module: BaseTimeSeriesDataModule`
       After: `data_module: pl.LightningDataModule`
    2. Ensure `pl.LightningDataModule` is importable. The TYPE_CHECKING block already imports `pl`:
       `import lightning.pytorch as pl` (line 13). The type is used only at static analysis time.
    3. Remove `BaseTimeSeriesDataModule` from the TYPE_CHECKING import (line 18) if it is no longer referenced elsewhere in config.py.

    **core.py changes (D-13):**
    Remove all `ty: ignore[invalid-argument-type]` comments from encode_data call sites in core.py. Find by searching for `ty: ignore` in the file. All should be on `model=model,` argument lines. After Wave 1 widening, these are no longer needed.

    After Wave 1's `_ModelType = pl.LightningModule`, these ignores are no longer needed because `pl.LightningModule` passed to `encode_data()` matches the widened type.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/pipeline/config.py src/rbspaper/pipeline/core.py 2>&1 | grep 'error\|warning' | wc -l</automated>
    - grep 'data_module: pl.LightningDataModule' src/rbspaper/pipeline/config.py returns 1 match
    - grep -v '^#' src/rbspaper/pipeline/core.py | grep -c 'ty: ignore' returns 0 (or only remaining if any)
  </verify>
  <acceptance_criteria>
    1. `DataConfig.data_module` type is `pl.LightningDataModule` in config.py
    2. `BaseTimeSeriesDataModule` import removed from config.py TYPE_CHECKING block (if unused)
    3. Zero `ty: ignore[invalid-argument-type]` comments in core.py (all removed)
    4. `uv run ty check src/rbspaper/pipeline/config.py` reports zero errors/warnings
    5. `uv run ty check src/rbspaper/pipeline/core.py` reports zero errors/warnings
  </acceptance_criteria>
  <done>data_module widened to pl.LightningDataModule, core.py ty: ignore removed, both files ty clean</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/pipeline/config.py src/rbspaper/pipeline/core.py` — zero errors/warnings
- `uv run ruff check src/rbspaper/pipeline/config.py src/rbspaper/pipeline/core.py` — zero errors
</verification>

<success_criteria>
1. DataConfig.data_module is pl.LightningDataModule
2. Zero ty: ignore comments in core.py
3. ty check clean on both files
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — type widening and suppression removal.

## STRIDE Threat Register
Not applicable. Pure type contract changes.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-06-SUMMARY.md`
</output>
