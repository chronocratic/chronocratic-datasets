---
phase: 08-code-quality-audit
plan: 09
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03, 08-04, 08-05, 08-06, 08-07, 08-08]
files_modified:
  - ruff.toml
  - src/rbspaper/pipeline/core.py
  - src/rbspaper/models/cost/model.py
  - src/rbspaper/pipeline/loggers.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ruff.toml has no per-file-ignores for core.py (C901, PLR0912 removed)"
    - "ruff.toml has no per-file-ignores for adapters (ANN401, TC001 removed)"
    - "core.py run_experiment_pipeline has inline noqa: C901, PLR0912, PLR0915"
    - "All structural noqa comments have # why: explanations"
    - "cost/model.py CoSTAugmentationMode is in TYPE_CHECKING block"
  artifacts:
    - path: "ruff.toml"
      provides: "Cleaned per-file-ignores"
      min_lines: 77
    - path: "src/rbspaper/pipeline/core.py"
      provides: "Inline noqa on complex function"
      min_lines: 1223
  key_links:
    - from: "ruff.toml"
      to: "src/rbspaper/pipeline/core.py"
      via: "inline noqa replaces per-file-ignore"
      pattern: "noqa: C901"
---

<objective>
Remove dead adapters, clean ruff.toml per-file-ignores, add inline noqa, fix TC001, add why comments to structural noqa.
Purpose: Per D-06, D-08, D-07: Remove dead code, move complexity ignores from ruff.toml to inline noqa, fix TC001 import ordering, add why comments to all remaining noqa directives.
Output: ruff-clean, adapters removed, ruff.toml cleaned.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove adapters package and clean ruff.toml + inline noqa</name>
  <read_first>
    - ruff.toml (full file: current per-file-ignores section, lines 48-55)
    - src/rbspaper/pipeline/core.py (lines 100-102: current noqa on run_experiment_pipeline)
    - src/rbspaper/models/cost/model.py (lines 1-25: TYPE_CHECKING block and CoSTAugmentationMode import)
    - src/rbspaper/pipeline/loggers.py (lines 64, 195: PLC0415 noqa)
  </read_first>
  <files>
    ruff.toml
    src/rbspaper/pipeline/core.py
    src/rbspaper/models/cost/model.py
    src/rbspaper/pipeline/loggers.py
  </files>
  <action>
    **Step 1 — Remove dead adapters package (D-06):**
    ```bash
    rm -rf src/rbspaper/adapters/
    ```
    Remove the per-file-ignore line from ruff.toml:
    Delete: `"src/rbspaper/adapters/*.py" = ["ANN401", "TC001"]`

    **Step 2 — Move core.py ignores to inline noqa (D-08):**
    Delete from ruff.toml: `"src/rbspaper/pipeline/core.py" = ["C901", "PLR0912"]`

    In core.py, update the `run_experiment_pipeline` function signature (line 101):
    Before: `def run_experiment_pipeline(  # noqa: PLR0915`
    After: `def run_experiment_pipeline(  # noqa: C901, PLR0912, PLR0915 # why: orchestrator function with multiple sequential steps; refactoring would lose readability`

    **Step 3 — Fix TC001 in cost/model.py:**
    Move `CoSTAugmentationMode` import into the TYPE_CHECKING block:
    Before (line 21):
    ```python
    from src.rbspaper.models.augmentation.enums import CoSTAugmentationMode
    ```
    After (inside `if TYPE_CHECKING:` block):
    ```python
    from src.rbspaper.models.augmentation.enums import CoSTAugmentationMode
    ```
    Verify CoSTAugmentationMode is only used as a type annotation, not at runtime.

    **Step 4 — Add why comments to remaining structural noqa:**
    Review all noqa comments in the modified files and ensure each has a `# why:` explanation:
    - core.py line 50: `noqa: F401` -> `noqa: F401 # why: intentional barrel re-export`
    - core.py line 101: already handled above
    - loggers.py line 64: `noqa: PLC0415` -> `noqa: PLC0415 # why: lazy wandb import to avoid hard dependency`
    - loggers.py line 195: `noqa: PLC0415` -> `noqa: PLC0415 # why: lazy wandb import to avoid hard dependency`

    Also verify other structural noqa files have why comments (from CONTEXT.md canonical refs):
    - models/utils.py:19 `N812` -> already has `# why: torch.nn.functional convention`
    - models/losses.py:22 `N812` -> already has `# why: torch.nn.functional convention`
    - attacks/_backend.py:77,103 `PLC0415` -> already has `# why:` or needs adding
    - models/layers/general.py:88 `SLF001` -> already has `# why: PyTorch internals`

    If any are missing, add appropriate why comments.
  </action>
  <verify>
    <automated>uv run ruff check src/rbspaper runners/py/runner.py 2>&1 | head -10</automated>
    - test -d src/rbspaper/adapters/ returns non-zero (directory removed)
    - grep 'core.py' ruff.toml returns 0 matches (per-file-ignore removed)
    - grep 'adapters' ruff.toml returns 0 matches (per-file-ignore removed)
    - grep 'noqa: C901' src/rbspaper/pipeline/core.py returns 1 match (inline noqa)
    - grep 'CoSTAugmentationMode' src/rbspaper/models/cost/model.py is inside TYPE_CHECKING block
  </verify>
  <acceptance_criteria>
    1. `src/rbspaper/adapters/` directory removed (rm -rf)
    2. `ruff.toml` has zero per-file-ignores for `core.py` and `adapters`
    3. `core.py` `run_experiment_pipeline` has inline `noqa: C901, PLR0912, PLR0915` with `# why:` comment
    4. `CoSTAugmentationMode` import moved into TYPE_CHECKING block in cost/model.py
    5. All remaining noqa directives in core.py and loggers.py have `# why:` comments
    6. `uv run ruff check src/rbspaper` reports zero errors
    7. `uv run ruff check runners/py/runner.py` reports zero errors
  </acceptance_criteria>
  <done>Adapters removed, ruff.toml cleaned, inline noqa with why comments, TC001 fixed, ruff clean</done>
</task>

<task type="auto">
  <name>Task 2: Final verification — ty, ruff, pytest</name>
  <read_first>
    - ruff.toml (verify cleaned state)
  </read_first>
  <files>
    ruff.toml
  </files>
  <action>
    Run comprehensive verification across the entire codebase:

    1. **ty check:** `uv run ty check src/rbspaper runners/py/runner.py`
       Expected: Only 4 documented limitation ignores remain:
       - autotcl/model.py — call-non-callable (AveragedModel, 2 occurrences)
       - ts2vec/model.py — call-non-callable (AveragedModel, 1 occurrence)
       - data/datasets/strategies.py — invalid-assignment (isinstance narrowing, 1 occurrence)

    2. **ruff check:** `uv run ruff check src/rbspaper runners/py/runner.py`
       Expected: Zero errors

    3. **ruff format check:** `uv run ruff format --check src/rbspaper runners/py/runner.py`
       Expected: All files formatted

    4. **pytest:** `uv run pytest -x -q`
       Expected: All 143 tests pass

    5. **Import smoke test:** `uv run python -c "from src.rbspaper.models.encoding import encode_data; from src.rbspaper.pipeline.state import PipelineStateDict; print('OK')"`

    Document the final diagnostic count in the summary.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper runners/py/runner.py 2>&1 | grep -c 'error\[' || true</automated>
    <automated>uv run ruff check src/rbspaper runners/py/runner.py 2>&1 | grep -c 'Found' || true</automated>
    <automated>uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    1. `uv run ty check` reports zero unignored errors (only 4 documented limitation ignores remain)
    2. `uv run ruff check` reports zero errors (Found 0 errors)
    3. `uv run pytest -x` — all 143 tests pass (exit 0)
    4. Import smoke test succeeds
  </acceptance_criteria>
  <done>All verification gates pass: ty clean (documented limitations only), ruff clean, pytest clean</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper runners/py/runner.py` — only documented limitation ignores
- `uv run ruff check src/rbspaper runners/py/runner.py` — zero errors
- `uv run pytest -x` — all tests pass
</verification>

<success_criteria>
1. adapters/ package removed
2. ruff.toml has no per-file-ignores for core.py or adapters
3. core.py complexity warnings suppressed via inline noqa with why comments
4. TC001 fixed (CoSTAugmentationMode in TYPE_CHECKING)
5. All structural noqa have why comments
6. ty check: only documented limitation ignores remain
7. ruff check: zero errors
8. pytest: all tests pass
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — dead code removal and linting cleanup.

## STRIDE Threat Register
Not applicable. Removing unused code, no behavioral changes.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-09-SUMMARY.md`
</output>
