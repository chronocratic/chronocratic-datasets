---
context: phase
phase: 03-pipeline-hardening
task: UAT test 2
total_tasks: 12
status: in_progress
last_updated: 2026-05-06T17:12:13Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Rebase-only merging — All branch integrations MUST use `git rebase` instead of `git merge --no-ff`. Never create merge commits.
- [ ] CONSTRAINT: Executor agents can miss tasks — Always verify actual code changes match ALL plan tasks, not just the commits.
- [ ] CONSTRAINT: gsd-sdk config clobber — Always verify config.json after any `gsd-sdk query config-set` call; use individual `config-set` per key, never bulk write.

**Do not proceed until all boxes are checked.**

## Current State

Phase 03 all 15 plans complete. UAT in progress — 1/12 tests passed (cold start smoke). Remaining tests blocked by runner import issue. User paused session to return later.

## Completed Work

- 15/15 plans executed (waves 1-5 complete)
- UAT session started, 12 tests extracted from 15 SUMMARY.md files
- Test 1 PASS: 60 pytest tests pass (test_pipeline_core.py + test_pipeline_state.py)
- Note: retry_step decorator has real 15s backoff waits — tests run slowly

## Remaining Work

- Tests 2-10: Runner CLI tests — blocked, need PYTHONPATH fix
- Test 11: Retry decorator — unit testable, can pass without runner
- Test 12: Config hash determinism — unit testable, can pass without runner

## Blockers

- Runner import chain: `experiment_instances` module not discoverable without `PYTHONPATH`
- `rbspaper-run` entry point broken — `runners/` directory lacks `__init__.py`
- Created `runners/py/__init__.py` during session but doesn't solve experiment_instances path

## Required Reading

1. `.planning/phases/03-pipeline-hardening/03-UAT.md` — current UAT state
2. `.planning/HANDOFF.json` — structured handoff

## Next Action

Resume with `/gsd:verify-work 3` — either fix PYTHONPATH for runner tests or mark remaining blocked and complete.
