---
phase: 03-pipeline-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements: [REQ-03]
must_haves:
  truths:
    - "tenacity is listed in project.dependencies (not dev deps)"
    - "uv sync completes without error after adding tenacity"
  artifacts:
    - path: "pyproject.toml"
      provides: "tenacity dependency"
      contains: "tenacity"
  key_links:
    - from: "pyproject.toml dependencies"
      to: "src/rbspaper/pipeline/core.py"
      via: "import tenacity"
      pattern: "tenacity>="
---

<objective>
Add tenacity to project dependencies for retry-with-backoff support.

Purpose: Implement D-04 (retry with backoff) requires tenacity library. Must be a runtime dependency (not dev) since the pipeline uses it at execution time.

Output: tenacity added to [project] dependencies in pyproject.toml.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tenacity to [project] dependencies</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 10-26 — the [project] dependencies block)
  </read_first>
  <acceptance_criteria>
    - "tenacity>=9.1.4" appears in the [project] dependencies list (NOT in [dependency-groups] dev)
    - Entry is alphabetically ordered within the dependencies list (after seaborn, before torch)
    - ruff check still passes on the rest of the codebase
  </acceptance_criteria>
  <action>
    In pyproject.toml, add tenacity to the [project] dependencies block. The dependency should be placed alphabetically: after "seaborn>=0.13.0" and before "torch>=2.4.0,<=2.8.0".

    Exact line to add:
    ```
        "tenacity>=9.1.4",
    ```

    The full dependencies block should now include:
    ```toml
    dependencies = [
        "numpy>=2.1,<3.0.0",
        "pandas>=2.2.0",
        "scipy>=1.13.0",
        "scikit-learn>=1.5.0",
        "matplotlib>=3.9.0",
        "seaborn>=0.13.0",
        "tenacity>=9.1.4",
        "torch>=2.4.0,<=2.8.0",
        ...
    ]
    ```

    IMPORTANT: Add to [project] dependencies, NOT to [dependency-groups] dev. This is a runtime dependency used by the pipeline core, not a development-only tool.
  </action>
  <verify>
    <automated>grep '"tenacity>=9.1.4"' pyproject.toml</automated>
  </verify>
  <done>
    tenacity>=9.1.4 is in [project] dependencies (not dev deps), alphabetically ordered. File is valid TOML.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tenacity import</name>
  <files>pyproject.toml</files>
  <read_first>
    - No additional files needed
  </read_first>
  <acceptance_criteria>
    - `python -c "import tenacity"` succeeds
    - tenacity version is >= 9.1.4
  </acceptance_criteria>
  <action>
    Run `uv sync` to install the new dependency, then verify the import works:

    ```bash
    uv sync
    uv run python -c "import tenacity; print(tenacity.__version__)"
    ```

    If uv sync fails due to existing environment, note the error but the plan is still complete if pyproject.toml is correct. The dependency declaration is the deliverable; resolution happens at execution time.
  </action>
  <verify>
    <automated>uv run python -c "from tenacity import retry, stop_after_attempt, wait_exponential; print('OK')"</automated>
  </verify>
  <done>
    tenacity is importable with retry, stop_after_attempt, wait_exponential available.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries — this plan only adds a dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | tenacity dependency | accept | Standard PyPI package with stable API; no security-sensitive usage |
</threat_model>

<verification>
1. `grep '"tenacity>=9.1.4"' pyproject.toml` -- dependency present in [project] section
2. `uv run python -c "from tenacity import retry, stop_after_attempt, wait_exponential"` -- import works
</verification>

<success_criteria>
1. tenacity>=9.1.4 is in [project] dependencies
2. tenacity imports successfully after uv sync
3. pyproject.toml is valid TOML
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-hardening/03-04-SUMMARY.md`
</output>
