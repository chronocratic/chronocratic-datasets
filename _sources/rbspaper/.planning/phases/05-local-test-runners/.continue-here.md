---
context: phase
phase: 05-local-test-runners
task: UAT test 8
total_tasks: 15 UAT tests
status: in_progress
last_updated: 2026-05-07T16:38:50Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: rbspaper-run entry point broken — The editable install `.pth` only adds `src/` to `sys.path`, but `runners/` lives at the project root. `ModuleNotFoundError: No module named 'runners'` crashes any call through the CLI entry point. Direct invocation via `uv run python runners/py/runner.py` works fine.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Editable install scope mismatch | `pyproject.toml` tools.setuptools.packages only declares `src.rbspaper.*`, so `runners/` is invisible to entry points. Fix requires adding the project root to `sys.path` in the entry point or adjusting package discovery. | blocking | Verify entry points resolve with `uv run rbspaper-run --help` before marking UAT complete. |

## Current State

Phase 05 UAT in progress. 3 plans fully executed. 7 static pre-checks completed:
- **5 passed:** runner.py zero print() calls, module logger correct, `__init__.py` exists, `config.sh.example` clean, `.gitignore` correct, 111 tests pass
- **2 failed (blocker):** `rbspaper-run --help` and `--list_experiments` crash with `ModuleNotFoundError`
- **6 pending:** tests 8-13 (bash runner help, config creation, dataset expansion, fraction sampling)
- **2 blocked:** tests 10, 14 (require DATA_ROOT with actual dataset files)

Working tree is clean — no merge commits on branch (rebased linear).

## Completed Work

- All 3 phase plans executed (package marker, logging conversion, local runners)
- Deep code review findings applied
- UAT file created with 15 tests derived from 3 plan summaries
- Static verification of all code artifacts
- Rebase: 2 merge commits replaced with 25 linear cherry-picks, tree verified identical

## Remaining Work

- **Test 8:** `./runners/bash/local_single.sh --help` — verify usage text
- **Test 9:** Auto-creates `config.sh` from template
- **Test 10:** End-to-end single experiment run (blocked until DATA_ROOT configured)
- **Test 11:** `./runners/bash/local_batch.sh --help` — verify usage text
- **Test 12:** Dataset spec expansion (range/list/all)
- **Test 13:** Fraction sampling
- **Test 14:** Aggregate summary table (blocked until DATA_ROOT configured)
- **Fix:** `rbspaper-run` entry point resolution (blocker for tests 1-2)

## Required Reading (in order)

1. `.planning/phases/05-local-test-runners/05-UAT.md` — current test state and gap details
2. `.planning/phases/05-local-test-runners/05-VERIFICATION.md` — full verification with data-flow traces
3. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses

## Infrastructure State

- Clean working tree, no background processes
- Virtual environment at `.venv/` — editable install scope only covers `src/`
- `config.sh` not yet created (clean state for test 9)

## Context

The key question for the next session: should the `rbspaper-run` entry point fix be scoped to Phase 05 or deferred? The runner works when invoked directly, only the setuptools entry point is broken. The fix likely involves adjusting package discovery in `pyproject.toml` to include `runners/`.

## Next Action

Continue UAT from test 8 — run `./runners/bash/local_single.sh --help` and present to user. After pending tests complete, diagnose and plan the entry point fix.
