---
phase: 08-code-quality-audit
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/rbspaper/models/cost/model.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "CoST._get_slice() return type matches EncodingFunctionalityMixin._get_slice() signature"
    - "uv run ty check reports no invalid-method-override for cost/model.py:350"
  artifacts:
    - path: "src/rbspaper/models/cost/model.py"
      provides: "Correct _get_slice return annotation"
      contains: "def _get_slice(self, _sliding_padding: int, _sliding_length: int) -> slice | None:"
  key_links:
    - from: "src/rbspaper/models/cost/model.py"
      to: "src/rbspaper/models/abstract/encoding_functionality_mixin.py"
      via: "@override decorator on _get_slice"
      pattern: "def _get_slice.*-> slice \\| None"
---

<objective>
Fix CoST._get_slice() return annotation from `-> None` to `-> slice | None`.
Purpose: Per D-09, the override violates Liskov substitution — the mixin declares `-> slice | None` but CoST returns `-> None`. One-line fix.
Output: ty-clean cost/model.py for _get_slice.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/src/rbspaper/models/abstract/encoding_functionality_mixin.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix _get_slice return type annotation</name>
  <read_first>
    - src/rbspaper/models/cost/model.py lines 348-352 (current method with `-> None`)
    - src/rbspaper/models/abstract/encoding_functionality_mixin.py lines 55-57 (base method signature: `-> slice | None`)
  </read_first>
  <files>
    src/rbspaper/models/cost/model.py
  </files>
  <action>
    Per D-09: Change the return type annotation on line 350 from `-> None` to `-> slice | None`.

    Before:
    ```python
    @override
    def _get_slice(self, _sliding_padding: int, _sliding_length: int) -> None:
        return None
    ```

    After:
    ```python
    @override
    def _get_slice(self, _sliding_padding: int, _sliding_length: int) -> slice | None:
        return None
    ```

    This is a one-line change. The body (`return None`) remains unchanged — `None` is already a valid value of `slice | None`.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/models/cost/model.py 2>&1 | grep 'invalid-method-override' | wc -l</automated>
    - grep 'def _get_slice.*-> slice | None' src/rbspaper/models/cost/model.py returns 1 match
  </verify>
  <acceptance_criteria>
    1. Line 350 contains `def _get_slice(self, _sliding_padding: int, _sliding_length: int) -> slice | None:`
    2. `uv run ty check src/rbspaper/models/cost/model.py` reports zero invalid-method-override errors
    3. Method body unchanged: still `return None`
  </acceptance_criteria>
  <done>_get_slice return annotation is `slice | None`, ty clean on invalid-method-override</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/models/cost/model.py` — zero errors for invalid-method-override
</verification>

<success_criteria>
1. CoST._get_slice() return type is `slice | None`
2. ty reports no invalid-method-override for this file
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — one-line type annotation fix.

## STRIDE Threat Register
Not applicable. Pure annotation change, zero behavioral impact.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-02-SUMMARY.md`
</output>
