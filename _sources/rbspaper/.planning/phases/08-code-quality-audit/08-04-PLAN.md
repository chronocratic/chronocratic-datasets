---
phase: 08-code-quality-audit
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/rbspaper/pipeline/state.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "PipelineStateDict TypedDict exists with keys: completed, config_hash, started_at, last_updated"
    - "from_dict() accepts PipelineStateDict instead of dict[str, object]"
    - "No ty: ignore comments remain in state.py"
  artifacts:
    - path: "src/rbspaper/pipeline/state.py"
      provides: "TypedDict for pipeline state serialization"
      contains: "class PipelineStateDict(TypedDict)"
  key_links:
    - from: "src/rbspaper/pipeline/state.py"
      to: "src/rbspaper/pipeline/state.py"
      via: "from_dict accepts PipelineStateDict"
      pattern: "def from_dict\\(.*, data: PipelineStateDict"
---

<objective>
Add PipelineStateDict TypedDict to state.py to replace 4 ty: ignore comments.
Purpose: Per D-04, use TypedDict for pipeline state deserialization so ty can verify key existence and types at compile time instead of suppressing dict access errors.
Output: ty-clean state.py, no unused noqa: UP017.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TypedDict and remove ty: ignore + stale noqa from state.py</name>
  <read_first>
    - src/rbspaper/pipeline/state.py (full file: current from_dict signature, ty: ignore lines 138-141, noqa: UP017 lines 73, 101)
  </read_first>
  <files>
    src/rbspaper/pipeline/state.py
  </files>
  <action>
    Per D-04: Add TypedDict for state serialization and remove all suppressions.

    Step 1 — Add TypedDict import and definition. Add `TypedDict` to the existing `from typing` import (or add a new import if typing is not imported yet). Then add the TypedDict class:

    ```python
    class PipelineStateDict(TypedDict):
        """Typed dict for deserializing PipelineState from JSON."""
        completed: dict[str, list[str]]
        config_hash: str
        started_at: str
        last_updated: str
    ```

    Step 2 — Update `from_dict` signature:
    Before:
    ```python
    def from_dict(*, data: dict[str, object]) -> PipelineState:
    ```
    After:
    ```python
    def from_dict(*, data: PipelineStateDict) -> PipelineState:
    ```

    Step 3 — Remove 4 `ty: ignore[invalid-argument-type]` comments from the `PipelineState(...)` constructor call inside `from_dict`. Find by searching for `ty: ignore` in state.py.

    Step 4 — Remove all stale `noqa: UP017` comments from state.py. Per RESEARCH.md Pitfall 3, the code already uses `datetime.now(timezone.utc)` which is the correct modern form — the noqa was added when the code used `datetime.utcnow()` and is now stale. Find by searching for `UP017` in state.py.

    Updated `load_pipeline_state`: the `json.load(fh)` result is `dict[str, Any]` which coerces to `PipelineStateDict` at the `from_dict` call. If ty complains, cast: `from_dict(data=cast('PipelineStateDict', data))`. But TypedDict should accept a broader dict input since it's a structural type.

    Also update `__all__` to include `PipelineStateDict`.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/pipeline/state.py 2>&1 | grep 'error\|warning' | wc -l</automated>
    - grep -c 'ty: ignore' src/rbspaper/pipeline/state.py == 0
    - grep -c 'UP017' src/rbspaper/pipeline/state.py == 0
    - grep 'class PipelineStateDict' src/rbspaper/pipeline/state.py returns 1 match
    - grep 'def from_dict' src/rbspaper/pipeline/state.py contains 'PipelineStateDict'
    - uv run ruff check src/rbspaper/pipeline/state.py reports zero UP017 errors
  </verify>
  <acceptance_criteria>
    1. `PipelineStateDict(TypedDict)` class exists with keys: completed (dict[str, list[str]]), config_hash (str), started_at (str), last_updated (str)
    2. `from_dict` accepts `data: PipelineStateDict`
    3. Zero `ty: ignore` comments in state.py
    4. Zero `noqa: UP017` comments in state.py
    5. `uv run ty check src/rbspaper/pipeline/state.py` reports zero errors/warnings
    6. `uv run ruff check src/rbspaper/pipeline/state.py` reports zero errors
    7. `PipelineStateDict` is in `__all__`
  </acceptance_criteria>
  <done>TypedDict added, all ty: ignore removed, all UP017 noqa removed, ty + ruff clean</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/pipeline/state.py` — zero errors/warnings
- `uv run ruff check src/rbspaper/pipeline/state.py` — zero errors
- `uv run pytest test/test_pipeline_state.py -x` — state serialization tests pass
</verification>

<success_criteria>
1. PipelineStateDict TypedDict exists with correct keys/types
2. from_dict uses PipelineStateDict
3. Zero ty: ignore comments in state.py
4. Zero UP017 noqa comments in state.py
5. ty + ruff clean on state.py
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — TypedDict is a compile-time construct.

## STRIDE Threat Register
Not applicable. Pure type contract improvement.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-04-SUMMARY.md`
</output>
