---
phase: 08-code-quality-audit
plan: 05
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/rbspaper/attacks/_backend.py
  - src/rbspaper/attacks/batch.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "run_torchattacks accepts dict[str, AttackKwargValue] instead of dict[str, object]"
    - "run_attack_backend accepts dict[str, AttackKwargValue] instead of dict[str, object]"
    - "attack_dataset dataloader type is compatible with DataLoader call site in batch.py"
  artifacts:
    - path: "src/rbspaper/attacks/_backend.py"
      provides: "AttackKwargValue-typed attack_kwargs parameter"
      contains: "attack_kwargs: dict[str, AttackKwargValue]"
    - path: "src/rbspaper/attacks/batch.py"
      provides: "Clean dataloader pass to attack_dataset"
      min_lines: 184
  key_links:
    - from: "src/rbspaper/attacks/functional.py"
      to: "src/rbspaper/attacks/_backend.py"
      via: "AttackKwargValue type alias import"
      pattern: "AttackKwargValue"
---

<objective>
Align attack_kwargs value type and fix batch.py dataloader type.
Purpose: Per D-03, change `dict[str, object]` to `dict[str, AttackKwargValue]` in _backend.py to match functional.py. Per batch.py, widen the dataloader parameter type to accept the unparameterized DataLoader from `DataLoader(dataset=TensorDataset(...))`.
Output: ty-clean _backend.py and batch.py.
</objective>

<execution_context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/workflows/execute-plan.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/PROJECT.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/ROADMAP.md
@/Users/skaf/VSCodeProjects/robust_time_series_representations/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align attack_kwargs type in _backend.py and fix batch.py dataloader</name>
  <read_first>
    - src/rbspaper/attacks/_backend.py (full file: lines 131, 166, 229 have dict[str, object])
    - src/rbspaper/attacks/functional.py line 24 (AttackKwargValue definition)
    - src/rbspaper/attacks/batch.py lines 89-98 (DataLoader construction + ty: ignore)
    - src/rbspaper/attacks/batch.py lines 119-126 (attack_dataset function signature with DataLoader[tuple[Tensor, Tensor]])
  </read_first>
  <files>
    src/rbspaper/attacks/_backend.py
    src/rbspaper/attacks/batch.py
  </files>
  <action>
    Per D-03: Import `AttackKwargValue` and replace `dict[str, object]` in _backend.py.

    **_backend.py changes:**
    1. Add TYPE_CHECKING import for `AttackKwargValue`:
       ```python
       if TYPE_CHECKING:
           from src.rbspaper.attacks.functional import AttackKwargValue
       ```
       (Add to existing imports block or create new one.)

    2. Replace `dict[str, object]` with `dict[str, AttackKwargValue]` in:
       - `run_torchattacks()` parameter (line 131): `attack_kwargs: dict[str, object]` -> `dict[str, AttackKwargValue]`
       - `run_art()` parameter (line 166): `attack_kwargs: dict[str, object]` -> `dict[str, AttackKwargValue]`
       - `run_attack_backend()` parameter (line 229): `attack_kwargs: dict[str, object]` -> `dict[str, AttackKwargValue]`

    **batch.py changes:**
    1. The `attack_dataset()` function declares `dataloader: DataLoader[tuple[Tensor, Tensor]]` (line 122), but the caller in `batched_attack()` passes `DataLoader(dataset=TensorDataset(...))` without type params.
    2. Widen `attack_dataset`'s dataloader parameter to `DataLoader` (unparameterized) to accept any DataLoader:
       Before (line 122): `dataloader: DataLoader[tuple[Tensor, Tensor]],`
       After: `dataloader: DataLoader,`
    3. Remove the `ty: ignore[invalid-argument-type]` from line 98 in `batched_attack()`.
  </action>
  <verify>
    <automated>uv run ty check src/rbspaper/attacks/_backend.py src/rbspaper/attacks/batch.py src/rbspaper/attacks/functional.py 2>&1 | grep 'error' | wc -l</automated>
    - grep 'AttackKwargValue' src/rbspaper/attacks/_backend.py returns >= 4 matches (import + 3 usages)
    - grep -c 'ty: ignore' src/rbspaper/attacks/batch.py == 0
    - grep 'dataloader: DataLoader,' src/rbspaper/attacks/batch.py returns 1 match (unparameterized)
  </verify>
  <acceptance_criteria>
    1. `_backend.py` imports `AttackKwargValue` from `functional.py` (via TYPE_CHECKING)
    2. `run_torchattacks` parameter is `attack_kwargs: dict[str, AttackKwargValue]`
    3. `run_art` parameter is `attack_kwargs: dict[str, AttackKwargValue]`
    4. `run_attack_backend` parameter is `attack_kwargs: dict[str, AttackKwargValue]`
    5. `batch.py` `attack_dataset` dataloader parameter is `DataLoader` (unparameterized)
    6. Zero `ty: ignore` comments in batch.py
    7. `uv run ty check` reports zero errors on _backend.py, batch.py, functional.py
  </acceptance_criteria>
  <done>AttackKwargValue used in _backend.py, DataLoader unparameterized in batch.py, all ty clean</done>
</task>

</tasks>

<verification>
- `uv run ty check src/rbspaper/attacks/_backend.py src/rbspaper/attacks/batch.py src/rbspaper/attacks/functional.py` — zero errors
- `uv run ruff check src/rbspaper/attacks/_backend.py src/rbspaper/attacks/batch.py` — zero errors
</verification>

<success_criteria>
1. attack_kwargs uses AttackKwargValue in _backend.py (3 functions)
2. dataloader parameter is unparameterized DataLoader in batch.py
3. No ty: ignore in batch.py
4. ty check clean on _backend.py, batch.py, functional.py
</success_criteria>

<threat_model>
## Trust Boundaries
No trust boundaries crossed — type annotation changes only.

## STRIDE Threat Register
Not applicable. Pure type contract alignment.
</threat_model>

<output>
After completion, create `.planning/phases/08-code-quality-audit/08-05-SUMMARY.md`
</output>
