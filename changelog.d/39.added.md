feat(dataloader): unified loader_mode injection for forecasting and classification modules: Introduce `loader_mode` as an instance-level property on both base classes (`BaseForecastingTimeSeriesDataModule` / `BaseClassificationTimeSeriesDataModule`) with validated setters, init-time defaults, and call-time overrides. All 5 concrete modules (ETT, Electricity, Weather, UCR, UEA) propagate `loader_mode` through `super().__init__()` and use None-to-self fallback in dataloader methods. **What ships:** - `loader_mode` property + setter on forecasting base (default: `RAW_SERIES`) and classification base (default: `SAMPLE_LABEL`) - All 15 dataloader methods accept `loader_mode: Enum | None = None` with None → self fallback - Classification dataloaders renamed `mode` → `loader_mode` (clean break, no back-compat) - 25 new TDD tests (`test_modules_loader_mode_injection.py`) + 2 additional reset-preservation tests - Resolved-mode logic centralized in `_build_dataloader` (forecasting) and `_resolve_loader_mode` (classification) - CI workflows aligned with `chronocratic-models`: improved changelog-fragment automation + uv-based docs job + push-to-main test trigger **Code review:** 6 findings (3 Warning, 3 Info) — all fixed. Review marked clean. **Verification:** 344 tests pass · ruff clean · 15/15 must-haves verified ---
