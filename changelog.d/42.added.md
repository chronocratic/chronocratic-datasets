feat: add loader_strict_batch_size for uniform batch sizes; clean code and update CI: Added `loader_strict_batch_size` as an `__init__` parameter on `BaseTimeSeriesDataModule` (propagated through all 5 concrete modules) so dataloaders always produce uniform batch sizes via cycle-padding. Cleaned barrel exports and updated all CI workflows to match chronocratic-models (`uv` tooling, `ty` type checking, protected files).
