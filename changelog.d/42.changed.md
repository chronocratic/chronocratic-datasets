ci(workflows): align CI with chronocratic-models — uv tooling, ty type checking, protected files: Standardized all CI workflows on `uv` for dependency and tool management (matching chronocratic-models), added `ty` type checking to the lint pipeline, introduced a Protected Files Check workflow for `pyproject.toml` and `.vscode/` changes, and updated the contributing guide accordingly.
